# Teaching and Product Notes

- All 644 lessons across 28 tracks have individual content sign-offs as of 2026-09-14: 644 complete, zero pending, zero changed revisions. Completed the remaining AI and interview review with 68 worked checkpoints and targeted code/terminology fixes; senior scope remains approximately ten years of experience. Content, catalog, 9,372 local links and TypeScript checks pass. Teach guided explanation/attempt/feedback/delayed recall; existing helpers and stdlib checks avoided new dependencies. Nothing installed, committed, pushed or deployed. Live browser/framework/data/cloud/provider/GitHub Pages verification remains separate and unclaimed. Historical progress entries below are superseded by this completion entry.

- Kubernetes individually reviewed through 0576 on 2026-09-14: 576 complete, 68 pending. Thirty worked checkpoints, contextual terminology and trace corrections validated locally. No installations or cluster operations. AI 0577–0617 read; AI improvements/sign-off and interviews remain.

- DevOps/Docker individual review completed through 0546 on 2026-09-13: 546 complete, 98 pending. Added 40 checkpoints; corrected migration prerequisite order, writable-layer/registry/image terminology and trace titles. Local validation/link suite passes; no installs, builds or infrastructure mutations. Next pending 0547 (Kubernetes); read through 0552.

- AWS individual review completed through 0506 on 2026-09-13: 506 complete, 138 pending. Replaced cloud placeholder definitions, added concrete worked criteria and corrected stream-partition meaning and telemetry/security diagrams. Local audit/catalog/link checks pass. No installations or cloud executions. Next pending 0507 (DevOps); read through 0512.

- Individual review signed off through 0480 on 2026-09-13 (480 complete, 164 pending). Quality/security checkpoints and AWS foundation terminology improved; audit-hash newline guard and contextual regressions pass. AWS 0481–0484 starters/terms/diagrams read but pending changes and sign-off. No installations or external integrations.

- Individual API/distributed/domain/event review completed through 0464 on 2026-09-13. Added 44 checkpoints, corrected network diagram routing, capacity/acknowledgement/2PC/Raft/sharding terminology and canonical Kafka offset newline rejection. Local models, catalog and 9,372 links pass; no installation or live protocol/infrastructure testing. 464 signed off; next pending 0465 (quality/reliability).

- API individual review completed through 0420 on 2026-09-13. Added 24 worked checkpoints, fixed newline acceptance in ID/webhook/precondition guards and HTTP whitespace handling, and corrected log terminology/testing diagram routing. Local review, catalog and link validation pass; no installations or live integrations. Next pending: 0421; 420 signed off, 224 pending.

- Redis/data capstone individual review completed through 0396 on 2026-09-13. Added 16 worked checkpoints and corrected Redis database/connection inheritance, statistical error wording and trace labels. Offline RESP/cache, generation and catalog/link checks pass; no Redis/database/extension installation or external recovery. Next pending: 0397 (API/distributed systems); starters and effective definitions already read through 0420, not yet signed off.

- PostgreSQL individual review completed through 0380 on 2026-09-13. Added 35 worked checkpoints, independent-fixture limits and contextual corrections for SSI locks, B-tree deduplication, joins, HOT, partition routing and physical backups. Existing adapter checks and generation/catalog/link suite pass; no database/extension installation or SQL execution. Next pending: 0381 (Redis).

- FastAPI individual review completed through 0345 on 2026-09-13. Added 43 checkpoints and integration limits; corrected scope/lifespan/callback definitions, bounded query tags and reflected request IDs, and settings/delivery/diagnostic routing. AST, extracted request-ID policy, existing ASGI body checks, catalog and link suite pass. Framework/browser/database/deployment execution remains unverified; no installs. Next pending entry: 0346 (PostgreSQL/data systems).

- Python individual review completed through 0302 on 2026-09-13. Added 43 worked checkpoints and explicit fixture/integration limits; corrected contextual containers, named tuples, start methods and testing properties definitions, timeout retry wording, Git helper flags and architecture trace routing. Generation, local review, validation and 9,372 links pass. No installations or native/process/framework integration. Next pending entry: 0303 (FastAPI).

- Node individual review completed through 0259 on 2026-09-13. Added 46 worked checkpoints and explicit integration limits; corrected ESM microtask ordering, cork/drain definitions, revision newline guard and test/path trace routing. Existing local suite passes. Next pending entry: 0260 (Python). No installs, external DNS/TLS runs, profiler targets or full-service deployment.

- React individual review completed through 0213 on 2026-09-12. Added 43 worked checkpoints; corrected event/routing terminology, Hook exception, derived selection, tooltip positioning/timing, optional preload rejection and Fiber trace title. Existing recipes remain integration requirements, not executed applications. Reducer/static regressions and catalog/link checks pass. Next pending entry: 0214 (Node). No installs or browser/deployment verification.

- Individual review advanced through 0125 on 2026-09-12; prior 0126–0170 TypeScript records retained. Next pending entry is 0171 (React). JavaScript fixes include sparse-array length, newline regex validation, resizable buffers, property ownership terminology, offline error/security fixtures and architecture diagram routing. Local validation/link checks pass; browser, module-graph, worker and repository integrations remain explicitly unverified. No installs. Re-run audit for current signed-off counts.

- JavaScript individual review advanced through 0090 on 2026-09-12. Added eleven worked checkpoints and explicit core/extension boundaries; corrected shared scope-starter assignment wording and strengthened the expected TDZ failure check. Generation, content audit, validation and 9,372 local link checks pass. Next pending entry: 0091. No installs or live/browser integration verification.

- Individual sign-offs advanced on 2026-09-11/12: reviewed and improved 0001–0079, retaining the 45 prior TypeScript records. See REVIEW-EVIDENCE.md for individual findings, executed checks and explicit scope limitations; regenerate REVIEW-CHECKLIST.md for current counts. Next pending catalog entry is 0080. No installations, commits, pushes or live deployment verification. Teach guided concrete feedback and core/extension boundaries; authoring is not learner mastery. Existing stdlib checks were extended rather than adding dependencies. The congestion-control source changed to RFC 5681, so the validator's matching source expectation was updated after it correctly detected the old expectation.

- Completion tracking established 2026-09-11: REVIEW-CHECKLIST.md is generated from the manifest and manual lesson-review-status.json. Imported only the 45 explicit TypeScript content passes; 599 other lessons await final individual sign-off, not 599 untouched lessons. Existing batch work must be reused. Complete the five content criteria and individual evidence before adding a sign-off; changed revisions reopen review. Integration testing and live deployment have separate checkboxes and do not silently expand the no-install content task.

- API boundary follow-up 2026-09-11: 0416/0418 now execute denied tenant/scope/action/classification and writable-field cases without missing helpers. 0419 uses synthetic observations and bounded metric labels; 0420 uses real node:test assertions for frozen keyset traversal and a tie-breaker counterexample. Live authentication, SSRF, audit and database behavior remain outside these local checks. The later follow-up below replaces leaderless/discovery sketches with executable local models, not full integrations. Retain these limits when reporting completion; do not repeat already completed starter batches.

- API continuation 2026-09-11: completed the paused CAP/poison/event-replay documentation and continued into 0407/0408/0410/0440/0442/0444/0446. Removed unsupplied helpers from filter construction, compatibility testing, prepared-decision dispatch, version reconciliation, consistency counterexample and ring movement. Added bounded fixtures and regression checks, including stale/invalid outcomes; JSON hypermedia is directly parseable. Primary sources are now topic-specific for CAP, linearizability, Dynamo-style versioning, prepared transactions and CQRS. No database, broker or provider integration ran. Preserved the GitHub Pages path fix and rechecked all deployment prefixes.

- GitHub Pages fix 2026-09-10: the manifest retained prototype-relative ../../lessons paths, causing the root lesson viewer and several reference links to escape the repository URL prefix. The generator now emits site-root-relative lessons paths; updated the senior reference link and validator consumers. Regenerated manifest/reference pages and added scripts/check-pages-links.mjs, which verifies 9,372 local targets at three deployment prefixes. No deployment, commit, push or installation performed.

- API review paused for the Pages report: 0437/0445/0452 now have runnable offline poison-message, CAP-history and event-replay checks, replacing undefined helpers. Four routing regressions prevent SSE substring, retry/latency and CQRS topic collisions. Local review/validation passed before the deployment-path fix. Complete any remaining report/source documentation when resuming content work; do not claim full database/broker/consensus integration.

- Frontend diagram follow-up 2026-09-10: inspected routing for all 46 JavaScript and 43 React titles. Testing lessons no longer inherit an event-loop/render trace from secondary keywords. Controlled inputs and React output security now have dedicated owner/edit/submit and trust-boundary flows, with four regression entries. Existing browser-dependent experiments remain explicitly unexecuted; no UI libraries or browsers installed.

- AI evaluation follow-up 2026-09-10: benchmark 0614 now retains per-attempt records, exposes per-slice quality and failed calls, distinguishes unknown charges from zero cost and labels observed score spread as neither standard error nor a confidence interval. Offline assertions cover the hidden format regression, timeout, invalid response cost and repetition bounds. Two deterministic cases repeated five times are explicitly not ten independent cases or a real model benchmark. Reviewed diagram routing for 0577–0621; ingestion 0595 and prompt-injection 0615 now have mechanism-specific traces with regressions. Nothing installed or sent to a provider.

- Infrastructure diagram follow-up 2026-09-10: inspected routing for all 102 AWS/DevOps/Docker/Kubernetes titles. Replaced unrelated delivery/storage traces for Linux/network diagnosis, observability, capacity and Kubernetes debugging with an evidence-first diagnostic flow. Six new regressions bring the cross-track matrix to 37. This is a routing review, not 102 executed integration labs.

- PostgreSQL diagram follow-up 2026-09-10: primary-title routing stops EXPLAIN and performance tuning inheriting a WAL crash-recovery diagram. Added pooling, authentication/authorization/RLS and measurement-driven diagnostics traces, with five routing regressions (31 in the cross-track matrix). The mapping for all 51 data-system lesson titles was inspected; several broader conceptual diagrams remain intentionally shared.

- PostgreSQL starter follow-up 2026-09-10: reviewed the specialized PostgreSQL branches through hybrid retrieval. Fixed shell/INI syntax inside SQL, a STABLE function in a generated column, a misleading single-session MVCC exercise, missing invariant recheck in the serializable doctor example and an unused logical slot that could retain WAL. Replaced the shell retry loop (which lost failure status) with a bounded SQLSTATE-aware Python adapter; offline checks cover eventual success, exhaustion, nonretryable errors and invalid bounds. Clarified independent schemas/parameter binding, idle replay timestamps, DDL/sequence replication limits, certificate verification and vector ranking versus hybrid fusion. PostgreSQL SQL and server behavior remain unexecuted; no installations. Continue beyond these reviewed starter branches rather than repeating them.

- Teach-loop follow-up 2026-09-10: every lesson now requests next-day closed-notes explanation, a failure prediction and a changed design constraint, followed by comparison and corrective practice. Removed the orientation question's residual claim to demonstrate mastery without changing progress behavior. No learner achievement is inferred from these authoring changes.

- Infrastructure/copy-boundary follow-up 2026-09-10: moved prediction/observation notes outside all 644 code blocks, so copying JSON no longer includes invalid generated comments. Added a curriculum-wide regression and kept the SQS fixture directly JSON-parseable. Kubernetes Service/resource examples no longer mix executable shell with YAML. Reviewed the specialized DevOps/Docker/Kubernetes starters: clarified rollout pseudocode and per-stage gates, Terraform secret-bearing plans and exit codes, non-root versus rootless, NetworkPolicy DNS/additive scope, Deployment deadline versus rollback, HPA request-relative CPU and Pod security fragment placement. Shell digest validation now has offline valid/invalid checks; no deploy adapter executes. These checks do not certify cluster, cloud, provider or container integrations; nothing installed. Broader individual explanation review remains separate from the complete definition/reuse pass.

- AWS follow-up 2026-09-09: SQS 0497 now supplies a locally parsed CloudFormation JSON template instead of fragile nested CLI JSON. S3 0490 distinguishes a no-version-ID delete marker from permanently deleting a specified version. CloudWatch 0500 explains that the example requires a published custom burn metric, count-weighted aggregation, notification actions and explicit missing-data policy; it is not a complete SLO alert. Bedrock 0505 checks all text blocks and stopReason through an extracted stdlib-only function, rejecting partial/tool/guardrail/empty outputs; no boto3 import or model call runs in the test. Corrected RDS diagram routing away from IAM (26th diagram regression). Next deeper infrastructure work should continue beyond these known fixes; don't repeat completed Node/interview starter checks or call local schema checks cloud integration.

- Interview completion follow-up 2026-09-09: all 22 JavaScript starters in the 23-lesson interview track now execute; the remaining profile lesson is Markdown. Replaced unsupplied interview/recording/evidence/demo helpers with an assertion-bearing pair-sum rehearsal and explicit human worksheets. Mock review distinguishes demonstrated, needs-practice and not-observed evidence, with adjustable delayed recall rather than automatic grades. Application deduplication preserves query/fragment posting IDs and uses an unambiguous tuple key. This closes the known interview-starter execution gaps, not the broader individual factual review of all 644 lessons. No installations or learner records changed.

- Further follow-up 2026-09-09: worker lifecycle now additionally runs REAL local worker threads for result, exception, exit-without-result and deadline termination; transfer/shared-memory/pool semantics remain outside that check. TCP framing now stops after write(false), resumes buffered frames on drain and checks fragment/buffer limits with a socket fake. Timer failures are observed immediately alongside the deadline. Added the 25th diagram regression for the previously unreachable L4/L7 trace. Reviewed shared infrastructure/AI definitions and corrected optional Docker stage naming, Service variants, taint effects, configured audit coverage, persisted versus in-memory agent state, MCP transport-specific sessions, vector-storage durability assumptions and webhook-effect deduplication boundaries. These supersede older notes claiming worker checks are adapter-only.

- Continued review 2026-09-09: closed the known Node gzip staging, HTTP/2 shutdown and local-queue follow-ups. Gzip tests exercise success, missing source, pre-cancellation, concurrent replacements and preservation of unrelated temporary files; HTTP/2 session-close/deadline tests use local adapters, not TLS. The queue is an explicitly unsafe, assertion-bearing counterexample for changed intent and ambiguous effects. Added bounded fetch-body checks, completed the Node test-runner fixture and clarified partial ingestion commits. Audited diagram selection across the 644-title inventory and added 24 principal-topic routing regressions across JavaScript, Node, Python, FastAPI, Docker and Kubernetes. Corrected stream/native-execution overclaims, FastAPI cleanup scope, HTTPS redirect/CORS explanations and coroutine/generator distinctions. No installations, browser tests, database integrations or learner-mastery claims. Remaining broader content review is not equivalent to these selected checks.

- Node/JavaScript follow-up 2026-09-08: 31 JavaScript module examples execute; corrected strict numeric parsing, aggregate integer overflow, bounded-map admission and generated regex escaping. Node checks now cover timer iteration, UTF-8 split chunks, byte-mode readable cleanup, HTTP parser status classification, read-only Git child execution and one-shot worker wrapper lifecycle (including falsy abort reasons). Worker lifecycle uses an adapter, not actual thread scheduling; HTTP checks do not establish socket behavior. Remaining identified Node review targets: gzip staging ownership, HTTP/2 shutdown/HTTP1 handling, queue scope and test-runner fixture completeness. No installations.

- Continued review 2026-09-08: all 12 systems-foundations starters execute, with five specific mechanism diagrams and fixes for progress, locking, framing and replacement boundaries. Added compiler/runtime checks for 12 software-design + four service/domain starters and the explicitly LF-only JSON streaming fixture; original 26 TS-track checks still pass. Corrected security-operation webhook SDK plumbing, version-pinned uploads, per-attempt lease identities and exact-byte audit hashing; adapter checks do not establish real Stripe/S3/DB behavior. Interview starters received truthful-fiction labels, exact-host screening, corrected generated regex/comments and separate recurring/first-year arithmetic; 16 standalone snippets execute (some smoke-only). AI checks cover tool intent replay, vector dimensions/recall, context goal retention, scripted step limits, partial MCP frames and extracted LangGraph approval logic. React source review corrected hook placement, cross-parent key claims, stale-room callbacks, client-module boundaries, form labels/awaiting and native dialog modality. React/browser/provider integrations remain unexecuted; nothing installed. Do not restart these reviewed starter batches or describe local fakes as integration certification.

- Follow-up review 2026-09-08: reviewed Python and FastAPI starter branches and continued API/distributed correctness checks. Corrected Python queue shutdown, comparisons, non-finite money, invalid countdowns, byte/text boundaries, atomic-write cleanup and capstone replay; 36 single-file Python starters execute (some smoke-only), plus a sequential capstone adapter check. All 41 Python-format and 42 FastAPI Python-format starters parse. FastAPI fixes cover bounded ASGI bodies, redacted errors, tenant checks, lifespan cleanup, output encoding, idempotency, OAuth caveats, CSRF binding, streaming cancellation, logging and bounded benchmark task creation. Only the framework-free body limiter executes; framework integrations are unavailable and nothing was installed. API fixes cover If-Match, signed raw webhook bytes, deprecation dates, complete schema references, outbox claims, awaited saga writes, lease allocation and a deliberately partial Raft precheck. Regression checks cover the executable parts, not real database/broker semantics. Current combined review requires Python 3.12+; earlier 3.10 notes are historical. Broader lesson-specific factual/diagram review and external integration evidence remain distinct from the completed definition/reuse pass.

- API breaker/delivery follow-up 2026-09-07: 0431 now executes a scoped half-open probe lifecycle with explicit missing full-breaker features; removed the implication that configuration objects enforce dependency pools. 0433 uses a valid-format synthetic traceparent and distinguishes delivery ownership from exclusive processing. 0435 specifies scoped durable inbox/intent and acknowledgment contracts, with sequential adapter checks for duplicates, mismatched data, failed effects, lost acknowledgments and replay. No database/broker integration ran; no installations.

- API measurement/admission follow-up 2026-09-07: 0426 now checks both vector-dominance directions, equality, receive merge and input preservation, with explicit fixed-node/small-counter limits. 0427 replaces an undefined load generator and invalid missing-time arithmetic with a runnable synthetic measurement exercise: success-only percentiles, timeouts, unsent requests, scheduling delay and invalid-input checks. 0430 validates admission limits, fixes mutable-limit bypass, and tests rejection plus slot restoration after synchronous/asynchronous errors. All three execute locally; no real load or distributed-system integration was run.

- API reliability follow-up 2026-09-07: corrected 0428 monotonic deadline units, fractional timer delays and cooperative-cancellation claims; 0429 now uses the standard abortable timer rather than leaking per-sleep listeners and checks cancellation before attempting work. Both execute self-checks. 0434 now stops a partition on handler/commit failure instead of committing past a gap; three scripted-adapter scenarios check this and offsets above JS safe-integer range. 0436 also preserves decimal offset precision and states its partition/auto-commit assumptions. No Kafka/network integration ran; broader API explanation/diagram review remains.

- Redis data/measurement follow-up 2026-09-07: reviewed 0381/0383/0384/0385/0388. Added bounded fixtures, exact list/rank/count and repeat-run expectations, bitmap allocation warnings, distinction between slow-log execution time and client latency, and benchmark measurement limits. Reduced default load and retained full latency output. All Redis starters 0381–0395 have now received targeted review across the recent batches; this is not full lesson-by-lesson factual certification or Redis execution. Next deeper work should move beyond Redis rather than restarting these starter reviews.

- Redis protocol/cluster/ACL follow-up 2026-09-07: 0382 now has an offline Python byte-framing exercise with assertions, not a network-dependent shell transcript. 0391 demonstrates why failed SET NX does not cancel a queued HSET and specifies owning-node/connection scope. 0395 uses a separate tenant-scoped lab identity, explicit allowed/denied checks, and separate admin commands. All three have dedicated diagrams with routing checks. Redis execution remains unavailable and installations remain prohibited.

- Redis follow-up 2026-09-07: reviewed and corrected 0386/0387/0389/0390/0392/0393/0394. Added concrete transaction-error, TTL, replication, stream-retention and fencing counterexamples; removed an executable in-place AOF repair command. Cache adapter now uses tenant-scoped exact-version reads, rejects wrong versions, tolerates cache errors and propagates database errors. Nine adapter scenarios pass; no Redis server/client is installed or executed. Keep integration verification separate from these local control-flow tests.

- Follow-up review 2026-09-07: user explicitly prohibits installations. Corrected 0380 claim/replay and stale-update control flow; five adapter-contract scenarios execute in the existing runner, without claiming PostgreSQL execution. Replaced 0396's unsafe unconditional job writes with a scoped integration contract and failure evidence. Both now have a command-replay diagram. Corrected the shared transaction diagram's snapshot-at-BEGIN claim. Real database concurrency, rollback, relay and recovery checks remain unexecuted.

- Current handoff, 2026-09-07 (supersedes earlier counts): completed the curriculum-wide definition and shared-starter pass across 644 lessons / 28 tracks. Zero fallback definitions; 83 retained shared starters all have distinct exercises, zero unexplained reuse. Replaced unrelated foundation, web, React, infrastructure, ML/LLM and integration placeholders with topic-specific experiments or clearly labelled assignments. Fixed substantive Node, SQL, Lambda, Docker and CI issues. `CONTENT-REVIEW.md` records exact checks and every lesson. All 26 selected TypeScript checks pass. This is not line-by-line factual certification or execution of every lab; PostgreSQL, Docker, Kubernetes, browser and provider integrations need their required environments. No learner mastery is inferred. Earlier entries below are historical.

- Follow-up 2026-09-07: replaced the unused shared test helper in CS lessons 0018–0021 and 0050 with five topic-specific runnable exercises: comparison counts, data-structure workload, connectivity, greedy counterexample/DP, and oracle-based interview verification. All 33 CS examples now execute assertions, enforced by the audit. Shared-starter count is now 217. These overview labs explicitly limit their scope rather than claiming implementations of every title subtopic. Continue from the remaining report entries.

- 2026-09-07 breadth pass: all 644 lessons have concrete title-term definitions; zero generic fallbacks remain. The audit now enforces this and runs computer-science examples (28 assertion-bearing, five smoke-only), all 12 LLD examples, and the JS primitive-values lab. Python 3.10+ is required; this machine uses `LESSON_PYTHON=python3.13`. Fixed quickselect mutation, two-pointer expected trace length, non-finite money acceptance, and misleading outbox wording. Context checks distinguish broker/network partitions and HTTP/2/TLS sessions. Still outstanding: deeper lesson-specific explanation/diagram checks, 222 shared-starter teaching purposes, and external integration execution. Do not describe the full curriculum as fully verified.

- TypeScript track content-review pass complete (2026-09-06): all 45 lessons, 0126–0170, now have concrete definitions, plain models, senior reasoning checkpoints, and explicit practice scope. See `TYPESCRIPT-CONTENT-REVIEW.md` for every lesson and limitations. This supersedes the earlier per-lesson resume points.
- Verification: `node scripts/check-typescript-lessons.mjs` checks 26 generated snippets with the installed TypeScript 5.3.3 and runtime assertions/probes. The other 19 are counterexamples, recipes, integration sketches, or projects; they were not executed end-to-end. Structural validation covers all 644 lessons. Content review is not learner mastery or production certification.
- Continue the wider curriculum from the findings in `CONTENT-REVIEW.md`; do not restart the TypeScript content pass. Integration implementation is a separate exercise, not silently marked complete. User prefers no routine progress messages and conserving weekly allowance; latest scope explicitly requested the entire TypeScript track at once. Quiz completion remains unchanged.
- Senior target confirmed on 2026-09-06: around ten years of experience. This supersedes the prior basic-familiarity assumption as a general learner profile; unfamiliar topics still need plain explanations.
- Use the teach skill for content review. Assess mechanisms, counterexamples, alternatives, changed constraints, operational ownership, and technical leadership. Years of experience and catalog completion do not establish mastery.
- See `CONTENT-REVIEW.md` for the full lesson audit and remaining authoring gaps, and `reference/senior-interview-practice.html` for the 28 worked track cases. The legacy auto-check is only an orientation/progress interaction; a switch to written self-assessment is awaiting the learner's preference.
- Optimize the curriculum for international full-stack AI engineering interviews, portfolio credibility, and relocation-friendly hiring.
- Go deep: teach internals, failure modes, tradeoffs, debugging, and production behavior alongside practical implementation.
- Include JavaScript and Node.js as first-class deep tracks, not only as prerequisites for React.
- Use focused lessons with code, analogies, high-contrast visuals, blackboard diagrams, and one mastery check. Beginner lessons can take 15–35 minutes.
- Use a dark, high-contrast focus-mode interface with IBM Plex Mono Nerd Font and comfortably readable text.
- Keep `PLAN.md` synchronized whenever product or curriculum decisions change.
- Pre-generate every mapped topic through Codex; do not generate lessons in the browser or on application startup.
- Treat JavaScript as a complete deep-dive curriculum, including specification literacy, values, execution and object models, functions and closures, modules, asynchronous scheduling, cancellation, resource management, binary and shared memory, engine internals, performance, security, testing, debugging, production architecture, extensive code, practical exercises, and interview preparation.
- Treat TypeScript as a complete deep-dive curriculum, including compiler internals, inference, structural relationships, narrowing, generics, advanced type transformations, modules, declarations, runtime validation, framework integration, testing, project scaling, performance, upgrades, safe AI-assisted development, production architecture, extensive code, practical exercises, and interview preparation.
- Treat Node.js as a complete deep-dive curriculum, including V8 and libuv architecture, event-loop scheduling, the worker pool, binary data, streams and backpressure, files and networking, HTTP, modules, package behavior, processes, workers, security, testing, diagnostics, memory, performance, native boundaries, process lifecycle, production architecture, extensive code, practical exercises, and interview preparation.
- Treat React as a complete deep-dive curriculum, including every current stable React and React DOM hook with examples, runtime internals, server features, practical exercises, and extensive code where the concept requires it.
- Treat FastAPI as a complete deep-dive curriculum, including ASGI and Starlette internals, the full documented request/response and dependency surface, security, data, testing, operations, deployment, extensive code, practical exercises, and interview preparation.
- Treat Python as a complete deep-dive curriculum, including its execution and object models, language protocols, typing, concurrency, memory, testing, packaging, native boundaries, security, production architecture, extensive code, practical exercises, and interview preparation.
- Treat Cloud and AWS as a complete independent deep-dive curriculum, including shared responsibility, accounts, IAM, networking, edge delivery, compute, storage, databases, events, operations, infrastructure as code, reliability, cost, Bedrock, SageMaker, extensive code, practical exercises, and interview preparation.
- Treat DevOps as a complete independent deep-dive curriculum, including delivery flow, Linux, networking, shell, Git, CI/CD, software supply-chain security, release strategies, observability, incidents, infrastructure as code, platform engineering, extensive code, practical exercises, and interview preparation.
- Treat Docker as a complete independent deep-dive curriculum, including engine and image internals, BuildKit, process behavior, isolation, networking, storage, Compose, observability, registries, multi-platform builds, hardening, extensive code, practical exercises, and interview preparation.
- Treat Kubernetes as a complete independent deep-dive curriculum, including API machinery, control-plane reconciliation, scheduling, workloads, networking, storage, configuration, security, autoscaling, observability, extensibility, cluster operations, EKS, extensive code, practical exercises, and interview preparation.
- Treat PostgreSQL, Redis, and Data Systems as a complete independent deep-dive curriculum, including relational modeling, SQL, planner and storage internals, MVCC, isolation, indexing, maintenance, WAL, replication, recovery, security, observability, performance, Redis structures, persistence, clustering, streams, caching, coordination, extensive code, practical failure experiments, and interview preparation.
- Treat API Design and Distributed Systems as a complete independent deep-dive curriculum, including HTTP and schema contracts, protocol styles, API evolution, security, explicit L4/L7 load balancing, streaming, partial failure, clocks, deadlines, retries, overload, messaging, workflows, replication, consistency, consensus, sharding, multi-region operation, system-design interviews, extensive code, practical fault experiments, and interview preparation.
- Treat International Interviews and Relocation Readiness as a complete practical module, including role targeting, ATS resumes, LinkedIn and GitHub, job portals, employer verification, application tracking, ethical local automation, recruiter screens, coding and technical loops, system design, AI, behavioral and situational model answers, technical storytelling, influence without authority, ethical persuasion, portfolio presentations, networking, referrals, relocation research, sponsorship, salary and offer negotiation, and measured mock practice. Never teach deception, coercion, scraping, automated engagement, or mass application submission.
- Keep entry explanations accessible for unfamiliar topics across all 644 lessons and 28 tracks; allow an experienced learner to proceed directly to retrieval and senior practice.
- Use human language based on ASD-STE100 Simplified Technical English. Prefer short active sentences, complete words, consistent technical nouns, gradual detail, concrete examples, and one instruction per sentence.
- Define every title-level term once in plain language, then connect the terms with one mechanism explanation.
- Keep the beginner foundation, four-stage walkthrough, practical steps, code-reading guidance, common mistakes, failure diagnosis, and interview answer frame concise and human. Do not add agendas, outcome cards, or repeated templates.
- Keep lesson headings compact; generated lesson titles use a responsive 22–34 px range.
- Do not repeat the track goal and generic interview-readiness introduction beneath every lesson title.
- Omit generic content headings that only restate their section label.
- Add each lesson's content revision to its iframe URL so regenerated lesson HTML cannot remain stale in the browser cache.
- Cover LangChain v1, LangGraph, and reproducible LLM benchmarking through dedicated practical lessons, while still teaching the underlying framework-independent mechanisms.
- Cover MCP, vector databases and their index and storage internals, AI frontend engineering, production webhooks, application file storage, durable background jobs, audit logs, and admin systems through dedicated practical lessons with beginner definitions, failure experiments, extensive code, and interview preparation.
- Treat software design as a dedicated practical track. Cover clean code, DRY, KISS, YAGNI, cohesion, coupling, SOLID, composition, GoF pattern families, safe refactoring, and architecture patterns without teaching principles as dogma or adding patterns before current requirements justify them.
- Treat DSA interview preparation as a 15-lesson progression with dedicated coverage of sorting, binary search, array and string patterns, linked lists, trees, graphs, backtracking, dynamic programming, greedy proof, and bit manipulation.
- Treat microservices, domain-driven design, event-driven systems, and Kafka as one dedicated 9-lesson architecture track. Compare modular monoliths before extraction, preserve explicit service and data ownership, and teach reliable event delivery, schemas, replay, testing, observability, and operations.
- Use topic-aware trace language throughout lessons; reserve “unit of work” for the database transaction pattern where it is the actual concept.
- Blackboard visuals must teach the real mechanism with readable stages, a concrete experiment, and evidence to inspect; never fill diagram boxes by splitting the lesson title.
- Keep the focus-mode sidebar header minimal and omit the removed curriculum manifesto tagline.
- Allow a mastered lesson to be marked incomplete individually so progress can be corrected without resetting the curriculum; keep it keyboard and screen-reader accessible with native button behavior, a visible focus ring, a status announcement, and Alt+U.
- Keep a separate focus-mode dashboard for a concise summary of overall and per-track progress, with JSON backup and restore for local progress.
- The learner can study approximately 6–8 hours per week. Prioritize the core from demonstrated gaps rather than requiring every deep extension in 24 weeks.

# Review follow-up — 2026-09-11

- Retrieval/agent definition follow-up: corrected exact-search versus exhaustive-scan terminology, ANN versus relevance recall, rewrite/rerank limits, citation support, checkpoint/interrupt replay guarantees and StateGraph versus create_agent schema support. Eight effective-definition regression checks added; the LangGraph starter now specifies replay of code before interrupt within its node. No retrieval service or LangGraph runtime invoked.
- Retrieval evaluation 0597 now runs precision/recall arithmetic, perfect-ANN/zero-relevance and reranking-candidate counterexamples. Explicitly documents empty-denominator, ties, duplicates and chunk/document conventions to settle before real evaluation; no model outputs are graded.

- ML foundations 0577–0580 follow-up: inspected definitions and numerical starters. Clarified 0579's deployment-dependent entity overlap, fitted-preprocessing leakage and metric limitations; added executable training-only mean checks and three definition regressions. This does not train or evaluate a real model.
- LLM internals 0581–0585 follow-up: inspected numerical starters; added a two-band counterexample in 0585 showing zero aggregate confidence gap despite miscalibration in both bands. Numerical checks execute; no model/provider behavior is certified.

- Quality/security glossary follow-up: corrected webhook algorithm scope, freshness versus atomic duplicate-effect protection, presigned URL reuse/early expiry and hash-chain trust assumptions. Four effective-definition regressions preserve these boundaries; external security integrations remain unexecuted.

- Service architecture/Kafka definition follow-up: inspected the 0456–0464 glossary and corrected nine guarantee boundaries (event durability, atomic deduplication, key placement, compaction, follower reads, ISR, transaction isolation, stale consumers and retry ordering). Added nine effective-definition regression checks. Version-pinned Kafka documentation reviewed; no broker ran.
- Starter follow-up 0460–0464: fan-out invokes later subscribers even after a synchronous throw; Kafka command/property/stdin boundaries are explicit; batch metadata and offset order are checked before effects; effect and schema-validator dependencies are explicit arguments. Schema defaults, format settings and old/new compatibility limits are explained. Local checks cover fan-out, invalid/gapped offsets and quarantine failure, not broker behavior or real schema validation.
- Aggregate 0458 now freezes its USD-only Money value, rejects invalid amounts/identity and checks repeated placement. Explicitly distinguishes an in-memory invariant from concurrent persistence and warns that draining events is not durable publication.

- Reliability 0469 now supplies a runnable event-budget exercise with exact threshold comparisons, no-data/invalid-input checks and an unequal-traffic aggregation counterexample. Replaced its unrelated ASVS primary link with the SRE SLO chapter. Counts are synthetic; no monitoring or incident integration is claimed.
- Testing/security 0465–0468 now have topic-specific primary sources, enforced along with 0469 by source-routing checks. Their existing bounded integration assignments remain assignments, not executed systems.

- Replaced generic request diagrams in 0321/0322 with execution-boundary and cancellation-ownership traces. Added regression checks; explicitly distinguish directly called helpers, cooperative cancellation, remote side effects and server-specific disconnect behavior.
- Inspected foundation, service-architecture and quality/security diagram routing. Reused the behavior-test trace for 0465/0466 and gave 0469 a user-outcome/error-budget/incident-learning trace, with regression checks. This is diagram review, not live reliability or security verification.

- Replaced leaderless-replication and service-discovery helper sketches with runnable quorum-set and endpoint-selection exercises, including counterexamples and explicit integration limits.
- Corrected Python sequence, Python testing, FastAPI response and FastAPI testing diagram routing. Reused the existing behavior-test trace; added regression checks for all five affected lessons.
- Response semantics checked against https://fastapi.tiangolo.com/advanced/custom-response/. No dependencies installed or live services invoked.
