// Reviewed reuse keeps a common mechanism but assigns a distinct piece of practice.
export const REUSE_PURPOSE = {
  "0362": "In a disposable PostgreSQL database, focus the index fixture on B-tree order and index-only scans. Compare heap fetches before/after visibility changes; an INCLUDE column alone does not guarantee a heap-free plan. The sample does not expose page splits directly.",
  "0363": "Use the access-method examples only after defining matching column types and operator classes. Compare text search, ranges and physically correlated timestamps; these indexes are not interchangeable ways to speed every predicate.",
  "0364": "Vary the query predicate and selected columns around the partial covering index. Record whether the planner can establish the partial predicate, then measure index size and write cost. CREATE INDEX CONCURRENTLY must run outside a transaction block.",
  "0366": "Read the shared join plan node by node. Compare estimated and actual rows per loop, then multiply by loops for total work. EXPLAIN ANALYZE executes the query; rollback does not undo every external effect, so use disposable data.",
  "0367": "Change selectivity and test data size in the same join fixture. Compare scan/join choices and spill evidence without forcing a preferred plan as the final fix. Explain when a sequential scan is cheaper than an index lookup.",
  "0368": "Use the maintenance fixture to investigate tuple/page lifecycle, not only table size. Create update churn in a disposable table and inspect HOT/visibility evidence with suitable tools. Approximate dead-tuple counters alone do not prove exact bloat.",
  "0369": "Focus on maintenance ownership: hold an old snapshot in a separate test session, run vacuum and inspect what can be reclaimed. Release it and compare. Explain ANALYZE versus VACUUM and why ordinary vacuum does not generally shrink the file to its minimum size.",
  "0377": "Configure the required statistics extension and permissions in a disposable database, then correlate a slow query with waits and locks. Use PostgreSQL-version-appropriate views; cumulative counters need time deltas and reset awareness.",
  "0378": "Reuse observability queries as a baseline, then change one measured bottleneck under representative concurrency. Budget work_mem across concurrent operations rather than treating it as a single server-wide allocation. Keep throughput, latency and memory evidence together.",
  "0398": "Run the HTTP transcript against a local fixture or an endpoint you own. Trace the raw request through an intermediary and distinguish representation metadata from transport behavior. api.example.test and credentials are placeholders, not a deployed service.",
  "0399": "Repeat safe reads and idempotent updates against the same fixture, including a lost response. Compare intended server effect rather than requiring identical status codes. Idempotent does not mean read-only, and cacheability is a separate policy.",
  "0400": "Use the project representation to design stable resource identity, collection relationships and a job-creation operation. Defend a resource-oriented endpoint versus an explicit action from its semantics, not a rule that every URI must look identical.",
  "0401": "Focus on representation negotiation: supply two media types and languages and verify Vary/cache behavior. The header object is a sketch; setting Content-Encoding to br does not compress a JSON body. Verify actual encoded bytes in an HTTP integration.",
  "0423": "Use the fault-plan sketch to state an explicit failure model: process crash, omission and malicious behavior need different assumptions. Supply a controlled test harness before executing it; do not treat a list of injected faults as an implemented resilience test.",
  "0424": "Reuse the fault plan to separate delayed, lost, duplicated and reordered application messages. TCP's ordered byte stream does not give application-level exactly-once effects or a failure detector. Trace a successful write with a lost response and decide how retry learns the result.",
  "0223": "Run the byte-frame example as a Node ES module. Explain copy versus view ownership, reject a truncated or oversized frame, and show why allocUnsafe requires complete initialization before exposure.",
  "0224": "Use the same frame to compare big-endian decoding with a little-endian DataView interpretation. Preserve byteOffset/byteLength when interoperating with an ArrayBuffer; a pooled Buffer's backing allocation can include unrelated bytes.",
  "0228": "Run the compression recipe only in a disposable directory with input.ndjson. Inject source, transform and destination failures separately; verify all pipeline stages close and no incomplete output is published. It creates/replaces files, not a read-only demo.",
  "0229": "Adapt the disposable compression fixture to Web Streams using supported Node adapters. Test cancellation and consumed/locked fetch bodies separately. The original file pipeline demonstrates Node streams only, not a completed Web Streams integration.",
  "0238": "Split the labelled package metadata and JavaScript into actual files in a disposable package. Trace ESM URL resolution and live bindings; record the Node version because import.meta helpers and loader customization vary by release.",
  "0239": "Create a CommonJS consumer for the same package and a small cycle showing partial exports. Compare reassigning exports with assigning module.exports; do not paste JSON and ESM statements into one executable file.",
  "0240": "Pack the package and test both import and require consumers from clean directories. Verify intended state sharing and exported entry points. Conditional export branches can load distinct implementations; a successful ESM import alone is insufficient.",
  "0244": "Run the controlled dependency failure in an isolated child process. Inspect the stable error code and original cause, then decide which failures can be retried under a deadline and idempotency contract. Never classify every Error as transient.",
  "0245": "Use --unhandled-rejections=strict for the isolated fatal-error fixture and verify nonzero exit. Explain why merely setting exitCode can leave open servers accepting requests; fatal-error monitoring must not silently turn into unsafe recovery.",
  "0258": "Treat createService as an integration sketch. Supply validated stream adapters, identity/tenant checks and a repository; inject a source error and prove that the full pipeline propagates cancellation and cleans up each stage.",
  "0259": "Complete the service project with total body limits, bounded persistence, idempotent effects and overload policy. Measure queue depth, loop delay and shutdown under failures; the sketch's fixed concurrency is not a justified production capacity budget.",
  "0087": "Run as an ES module. Trace identifier resolution from the returned closure through block, function and module bindings; shadow one name and predict which binding is read.",
  "0088": "Reuse the closure trace to distinguish a returned function's retained lexical environment from the completed outer call's stack frame. Compare module execution with a separate realm; the sample is not an engine memory-layout diagram.",
  "0089": "Predict the TDZ failure, then replace the local declaration with var in a disposable copy. Explain declaration instantiation separately from assignment; hoisting is not literal source-code movement.",
  "0091": "Run as an ES module. Compare method syntax, a detached function, a bound wrapper and an arrow. Add rest/default parameters and inspect function length without mistaking declared arity for runtime validation.",
  "0092": "Focus on call-site receiver selection, then detach read and predict its strict-mode failure. Compare new.target and bound calls; top-level arrow this is undefined in an ES module, not necessarily in a script or CommonJS wrapper.",
  "0093": "Inspect own keys and descriptors before and after the setter creates _name. Compare enumerable keys, symbols and inherited properties, then predict JSON serialization; a getter can run during reading/serialization.",
  "0094": "Trace the same object's inherited kind lookup and own name accessor. Try shadowing an inherited non-writable data property under strict mode; explain why prototype delegation is not property copying.",
  "0096": "Compare the original and updated object identities along every path. Explain what Object.freeze covers, then add an unfrozen nested object to demonstrate shallow freezing; choose structural sharing from ownership requirements.",
  "0097": "Use the sparse-array portion to compare a hole with present undefined, map with spread, and mutating sort with toSorted. This requires a runtime supporting copying array methods. Engine element kinds are not portable language contracts.",
  "0099": "Implement a small non-generator iterable with next and return, then compare it with range. Verify early break invokes cleanup; do not assume every traversal helper has identical closing behavior.",
  "0100": "Trace the generator's suspended state across next, next('stop'), return and throw on separate instances. Explain why the first next argument has no suspended yield to receive it and why finally may run before normal exhaustion.",
  "0101": "Use the custom symbol to explain collision-resistant property keys. Add a well-known protocol such as Symbol.iterator separately; a symbol's description is not its identity and symbol-keyed properties are not secret.",
  "0102": "Deliberately omit the non-configurable key in ownKeys and assert the resulting TypeError. Then revoke a separate proxy and verify access fails. A proxy cannot violate target invariants simply by returning a preferred answer.",
  "0104": "Split the labelled counter.mjs and feature.mjs sections into actual files. Trace live binding updates, then create a cycle with an early lexical read. This is a multi-file recipe, not one runnable pasted module.",
  "0105": "Reuse the split module fixture but focus on disabled loading, missing chunks and cache behavior. Supply analytics.mjs explicitly. Dynamic import is a runtime boundary; bundle splitting and tree shaking depend on tooling and side-effect analysis.",
  "0107": "Predict operation's synchronous prefix and the await resumption. Replace one await with sequential awaited work and compare it with starting independent promises together; a promise is not itself a thread.",
  "0108": "Use the same trace to separate Promise Jobs from host timers. Compare browser and Node observations without claiming timer timing establishes rendering order or a portable total order across all task sources.",
  "0118": "Run the benchmark as a Node ES module, then inspect optional engine diagnostics on a pinned runtime. Distinguish parsing, interpretation and optimized execution without treating flags or bytecode as standardized APIs.",
  "0119": "Add a shape-changing input variant, verify equal results and compare repeated timings. A changed timing is a hypothesis about optimization, not proof of a particular inline-cache state; retain runtime and workload details.",
  "0120": "Focus on benchmark methodology: verify the result, report all sample sizes, separate warmup and measured runs, and vary data size. Add a responsiveness measurement separately; this throughput loop does not measure event-loop delay.",
  "0122": "Run as an ES module with Node's built-in tests. Extend the table with empty and already-normalized strings, then break normalization deliberately and verify a test fails. Pre-aborted fetch exercises cancellation without issuing a request.",
  "0123": "Use the same failing test as a debugging fixture: locate the first wrong value with a conditional breakpoint, make a minimal fix and retain the regression test. Heap and CPU investigations require different evidence than a functional assertion.",
  "0124": "Review the serial integration core and define the repository's transaction contract. Test it with adapters that reproduce same-key replay, changed-intent conflict and failure; the sample does not provide a production repository.",
  "0125": "Implement the repository and relay in the capstone, then demonstrate crash recovery, replay, authorization and bounded admission. Defend the concurrency limit using measured pool capacity; do not count the integration sketch as a deployed system.",
  "0023": "First encounter: trace lowerBound on empty, duplicate-heavy and absent-target arrays. State the half-open interval invariant and explain why returning length is valid. The sample does not implement every search variant in the title.",
  "0038": "Delayed transfer from 0023: derive upperBound without copying the condition, then turn a capacity-feasibility predicate into answer search. Explain what monotonicity must hold and provide a counterexample when it does not.",
  "0025": "First encounter: draw each pointer update during reversal and explain why the original next pointer must be saved. Compare array queues, deques and linked ownership; reversal is the worked subset, not an implementation of every structure.",
  "0036": "Pointer-surgery drill: reverse an empty list, one node and three nodes, then reverse twice and check identity and order. Add cycle detection separately; ordinary reversal assumes an acyclic list and does not establish that precondition.",
  "0027": "First encounter: trace the unweighted BFS distances and explain discovery-time marking. Compare the role of an adjacency graph with a disjoint set; the BFS example does not implement weighted shortest paths or minimum spanning trees.",
  "0042": "Transfer drill: give an edge weight counterexample to BFS as a minimum-cost algorithm, then choose Dijkstra or Bellman-Ford under explicit weight assumptions. Reconstruct a path from parent links and explain unreachable vertices.",
  "0028": "First encounter: trace choose, recurse and undo for duplicate values. Explain why outputs copy the working path and how duplicate skipping avoids repeated permutations.",
  "0043": "Constraint-search drill: add a prefix constraint to the permutation search and count visited states before/after pruning. Justify that pruning cannot remove a valid solution; a faster wrong search does not satisfy the exercise.",
  "0029": "First encounter: derive minimumCoins from the final coin choice, including zero amount and unreachable states. Trace the table for denominations 1, 3, 4 and amount 6, where greedy fails.",
  "0041": "Reconstruction drill: store a chosen predecessor and return actual coins, not only the count. Verify their sum and length; explain why memory compression can discard information needed to reconstruct an answer.",
  "0030": "First encounter: manually choose the earliest-finishing compatible intervals and give the exchange argument. State whether intervals are half-open and whether touching endpoints are compatible.",
  "0040": "Counterexample drill: assign interval weights so maximum count differs from maximum value. Explain why the original greedy proof no longer applies and derive a weighted scheduling recurrence before implementation.",
  "0031": "First encounter: trace add, test, remove and clear-lowest-set-bit operations. Explain JavaScript's 32-bit coercion and test bit 31; do not interpret the signed printed number as loss of the stored bit.",
  "0049": "Representation drill: demonstrate that Number shifts wrap the shift count modulo 32, then implement a BigInt mask for bit 40. Test an empty mask and explain why Number and BigInt operands cannot be mixed in bitwise operations."
};

// Authored interview cases. Numbers are exercise assumptions, not provider limits.
// These are worked examples and review prompts, not automated hiring assessments.
export const SENIOR_CASES = {
  "engineering-foundations": {
    title: "A release works locally and fails in CI",
    scenario: "A release is due today. A teammate proposes deleting the lockfile because installation fails only in CI. How do you investigate and decide whether to ship?",
    reasoning: "First capture the failing command, exit status, runtime version, dependency-manager version, platform, and lockfile revision. Reproduce with a clean environment using that same lockfile. Separate dependency resolution from lifecycle scripts, permissions, and missing configuration. Deleting the lockfile changes the experiment and may introduce unrelated upgrades. Keep the last known-good artifact available; ship only when the failure has a bounded explanation and the same artifact passes the required checks.",
    followup: "The build passes only when a cache is restored. What would you inspect, and what evidence makes the build reproducible?",
    signals: "Identify the missing or stale cached input; distinguish source, dependency cache, and release artifact; reproduce from declared inputs; record the decision and owner.",
    gap: "All five foundations now have distinct experiments or worked artifacts. Shell, Git and debugging checks execute locally; dependency installation remains a disposable-environment assignment and the ADR is a reasoned example.",
    source: ["Git dependency and history investigation", "https://git-scm.com/docs/git-bisect"]
  },
  "software-design": {
    title: "Two similar rules begin to diverge",
    scenario: "Two products share a pricing helper. One introduces subscription discounts while the other adds regional tax rules. A proposal adds six flags to the shared helper. What would you change?",
    reasoning: "List the business rules and which owner changes each rule. Similar arithmetic does not imply shared policy. Preserve shared money validation and rounding only where their contracts actually agree, then separate the two product policies behind their existing entry points. Add characterization examples before moving behavior. Compare a small duplication with the combinations and coordination cost of six flags. Review the resulting dependency direction and changed call sites, not just the line count.",
    followup: "Finance now requires both products to change one rounding rule on the same day. Which decision changes?",
    signals: "Centralize that shared policy with explicit currency and rounding semantics; keep unrelated discounts separate; test both products; explain migration and ownership.",
    gap: "Strong pragmatic design coverage. Senior practice must evaluate a real change request, behavior preservation, and coordination cost rather than pattern recall.",
    source: ["Google: what to look for in code review", "https://google.github.io/eng-practices/review/reviewer/looking-for.html"]
  },
  "computer-science": {
    title: "Top K under changing input constraints",
    scenario: "Return the largest K values from N inputs. Duplicates count, K is much smaller than N, and the input may become a stream. Compare sorting, a heap, and selection.",
    reasoning: "Clarify output order and validate K. Sorting costs O(N log N) time. A min-heap retaining at most K values gives O(N log K) time and O(K) auxiliary space; its root is the smallest retained candidate. Quickselect gives expected O(N) selection on a stored mutable array, but ordinary pivot strategies have O(N²) worst cases and do not directly solve an unbounded stream. If sorted output is required, sort the retained K items. Test duplicates, negative values, K=0, K>N, and the chosen tie policy.",
    followup: "Inputs are events that expire after ten minutes. Is the same heap sufficient?",
    signals: "Explain expiration and removal of stale entries, bounded memory, timestamps, and a deletion-capable structure or indexed counts. The original heap invariant alone does not handle expiry.",
    gap: "Coverage includes the core patterns and worked DSA modules. Shared examples between overview and drill lessons are useful only if the second lesson changes constraints and requires a fresh proof.",
    source: ["MIT 6.006 algorithms course", "https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/"]
  },
  "web-platform": {
    title: "The network is fast but the page feels slow",
    scenario: "An API responds in 80 ms, but typing on the results page stalls for 700 ms. A teammate proposes a CDN. What evidence would you collect?",
    reasoning: "Measure the interaction from input to the next paint. Use a browser performance trace to separate network waiting, JavaScript, style calculation, layout, and paint. Look for long tasks, synchronous layout reads after writes, and excessive DOM work. A CDN can reduce delivery latency but cannot fix a busy main thread after the data arrives. Change the measured bottleneck, then compare the same interaction and dataset on a representative device. Preserve keyboard interaction and focus while optimizing.",
    followup: "A worker makes computation faster, but sending it a large object freezes the UI. What changed?",
    signals: "Account for serialization and transfer costs, chunking, ownership of transferable buffers, cancellation, and measurement of the complete interaction.",
    gap: "The original five browser lessons share a fetch timer. DOM, CSS, security, and scheduling need distinct browser experiments; server response time alone is not interaction performance.",
    source: ["Web performance: optimize long tasks", "https://web.dev/articles/optimize-long-tasks"]
  },
  "systems-foundations": {
    title: "Connections time out while CPU stays low",
    scenario: "A service is slow under load. CPU is 25%, but connection establishment times out. Would adding threads solve it?",
    reasoning: "Follow one failed connection across DNS, routing, transport, TLS, accept queues, and application admission. Compare connection count, file descriptor limits, retransmissions, socket states, queue lengths, and connection-pool waits. Low CPU does not distinguish network loss, exhausted handles, blocking I/O, and downstream saturation. Add capacity only at the measured limiting resource. Bound admission while investigating so waiting work does not consume all memory or sockets.",
    followup: "A restart helps for twenty minutes and then failures return. Which hypotheses become stronger?",
    signals: "Investigate resource retention and leaks with before/after counts; distinguish a connection leak from traffic growth; identify who closes sockets on failure and cancellation.",
    gap: "Concrete terminology now supports the network-to-kernel examples. Practice resource ownership, queueing and observable diagnosis; OS-specific experiments still require their stated host environment.",
    source: ["Linux socket interface", "https://man7.org/linux/man-pages/man7/socket.7.html"]
  },
  "lld-machine-coding": {
    title: "Two customers reserve the last space",
    scenario: "Implement a parking allocation operation. Two callers see the last space as free. How do you prevent both from receiving it?",
    reasoning: "State the invariant: at most one active reservation owns a space. Keep availability checking and claiming in one atomic boundary. A lock can demonstrate this within one process; multiple processes need a shared transactional or conditional-write boundary. Make retries identify the same reservation request. Return a domain outcome for no capacity, and test two competing callers. Keep pricing policy separate from the allocation invariant so a pricing change does not alter concurrency control.",
    followup: "The claim succeeds, but the client times out before receiving the ticket. What does retry return?",
    signals: "Persist the operation identity and result with the claim; replay the ticket for the same request; reject the same key with different intent; distinguish local locks from distributed correctness.",
    gap: "The case-study sequence is relevant. Senior readiness requires a runnable acceptance scenario, a changed requirement, and explicit concurrency and persistence limits.",
    source: ["PostgreSQL constraints", "https://www.postgresql.org/docs/current/ddl-constraints.html"]
  },
  javascript: {
    title: "A copy changes the original object",
    scenario: "A service copies a request with {...request}. Changing copy.profile.name also changes request.profile.name. Explain why and choose a fix.",
    reasoning: "Object spread creates a new outer object and copies enumerable own property values. The profile value still identifies the same nested object, so both paths observe its mutation. Assignment and arguments copy values; JavaScript does not pass the caller's variable by reference. If only the name must change, copy the outer object and the profile object along that path. Deep cloning has different cost and supported-value semantics; it is not an automatic replacement for deciding who owns mutation.",
    followup: "The object contains a Date, a function, a cycle, and a transferred ArrayBuffer. Can JSON or structuredClone preserve every required behavior?",
    signals: "Name the required semantics: JSON changes or rejects some values and cycles; structuredClone rejects functions and can detach transferred buffers; explicit domain copying may be the correct contract.",
    gap: "The original generic definitions are now replaced throughout the 46-lesson JavaScript track, and the values/async traces are corrected. Remaining senior work: validate each lab and distinguish language guarantees, host behavior, and engine observations.",
    source: ["ECMAScript data types and values", "https://tc39.es/ecma262/multipage/ecmascript-data-types-and-values.html"]
  },
  typescript: {
    title: "The compiler accepts an invalid request",
    scenario: "An endpoint casts request JSON to CreateOrder. The project uses strict TypeScript, but production receives a negative quantity and an invalid currency. Where should the fix live?",
    reasoning: "A type assertion changes the checker's view; it does not validate bytes at runtime. Treat external data as unknown, parse its structure and bounded values, and then create the domain representation. Keep business checks such as account permission and inventory separate from shape validation. Test malformed input at the endpoint and valid domain operations independently. Strict types help preserve facts after validation but do not prove that the parser or authorization policy is correct.",
    followup: "The caller is another service generated from the same schema. Can validation be removed?",
    signals: "Consider independent rollout, version skew, bugs, compromised callers, and resource bounds; measure parsing cost before changing the trust boundary.",
    gap: "Strong compiler and contract breadth. Distinguish type tests from runtime evidence, and make library evolution and migration part of senior answers.",
    source: ["TypeScript narrowing", "https://www.typescriptlang.org/docs/handbook/2/narrowing.html"]
  },
  react: {
    title: "An old search response replaces a new one",
    scenario: "A user searches for A, then B. B finishes first; A finishes later and overwrites B. Would wrapping the update in a transition fix correctness?",
    reasoning: "The problem is response ownership, not render priority. Associate each request with the current query or generation and commit only results that still belong to it. Cancel obsolete fetches to reduce work, while retaining a stale-result guard when cancellation races with completion or is unsupported. A transition can help responsiveness but does not make network responses arrive in order. Test the reversed completion order and loading, empty, and failure states. Keep the current input usable and announce changes appropriately.",
    followup: "The cache is shared across routes and two components request the same query. Who owns cancellation now?",
    signals: "Separate subscriber lifetime from request lifetime; use query identity, shared ownership, invalidation policy, and a test that unmounting one subscriber does not break another.",
    gap: "Unrelated advanced lessons now have distinct integration exercises for races, accessibility, server boundaries and profiling. These require a React project and browser evidence; they are not complete supplied applications.",
    source: ["React: fetching data with Effects", "https://react.dev/reference/react/useEffect#fetching-data-with-effects"]
  },
  nodejs: {
    title: "Promise.all overwhelms the database",
    scenario: "An endpoint starts 5,000 database operations with Promise.all. Latency and memory rise while the database pool has 20 connections. What do you change?",
    reasoning: "Promise.all observes promises; it does not limit work already started. Bound admission before starting database calls and choose concurrency from measured pool capacity and other traffic. Propagate deadlines and stop scheduling after cancellation. Separate pool wait from execution time, and monitor event-loop delay to rule out CPU work as a second bottleneck. More concurrent work can increase queues without increasing throughput. Reserve capacity or fairness for unrelated requests rather than letting one bulk request occupy the pool.",
    followup: "The database is now fast, but parsing each response takes 200 ms of CPU. Does increasing the pool help?",
    signals: "Profile CPU and event-loop delay; reduce computation or use a bounded worker pool; include serialization overhead and admission limits in the comparison.",
    gap: "Good runtime depth. Senior evidence should connect scheduling, streams, memory, process lifecycle, and pool budgets under real failure and load.",
    source: ["Node.js: do not block the event loop", "https://nodejs.org/en/learn/asynchronous-work/dont-block-the-event-loop"]
  },
  python: {
    title: "An async service becomes serial",
    scenario: "An async endpoint calls a blocking library for two seconds. Concurrent requests stall. Explain the scheduling behavior and choose an execution boundary.",
    reasoning: "An async function runs on the event-loop thread until it yields. Calling a blocking synchronous function there prevents other tasks from progressing. Prefer a compatible async client for I/O, or move bounded blocking I/O to a thread with explicit timeouts. For CPU work, compare processes or a suitable native implementation and account for serialization. The GIL's behavior depends on the implementation and build; it is not a reason to ignore blocking or assume shared mutations are safe. Resource ownership must survive cancellation.",
    followup: "Cancelling the awaiting task does not stop the thread's external operation. What does the API promise?",
    signals: "Distinguish stopping the wait from stopping the work; use library deadlines, idempotency for effects, bounded outstanding work, and an observable completion or reconciliation path.",
    gap: "Specific examples cover most topics. Senior checks must separate Python language contracts from CPython implementation details and from thread or process cancellation guarantees.",
    source: ["Python asyncio tasks and threads", "https://docs.python.org/3/library/asyncio-task.html#running-in-threads"]
  },
  fastapi: {
    title: "A background task uses a closed session",
    scenario: "A request schedules work using its database session and returns. The task later fails because the session is closed. Would extending the request dependency lifetime solve durable execution?",
    reasoning: "The request and the job have different lifetimes. Pass a stable identifier or serializable payload, and let the worker acquire and close its own session. If acceptance must survive a process crash, persist a job or outbox record before acknowledging it. In-process background tasks alone cannot provide that durability. Yield cleanup timing depends on dependency scope and FastAPI version; verify the installed behavior, especially for streaming. Keep transactions bounded and do not share a mutable session across concurrent tasks.",
    followup: "The response is lost after the job record commits. How do repeated submissions avoid duplicate effects?",
    signals: "Use a stable request identity, atomic deduplication, replayable response, worker idempotency, and failure tests around the commit/acknowledgement boundary.",
    gap: "Strong specific request and dependency examples. The senior bar is lifecycle ownership, version-aware cleanup, tenant isolation, and durability beyond the web process.",
    source: ["FastAPI: dependencies with yield", "https://fastapi.tiangolo.com/tutorial/dependencies/dependencies-with-yield/"]
  },
  "data-systems": {
    title: "A check-then-write oversells inventory",
    scenario: "Two transactions read stock=1 and both accept an order. Each transaction commits successfully. Why did using transactions not prevent overselling?",
    reasoning: "Atomicity alone does not make every concurrent history safe. Encode the invariant in an atomic conditional update, for example reducing stock only where stock>0 and checking whether a row was returned. Commit the reservation and related durable state together. Depending on the rule, row locking or serializable isolation may be appropriate; understand their contention and retry behavior. A cached read cannot authorize the sale. Reproduce the interleaving with two connections and test retry behavior after a deadlock or serialization failure.",
    followup: "A cache still reports stock=1 after the database commits stock=0. Must checkout wait for every cache replica?",
    signals: "Keep the authoritative write check in the database; state acceptable display staleness; invalidate or refresh the cache; distinguish availability and read freshness from the inventory invariant.",
    gap: "Excellent topic breadth. Definitions, SQL session setup, concrete query results, and crash schedules need more work than adding more database feature names.",
    source: ["PostgreSQL transaction isolation", "https://www.postgresql.org/docs/current/transaction-iso.html"]
  },
  "api-distributed-systems": {
    title: "A timed-out payment has an unknown outcome",
    scenario: "A payment request times out after two seconds. The client retries. Where can duplicate charges occur, and what contract do you expose?",
    reasoning: "A timeout means the caller stopped waiting; the server may have committed. Scope an idempotency key to the caller and operation, associate it with a request fingerprint, and durably store the result or in-progress state. Concurrent attempts must claim the operation atomically. Carry the operation identity into an external provider that supports it, or reconcile an unknown outcome instead of blindly repeating the charge. Specify key lifetime and behavior for a changed payload. Retries need bounded attempts, backoff, and a shared deadline.",
    followup: "The provider lacks idempotency support and its status endpoint is temporarily unavailable. Can you promise exactly one charge?",
    signals: "Name the uncertainty; avoid a false exactly-once claim; expose pending status, reconciliation, and an escalation path; explain the availability cost of refusing a blind retry.",
    gap: "Strong failure-model coverage. Senior answers need explicit guarantees and counterexamples, capacity arithmetic, and recovery sequences rather than a list of distributed patterns.",
    source: ["AWS Builders' Library: safe retries", "https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/"]
  },
  "service-architecture-events": {
    title: "An event is published twice after a crash",
    scenario: "An outbox publisher sends an event and crashes before marking it sent. The event is delivered again. Is the outbox broken?",
    reasoning: "The outbox couples the business change and publication intent in one database transaction. It does not atomically couple that database to every broker acknowledgement. A retry can publish again. Give the event a stable ID; consumers can store that ID and their local state change in the same transaction. External effects still need their own idempotency or reconciliation boundary. Order by the business key where needed, define schema evolution rules, and monitor lag and poison events.",
    followup: "A replay from six months ago invokes today's billing handler. What must be separated?",
    signals: "Separate rebuilding a projection from issuing live external effects; retain event meaning and versions; design replay controls and compatibility tests; do not promise global ordering across partitions.",
    gap: "Useful monolith-versus-service and Kafka scope. Make transaction boundaries, replay safety, independent deployment, and team ownership visible in each design exercise.",
    source: ["Apache Kafka design and delivery semantics", "https://kafka.apache.org/41/design/design/"]
  },
  "quality-security": {
    title: "A valid user reads another tenant's document",
    scenario: "A document endpoint validates a JWT and queries by document ID. A logged-in user can change the ID to read another tenant's data. Why did authentication not protect the object?",
    reasoning: "Authentication establishes a principal; authorization checks whether that principal may perform this action on this object. Scope the lookup and policy to the tenant and resource, deny by default, and enforce the rule on every access path including downloads, jobs, search, and admin tools. Add negative tests using two tenants and an authenticated but unauthorized user. Avoid leaking object contents or secrets into denial logs. Review cache keys and signed links as alternate paths to the same data.",
    followup: "Support staff need temporary access for debugging. How do you make that capability reviewable?",
    signals: "Use explicit privileged permission, limited scope and lifetime, attributable audit evidence, revocation, and tests. A hidden bypass or a UI-only check is insufficient.",
    gap: "Production workflow topics are strong. Earlier security/testing lessons share a sample; emphasize concrete abuse cases and observable negative tests.",
    source: ["OWASP authorization guidance", "https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html"]
  },
  "cloud-aws": {
    title: "Multi-AZ compute still has a single failure path",
    scenario: "An API runs across three Availability Zones, but an outage in one zone stops outbound calls from every instance. What would you inspect before adding a second Region?",
    reasoning: "Trace the full dependency path: routing, egress, load balancing, DNS, identity, database, and external providers. Three compute placements do not prove three independent paths. Check whether routes share a zonal egress dependency, and compare a per-zone path, private endpoints where applicable, and the operational cost. Define the failure objective and test failover, capacity, and data recovery. A second Region introduces replication, failover, and operating costs; it must solve a stated recovery requirement.",
    followup: "The business accepts one hour of recovery and fifteen minutes of data loss. How does that change the design?",
    signals: "Translate those assumptions into RTO and RPO, compare restore versus warm standby, test restore duration and backup freshness, and include egress and idle-capacity costs.",
    gap: "Generic VPC-listing starters have been replaced by service-specific design/failure exercises. Lambda batch processing has local assertions; cloud deployment, restore and cost evidence require an approved sandbox.",
    source: ["AWS reliability pillar", "https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/welcome.html"]
  },
  devops: {
    title: "A canary passes health checks but corrupts writes",
    scenario: "A release returns HTTP 200 and stays healthy, but writes incorrect totals for one customer segment. How should rollout checks change?",
    reasoning: "Health checks establish limited process or dependency readiness, not business correctness. Compare candidate and control using an invariant or business-level error signal for the affected segment, alongside latency and errors. Define the stop threshold and responsible responder before release. Use an immutable artifact and test old/new schema compatibility. Rolling back code does not undo corrupted data; preserve evidence, stop harmful writes, and plan repair with reconciliation and auditability.",
    followup: "The defect occurs once per million requests. What can a five-minute canary prove?",
    signals: "State sample-size limits; add targeted regression and synthetic cases; consider exposure duration and blast radius; avoid interpreting no observed failures as proof of absence.",
    gap: "Delivery breadth is good. Replace command checklists with release evidence, rollback limits, incident decisions, and measurable feedback.",
    source: ["Google SRE: canarying releases", "https://sre.google/workbook/canarying-releases/"]
  },
  docker: {
    title: "The container ignores graceful shutdown",
    scenario: "A worker runs behind a shell entrypoint. On deployment it is killed after the stop timeout and its jobs are retried. What would you inspect?",
    reasoning: "Inspect the process tree, PID 1, entrypoint form, signal delivery, and shutdown logs. Use an exec-form entrypoint or an entrypoint script that execs the application so signals reach the intended process. The application must stop accepting work, finish or release bounded in-flight work, and close resources. An init process can handle orphan reaping where needed; it does not implement the application's drain policy. Set the stop timeout from that bounded policy and verify duplicate-safe job handling.",
    followup: "A job takes longer than any acceptable deployment timeout. Should deployments wait forever?",
    signals: "Use durable progress or retryable work with idempotent effects; stop admission and release or renew leases deliberately; distinguish process termination from job completion.",
    gap: "Distinct build, lifecycle, storage, signal and diagnosis experiments now replace generic inspection starters. Corrected working directory and Compose wiring; execution requires Docker and the documented fixtures.",
    source: ["Docker container runtime options", "https://docs.docker.com/engine/containers/run/"]
  },
  kubernetes: {
    title: "A database outage causes a restart storm",
    scenario: "The liveness probe queries the database. When the database slows down, all application Pods restart and recovery becomes harder. How do you redesign the probes?",
    reasoning: "Liveness should detect a condition a container restart can fix. A shared database outage usually does not satisfy that test. Separate startup, readiness, and liveness responsibilities. Readiness can remove a Pod from serving traffic when it cannot meet the endpoint contract, but making every Pod unready has system-wide consequences; consider degraded behavior and overload policy. Use bounded checks and observe restart counts, probe latency, and database load. A readiness failure does not itself restart the container.",
    followup: "Removing all Pods from service makes the gateway retry aggressively. Where should overload control live?",
    signals: "Coordinate bounded retries, deadlines, admission and degradation at gateway and application boundaries; measure amplification; do not rely on probes as a complete resilience policy.",
    gap: "Broad orchestration coverage. Focus senior practice on controller responsibility, rollout and probe failures, resource budgets, and actual recovery rather than YAML vocabulary.",
    source: ["Kubernetes Pod lifecycle and probes", "https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/"]
  },
  "ml-foundations": {
    title: "A great offline metric fails after launch",
    scenario: "A fraud classifier scores very well offline and poorly on next month's transactions. The team normalized the full dataset before splitting it. What do you investigate?",
    reasoning: "The preprocessing learned from data that should have been held out, so the evaluation may contain leakage. Rebuild preprocessing on training data only and apply the fitted transform to validation and test data. Check time ordering, repeated customers, label availability, class imbalance, and distribution shift. Compare with a simple baseline using metrics that reflect false-positive and false-negative costs. Tune on validation data and reserve the test set for a final estimate; report important slices as well as the average.",
    followup: "Only one in a thousand transactions is fraudulent. Why is 99.9% accuracy not enough?",
    signals: "Compare the always-negative baseline; report precision, recall, threshold costs, and representative sample counts; explain delayed labels and monitoring.",
    gap: "Four assertion-tested numerical experiments now cover geometry, gradients, leakage/metrics and scale-sensitive baselines. They are introductory mechanisms, not evidence of training or evaluating a production model.",
    source: ["scikit-learn: common pitfalls", "https://scikit-learn.org/stable/common_pitfalls.html"]
  },
  "llm-internals": {
    title: "A larger context window reduces quality",
    scenario: "The model accepts a much longer prompt, so the team includes every retrieved document. Cost rises and answers become less reliable. Why can more context hurt?",
    reasoning: "A context limit describes accepted input size, not guaranteed use of every relevant fact. Extra material can introduce conflicting evidence and distractors, while input processing and cache memory still have costs. Keep document selection, ordering, and output quality measurable. Compare controlled prompts with relevant evidence at different positions and with distractors. Separate tokenization, attention, decoding, and product-level factuality; a plausible continuation is not a guarantee that the answer is supported.",
    followup: "Reducing temperature makes the same wrong answer repeat. What did that change establish?",
    signals: "Distinguish sampling variability from factual correctness; use evidence and evaluation rather than equating deterministic-looking output with truth.",
    gap: "Five distinct, assertion-tested numerical traces cover tokenization, causal attention, next-token loss, temperature and calibration. Each explicitly states what is omitted from a real model implementation.",
    source: ["Lost in the Middle: long-context evaluation", "https://arxiv.org/abs/2307.03172"]
  },
  "ai-application-engineering": {
    title: "Valid JSON requests an invalid action",
    scenario: "The model emits schema-valid JSON asking to refund an order. The user is not authorized to refund it. What must the application do?",
    reasoning: "A schema checks output structure, not permission or business correctness. Resolve the authenticated user server-side, validate the requested operation and resource, enforce policy, and seek confirmation where the product requires it. Bind approval to the specific action and arguments so later model output cannot substitute a different action. Bound tool execution, track idempotency, and return a truthful result. Streaming UI should distinguish a proposed action from an executed one.",
    followup: "The browser disconnects after execution but before receiving the tool result. What appears when it reconnects?",
    signals: "Read canonical server state using stable action/message IDs; reconcile pending UI with durable results; do not blindly execute again or trust browser approval state.",
    gap: "Good dedicated streaming and persistence lessons. Model contracts need worked examples of semantic validation, tool authorization, partial output, and fallback behavior.",
    source: ["OWASP LLM application risks", "https://genai.owasp.org/llm-top-10/"]
  },
  "retrieval-rag": {
    title: "Relevant retrieval leaks another tenant's data",
    scenario: "A RAG system improves recall by retrieving globally and asking the model to hide documents from other tenants. Why is that design unsafe?",
    reasoning: "The model is not an authorization boundary. Derive tenant and document permissions from authenticated server state and enforce them before unauthorized content reaches the model or client. Scope caches and citations to the same policy. Evaluate retrieval separately from answer generation: did the allowed evidence appear among candidates, and did the answer use it correctly? For approximate search, measure recall against an exact baseline under realistic filters. A better global recall score cannot justify a permission violation.",
    followup: "A user's access is revoked while documents remain in an index and response cache. What must invalidate or recheck?",
    signals: "Trace authorization freshness through index metadata, retrieval, cache keys or invalidation, generated answers, and source download links; test revocation end to end.",
    gap: "Strong ingestion, vector index, and storage topics. Senior readiness needs separate retrieval/generation metrics, permission tests, freshness, and measured recall/latency tradeoffs.",
    source: ["pgvector filtering and index tradeoffs", "https://github.com/pgvector/pgvector"]
  },
  agents: {
    title: "A checkpoint replays a successful tool call",
    scenario: "A workflow executes an external action, then crashes before saving its next checkpoint. On resume it requests the same action. How do you prevent duplicate effects?",
    reasoning: "A checkpoint preserves workflow state but does not automatically make an external effect atomic with that state. Give each logical action a stable identity that survives replay and use an idempotent execution boundary or reconcile its outcome. Do not reuse one workflow ID for every distinct action. Separate proposed actions, approvals, execution, and durable results. Bound steps, elapsed time, tokens, and external spending. Test crashes before and after the effect and checkpoint.",
    followup: "A human approval was recorded yesterday, but the tool arguments have changed. Can replay use that approval?",
    signals: "Bind approval to action identity, arguments and relevant policy context; re-evaluate changed or expired authorization; keep an attributable audit trail.",
    gap: "Specific framework and protocol lessons add depth. Strengthen the independent execution model: replay, approval binding, effect identity, stopping, and recovery.",
    source: ["LangGraph durable execution", "https://docs.langchain.com/oss/python/langgraph/durable-execution"]
  },
  "ai-quality-safety": {
    title: "The average improves while a critical slice regresses",
    scenario: "A candidate model improves average task success from 85% to 88%, but fails more permission-boundary cases. Should it ship?",
    reasoning: "First check dataset versions, sample sizes, repeats, uncertainty, and whether both systems saw comparable cases. Average improvement can hide a critical regression. Define release criteria per failure severity and important slice before comparing candidates. Separate deterministic policy checks, human judgments, and model-graded scores; calibrate graders against human labels. Investigate the permission failures and retain a baseline. A release decision must include latency, cost, recovery, and production monitoring, not only one quality average.",
    followup: "The model grader prefers longer answers even when they contain unsupported claims. How do you detect and reduce that bias?",
    signals: "Use human-reviewed examples, explicit rubrics, order controls, disagreement analysis, and deterministic support or policy checks; keep grader and candidate versions recorded.",
    gap: "Benchmarking coverage is useful. Senior practice must defend the dataset, uncertainty, grader calibration, critical slices, and the release decision.",
    source: ["LangSmith evaluation concepts", "https://docs.langchain.com/langsmith/evaluation-concepts"]
  },
  "portfolio-capstone": {
    title: "Defend one project under a changed requirement",
    scenario: "Present a project you actually built. The interviewer halves the budget and adds a data-isolation requirement. Which parts of your architecture change?",
    reasoning: "Start with the user outcome, workload, constraints, and your personal contribution. Trace one critical request and identify the invariant affected by isolation. Compare the simplest acceptable alternatives with measured or explicitly estimated cost. Separate existing evidence from proposed work. Show a test, trace, benchmark, or incident that changed a decision. Explain an incremental migration, its rollback limits, the owner, and how you would verify success. If the new constraints cannot all be satisfied, make the conflict explicit.",
    followup: "Which feature would you remove first, and what result would make you restore it?",
    signals: "Connect removal to user value and cost; preserve correctness and security; define a measurable trigger; do not invent production scale or ownership.",
    gap: "Four capstone outlines need learner-produced artifacts. Completion requires a defensible implementation and measurements, not reading a checklist or claiming deployment experience.",
    source: ["Amazon senior engineer interview preparation", "https://www.amazon.jobs/content/en/how-we-hire/sde-iii-interview-prep"]
  },
  "international-interviews": {
    title: "Explain an unpopular technical decision",
    scenario: "Describe a real time you disagreed with a proposed architecture and influenced the result without formal authority. What did you personally do?",
    reasoning: "Use a true situation with a specific business constraint. Explain the competing options fairly, the evidence you gathered, who had decision authority, and how you made disagreement productive. Separate your actions from the team's work. State the outcome with verifiable measures or honest qualitative evidence, including what failed and what changed in your judgment. If you did not get your preferred outcome, explain how you supported the decision and managed remaining risk. Years of experience alone do not establish this evidence.",
    followup: "A teammate says your intervention slowed delivery. What might they be right about?",
    signals: "Acknowledge the real cost; explain proportionality, alternatives and feedback; show mentoring, ownership, and learning without blaming or inventing metrics.",
    gap: "Strong breadth of hiring preparation. Senior stories must show scope, influence, mentoring, ambiguity, business impact, and reflection; company processes still vary.",
    source: ["Amazon senior engineer interview preparation", "https://www.amazon.jobs/content/en/how-we-hire/sde-iii-interview-prep"]
  }
};

// Exact, track-specific corrections take precedence over broad catalog matches.
export const TERM_CORRECTIONS = {
  "cloud-aws": {
    "cdk": "AWS CDK expresses infrastructure in supported programming languages and synthesizes CloudFormation templates. Generated resources still need review for permissions, replacement and deployment behavior.",
    "stacks": "A CloudFormation stack groups resources managed through one template and lifecycle. Cross-stack references and shared data can constrain independent updates or deletion.",
    "change sets": "CloudFormation change sets preview supported resource changes before execution. They can expose replacements but do not guarantee successful deployment or safe application data migration.",
    "custom resources": "CloudFormation custom resources delegate lifecycle actions to a supplied provider. That provider must report completion and handle retries, updates, deletion and stable physical identity correctly.",
    "infrastructure testing": "Infrastructure testing checks template shape, policy intent, planned changes and actual deployed behavior at appropriate boundaries. Static validation alone cannot verify live routing or restore capability.",
    "aws reliability": "AWS workload reliability depends on complete dependency paths, capacity, fault isolation and tested recovery. Managed-service availability does not automatically establish the application's user-facing SLO.",
    "quotas": "AWS quotas limit resources or request rates at defined service, account and regional scopes. Some are adjustable; approval and actual capacity can constrain recovery and scaling.",
    "multi-az design": "Multi-AZ design distributes required components across zonal fault domains and preserves usable capacity after a zone fails. Shared zonal egress, data or identity dependencies can defeat redundant compute.",
    "rto": "Recovery time objective states the acceptable time to restore the specified user capability after disruption. Measure detection, decision, restoration and verification against an explicit start/end definition.",
    "rpo": "Recovery point objective states the acceptable data-loss window. Backup or replication freshness must be measured against the authoritative business records actually recoverable.",
    "aws cost models": "AWS cost models combine service-specific usage dimensions, regional rates, commitments and shared costs. Model uncertainty and cost per successful business outcome rather than request count alone.",
    "tags": "AWS tags attach key/value metadata to supported resources for organization, policy or cost allocation. Coverage and activation rules matter; a tag alone is not an isolation boundary.",
    "cur": "AWS Cost and Usage Reports provide detailed billing/usage records for allocation and analysis. Select the supported export/report format and account for reporting delay and adjustments.",
    "cost explorer": "Cost Explorer analyzes supported historical and forecasted costs using billing dimensions. Forecasts and delayed billing evidence are not real-time hard spending limits.",
    "budgets": "AWS Budgets monitors configured cost or usage thresholds and can trigger configured alerts or actions. Reporting and action delays mean it does not guarantee a strict spending ceiling.",
    "rightsizing": "Rightsizing matches provisioned resources to measured workload needs with headroom for failure and variation. Reducing nominal capacity is useful only while service objectives remain satisfied.",
    "savings plans": "Savings Plans exchange a defined spending commitment for eligible discounted usage. Utilization, coverage and flexibility determine value; unused commitments still cost money.",
    "spot": "Spot capacity uses spare provider capacity under interruption and availability constraints. Design checkpointing, diversification and fallback around the chosen service contract.",
    "finops": "FinOps coordinates engineering, finance and product ownership of cloud economics. Allocation, forecasting and unit-cost decisions connect spending to business value and service constraints.",
    "sagemaker": "SageMaker provides managed capabilities for building, training and serving machine-learning workloads. Select the specific offering and own data, model quality, access and operating cost.",
    "model access": "Model access requires eligible account/Region availability, permission and any model-specific terms or prerequisites. A valid SDK call shape does not establish access to every model.",
    "inference": "Inference executes a trained model on input to produce output. Model version, input context, decoding and service limits shape quality, latency and cost.",
    "agents": "Model-driven agents select actions through a control loop with tools and state. Restrict authority and bound steps, time and cost while verifying effects outside the model.",
    "knowledge bases": "Managed knowledge bases connect ingestion and retrieval to generation through configured sources and indexes. Freshness, permission filtering and answer grounding still require application evaluation.",
    "guardrails": "AI guardrails apply configured checks or restrictions to supported inputs and outputs. They can miss threats and do not replace tool authorization, data isolation or release evaluation.",
    "evaluation": "AI evaluation measures defined tasks against datasets, rubrics and baselines. Report slices, failures and uncertainty along with latency and cost before accepting a release.",
    "ai security": "AI security controls untrusted context, data exposure and tool authority across model workflows. Treat generated actions as untrusted proposals until validated and authorized.",
    "cloud": "Cloud services expose metered compute, storage and platform capabilities through APIs. A workload design assigns ownership, identity, data placement and recovery across those services.",
    "aws production architecture capstone": "The AWS capstone integrates a user journey with identity, network, compute, data, messaging, model access and operations. Defend each dependency and recovery claim using explicit evidence.",
    "full-stack ai delivery": "Full-stack AI delivery joins browser, application, data and model changes in a reproducible release process with evaluation and operating ownership.",
    "cost": "Cloud cost combines metered usage, retained capacity, commitments and operational overhead. Include failed attempts and shared infrastructure when calculating cost per successful task.",
    "incident defense": "An incident defense explains how the architecture detects, contains and recovers from a concrete failure. Tie decisions to user impact, evidence and an accountable recovery owner.",
    "sns": "Amazon SNS publishes notifications to independently configured subscriptions. Endpoint type, topic type, retries and subscription filtering determine delivery semantics.",
    "standard queues": "SQS standard queues provide at-least-once delivery and best-effort ordering under the service contract. Repeated or out-of-order deliveries require application-level handling.",
    "fifo": "SQS FIFO queues order work within message groups and provide bounded producer deduplication. Visibility expiry can still repeat processing; external business effects need their own duplicate protection.",
    "visibility timeouts": "A visibility timeout temporarily hides a received SQS message from normal delivery. It does not stop the worker when it expires; successful processing must delete the correct delivery receipt.",
    "dlqs": "Dead-letter queues retain messages redirected after configured delivery failures. Retention, alarms, ownership and authorized redrive are necessary to recover useful work.",
    "long polling": "SQS long polling waits for available messages up to a configured duration instead of repeatedly returning empty immediately. Client timeouts must permit the wait, and processing still needs bounded concurrency.",
    "fan-out": "Fan-out sends one publication to multiple independent consumers or subscriptions. Each branch has its own delivery, failure, authorization and cost implications.",
    "rules": "EventBridge rules match event patterns and select configured targets. Pattern matching does not validate every business schema field or guarantee successful target processing.",
    "archives": "EventBridge archives retain selected events for later replay under configured retention. They do not rewind external state or automatically make replayed side effects safe.",
    "replay": "Replay redelivers retained historical input through a selected processing path. Preserve stable identity and define changed-handler, authorization and external-effect behavior before repeating it.",
    "scheduler": "EventBridge Scheduler invokes supported targets on one-time or recurring schedules with configured execution role, retry and failure handling. Scheduled time is distinct from successful business completion.",
    "step functions": "AWS Step Functions coordinates state-machine executions through configured tasks, transitions and error handling. Standard and Express workflow types have different durability and execution semantics; side-effect identity remains important.",
    "compensation": "Compensation is a new business action intended to counteract an earlier effect. It can fail or be incomplete, so durable state and repair ownership must preserve the unresolved outcome.",
    "kinesis data streams": "Kinesis Data Streams retains records in shards for consumers to read and replay within retention. Partition keys, sequence positions and consumer capacity define ordering and recovery behavior.",
    "firehose": "Amazon Data Firehose buffers and delivers streaming data to supported destinations, with configured transformation and retry behavior. It is not the same interface as a consumer-managed replayable stream.",
    "msk": "Amazon MSK manages Apache Kafka infrastructure under selected deployment options. Kafka partitioning, client configuration, consumer progress and application-effect guarantees remain relevant.",
    "shards": "A Kinesis shard is a unit of ordered record storage and throughput. Resharding changes lineage and ownership, so consumers must handle parent/child progress correctly.",
    "consumers": "Streaming consumers read records and maintain processing progress. Their checkpoint and effect boundary determine replay behavior after failure.",
    "checkpoints": "A stream checkpoint records a recoverable processing position. Advancing before a durable effect risks loss; advancing afterward permits replay and requires duplicate handling.",
    "streaming backpressure": "Streaming backpressure bounds ingestion or processing when downstream capacity is insufficient. Retention provides a finite recovery window, not unlimited tolerance for sustained overload.",
    "alarms": "CloudWatch alarms evaluate configured metric periods and missing-data rules to enter alarm states and invoke actions. An alarm definition does not create its underlying custom metric or response procedure.",
    "dashboards": "Dashboards present selected telemetry in a shared operational view. Useful panels expose user outcomes, workload and uncertainty; visual availability does not ensure someone responds.",
    "logs insights": "CloudWatch Logs Insights queries selected log data and time ranges. Field structure, retention, scan volume and access controls determine its diagnostic value and cost.",
    "x-ray": "AWS X-Ray collects distributed trace evidence for supported instrumentation and services. Sampling and propagation gaps limit completeness; traces are not a durable business audit ledger.",
    "opentelemetry": "OpenTelemetry defines vendor-neutral telemetry APIs, SDKs and collection protocols. Instrumentation, propagation, sampling and exporter configuration determine what evidence reaches a backend.",
    "correlation": "Correlation links observations across requests, spans, jobs and service events using safe identifiers. Shared identifiers help investigation but do not prove causality or complete delivery by themselves.",
    "cloudtrail": "AWS CloudTrail records supported account and API activity under configured event collection. Management-event history and selected data-event trails have different coverage and retention.",
    "aws config": "AWS Config records supported resource configurations and evaluates configured rules. It explains observed state and compliance findings rather than replacing a full API audit history.",
    "guardduty": "Amazon GuardDuty analyzes supported data sources for threat findings. Coverage, enabled protections and investigation determine what a finding means; detection does not itself prove compromise or complete containment.",
    "security hub": "AWS Security Hub brings together supported security findings and posture information. Configured integrations, coverage and response ownership determine whether aggregation improves remediation.",
    "detective": "Amazon Detective provides related security activity views for investigation under its supported service coverage. Treat correlation as investigative evidence and verify current account/Region availability before designing around it.",
    "audit trails": "Audit trails preserve attributable activity records with configured coverage, integrity, access and retention. Missing collection cannot be repaired by assuming every request was logged.",
    "detection": "Detection identifies activity that matches a rule or threat hypothesis. Validate false positives, missing coverage and incident context before choosing a response.",
    "response": "Security response investigates, contains and recovers from a suspected incident using authorized actions. Preserve evidence, assign an owner and verify the effect of mitigation.",
    "ecr": "Amazon ECR stores container images and related artifacts in repositories. Authentication, image digests, lifecycle policies and scanning determine delivery trust and retention.",
    "ecs task definitions": "An ECS task definition versions the containers, resources, networking and role configuration used to start tasks. Registering it does not itself run a service.",
    "services": "An ECS service reconciles a configured number or scheduling strategy of tasks and manages supported deployment behavior. Health checks, capacity and network dependencies determine whether tasks serve traffic.",
    "capacity providers": "ECS capacity providers connect task scheduling to a capacity source and strategy. Placement weights and scaling do not guarantee instant capacity or application readiness.",
    "fargate": "AWS Fargate runs supported container tasks without customer-managed host instances. Workload sizing, images, identity, networking and task lifecycle remain application responsibilities.",
    "ec2 launch type": "ECS on EC2 schedules tasks onto registered instances whose capacity, operating system and lifecycle the customer manages. Task placement must fit available host resources and constraints.",
    "deployments": "A deployment moves a service to a selected artifact and configuration revision. Readiness, rollout limits, retained state and rollback compatibility determine safe progress.",
    "handlers": "A Lambda handler is the runtime entry point for an invocation. Its input/return contract and effect handling depend on the invoking service or event-source mapping.",
    "cold starts": "A cold start includes initialization needed for a new execution environment before useful handler work. Measure it separately from warm execution and distinguish reserved capacity limits from provisioned initialized capacity.",
    "event sources": "Lambda event sources invoke functions directly or through poll-based mappings. Retry, batching, acknowledgement and failure destinations depend on that integration.",
    "layers": "Lambda layers package shared dependencies or runtime content for supported deployment types. Version, architecture and runtime compatibility matter; layers do not remove dependency patching responsibilities.",
    "failure semantics": "Lambda failure semantics specify how a particular invocation path treats exceptions, timeouts, throttling and partial batches. Synchronous callers, asynchronous delivery and queue mappings do not share one retry contract.",
    "api gateway http apis": "API Gateway HTTP APIs expose HTTP routes with supported integrations and authorization features. Their feature set and pricing model differ from the product named REST APIs; choose from requirements.",
    "rest apis": "API Gateway REST APIs are an AWS API product with resources, methods and supported management features. The product name does not prove that an application's design satisfies REST constraints.",
    "websocket apis": "API Gateway WebSocket APIs manage persistent client connections and message routes. Connection authorization, per-message policy, reconnects and application delivery history need explicit design.",
    "integrations": "An API Gateway integration maps a selected route or method to a backend operation and response behavior. Timeout and transformation rules affect what the caller sees and whether backend work continues.",
    "authorizers": "API authorizers evaluate supported identity or permission evidence before integration. Cached decisions must be scoped correctly, and backend object authorization may still be required.",
    "throttling": "Throttling limits admitted API traffic under configured service and account rules. It is not a precise spending cap or a substitute for backend concurrency and queue bounds.",
    "stages": "An API Gateway stage identifies a named deployment environment/configuration for an API. Deployment and auto-deployment behavior vary by API type; test the exact exposed stage.",
    "aurora": "Amazon Aurora separates database compute from managed replicated cluster storage under engine-specific contracts. Writer/reader endpoints, failover and recovery semantics differ from a conventional standalone database.",
    "multi-az": "Multi-AZ database deployments place supported database components across zones for resilience. Traditional RDS standby instances and readable Multi-AZ cluster replicas have different roles; identify the deployment type.",
    "read replicas": "Read replicas serve eligible read traffic with an explicit replication and freshness contract. They may lag and do not automatically preserve every session's read-your-writes behavior.",
    "backups": "Backups retain recoverable database state independently of the current serving copy. Retention, encryption-key access, restore duration and validation determine practical recovery.",
    "parameter groups": "Database parameter groups collect engine configuration applied at instance or cluster scope. Changes may apply dynamically or require restart, so review engine/version and rollout effects.",
    "iam auth": "IAM database authentication uses supported temporary authentication tokens to establish database connections. Database users, privileges, TLS and token generation remain configured separately from IAM permission to connect.",
    "rds proxy": "RDS Proxy manages database connection pools and supported authentication/failover behavior. Session pinning and transaction state affect multiplexing; it cannot resolve every unknown commit outcome.",
    "sort keys": "A DynamoDB sort key orders items sharing one partition-key value and supports range-oriented access patterns. It participates in item identity for a composite primary key.",
    "indexes": "DynamoDB secondary indexes expose alternate key access paths with projection, capacity and consistency implications. Global and local secondary indexes have different creation and read-consistency rules.",
    "adaptive behavior": "DynamoDB adaptive capacity redistributes supported capacity toward uneven demand within service limits. A permanently hot key and query fan-out still require an appropriate data model.",
    "ttl": "Time to live expresses expiry, but service semantics differ: DynamoDB asynchronously deletes eligible items, while Redis expiry governs key visibility under its clock and access rules. Enforce business expiry explicitly where delayed cleanup is unacceptable.",
    "elasticache redis": "ElastiCache manages supported Redis OSS or Valkey-compatible deployments and their infrastructure. Engine/version, topology, memory policy and replication determine application behavior and recovery limits.",
    "cache-aside": "Cache-aside reads the cache first, loads an authoritative source on a miss and stores an eligible result. Invalidation races, expiry and fallback load need explicit ownership.",
    "eviction": "Cache eviction removes entries under configured memory pressure policy. It can occur before intended reuse and must not be treated as deletion of authoritative business data.",
    "clustering": "Redis cluster-mode sharding distributes keys among hash slots and owners. Cross-key operations need compatible slot placement, and clients must handle topology changes.",
    "stampede control": "Stampede control limits simultaneous refreshes or source reads when popular cached values expire. Coalescing, jitter, stale-serving policy and bounded admission protect the authority under cache failure.",
    "records": "DNS records associate a name and type with data such as addresses or delegation. Resolver caching, TTL and authoritative zone ownership affect when changes become visible.",
    "aliases": "Route 53 alias records point to supported AWS targets or same-zone records and can serve the zone apex. Their target and health-evaluation rules differ from ordinary CNAME records.",
    "routing policies": "Route 53 routing policies select DNS answers using rules such as weight, latency, geography or failover. Cached answers and existing connections limit how directly those choices control actual requests.",
    "dnssec": "DNSSEC signs DNS data so validating resolvers can verify its origin and integrity through a trust chain. It does not encrypt queries or prove that the application at an address is safe.",
    "origins": "A CloudFront origin supplies content on an eligible cache miss. Origin access control, forwarded request data and origin-response policy must preserve the intended authorization and representation.",
    "cache keys": "A cache key identifies requests allowed to reuse one stored response. Include every relevant representation dimension or disable caching; forwarding a header to the origin does not necessarily put it in the cache key.",
    "behaviors": "CloudFront cache behaviors match path patterns and select origins, cache policies and other handling rules. Ordered matching and the default behavior can expose unintended paths if configured incorrectly.",
    "signed access": "CloudFront signed URLs or cookies grant constrained viewer access using a trusted signer. They must be paired with origin restrictions when direct origin access would bypass the grant.",
    "waf": "AWS WAF evaluates supported web requests against configured rules such as IP, rate and content checks. It reduces selected application threats but does not replace resource authorization or safe code.",
    "edge security": "Edge security applies viewer TLS, access rules, request inspection and origin protection near entry points. Preserve identity and cache isolation across the edge-to-origin boundary.",
    "elastic load balancing": "Elastic Load Balancing distributes traffic to registered targets using a selected balancer type, listeners, health rules and availability configuration. Application readiness and capacity remain explicit design inputs.",
    "alb": "An Application Load Balancer routes supported HTTP-family requests using application information such as host and path. It owns client-facing protocol handling and separate target connections.",
    "nlb": "A Network Load Balancer routes supported transport flows to targets. Protocol, TLS termination, client-address preservation and connection lifetime determine its behavior; it does not provide ALB-style HTTP path rules.",
    "listeners": "A load-balancer listener accepts a configured protocol and port and applies forwarding or other supported actions. TLS listeners also need certificates and protocol policy.",
    "target groups": "Target groups define eligible backends, protocol/port and health behavior for forwarding. Registration, deregistration delay and health state affect new and in-flight traffic differently.",
    "connection behavior": "Balancer connection behavior includes reuse, idle timeouts, source identity, draining and reset semantics. One long-lived connection can carry many requests, so connection counts need not measure request load.",
    "ec2 instance families": "EC2 instance families provide different CPU, memory, storage, network and accelerator profiles. Choose from measured workload constraints and available capacity rather than vCPU count alone.",
    "amis": "An Amazon Machine Image supplies instance launch image data and metadata. Track its identity, architecture, ownership and patch lineage; an AMI name is not an immutable trust guarantee.",
    "user data": "EC2 user data supplies launch-time bootstrap input processed by the configured image. Execution and retry behavior depend on the bootstrap agent; it is unsuitable for exposing long-lived secrets.",
    "imdsv2": "IMDSv2 requires a session token for instance metadata requests. Its token flow and hop-limit configuration reduce some metadata exposure paths, while workload SSRF and instance-role permissions still need controls.",
    "placement": "EC2 placement selects fault domains and optional placement strategies for instances. Network proximity, hardware separation and capacity availability create workload-specific tradeoffs.",
    "auto scaling": "Auto Scaling adjusts desired compute capacity according to configured signals and bounds. Warm-up, quotas, launch failures and safe scale-in determine whether capacity arrives in time.",
    "ebs": "Amazon EBS provides persistent block volumes with type-specific performance and attachment rules, usually tied to one Availability Zone. Volume lifecycle and snapshots determine recovery after instance loss.",
    "instance store": "Instance store is host-attached temporary storage whose data can disappear on supported instance lifecycle events or host failure. Use it for reconstructible data and distinguish stop/termination from reboot.",
    "efs": "Amazon EFS provides managed shared NFS file storage. Regional and One Zone choices have different fault-domain and recovery implications; access, throughput and application semantics still matter.",
    "fsx": "Amazon FSx provides managed file systems with distinct engines and protocols. Windows File Server, Lustre, ONTAP and OpenZFS have different access and performance models; choose the relevant engine explicitly.",
    "encryption": "Encryption protects data using keys at a specified storage or transport boundary. Key access, application plaintext exposure and recoverable key material are separate from ciphertext storage.",
    "attachment": "Storage attachment connects a volume or file system to a workload under service-specific location, protocol and access rules. Multiple attachments do not make an ordinary filesystem safe for concurrent writers.",
    "durability": "Durability concerns retaining acknowledged data through a stated failure model. It differs from immediate availability and does not substitute for independent recovery from authorized deletion or corruption.",
    "s3 buckets": "An S3 bucket holds objects under bucket-specific naming, location, access and lifecycle rules. Bucket type matters; the versioned-object exercise uses general-purpose bucket semantics.",
    "objects": "An S3 object contains bytes plus metadata addressed by a bucket and key, with a version ID when supported and enabled. Key prefixes organize names; they are not filesystem directories.",
    "versioning": "S3 versioning retains distinct object versions and uses delete markers for ordinary deletion. Deleting an explicit version removes that version; lifecycle policies can still expire retained history.",
    "replication": "Replication copies data to other eligible locations under a configured service contract. Lag, conflict, deletion and failover semantics vary; a replica is not automatically an independent backup.",
    "data protection": "Data protection combines access restrictions, encryption, version retention, recoverable backups and verified restore procedures. Each control addresses a different path to loss or exposure.",
    "envelope encryption": "Envelope encryption encrypts data locally with a data key and encrypts that key with a separately protected key. Store the encrypted data key with ciphertext; tightly control exposure and lifetime of its plaintext form.",
    "key policies": "A KMS key policy governs access to that key and can enable delegation through IAM policies. A broad IAM allow alone may be insufficient without the required key policy and cross-account permissions.",
    "grants": "KMS grants delegate specified operations on a key, often to an AWS service acting for a workload. Constraints, retirement and revocation determine the scope and lifetime of delegated access.",
    "encryption context": "KMS encryption context is nonsecret authenticated metadata bound to supported cryptographic operations. Decryption requires matching context; context can also constrain policy and may appear in audit records.",
    "rotation": "Rotation changes active key material or a secret through the service's supported process. KMS key rotation retains old material for decryption; rotating a database password additionally requires coordinating the database and its clients.",
    "secrets manager": "AWS Secrets Manager stores versioned secrets, controls retrieval and supports configured rotation workflows. Applications still own caching, refresh, safe use and avoiding secret disclosure.",
    "vpcs": "A VPC is a logically isolated regional virtual network with address ranges, subnets and routing. It spans Availability Zones; reachable paths still require routes, addresses and security controls.",
    "cidr planning": "CIDR planning allocates nonoverlapping address ranges with room for growth and connected networks. Prefix length determines range size; AWS reserves addresses and service interfaces consume capacity.",
    "route tables": "VPC route tables map destination prefixes to targets for associated subnets or gateways. Longest matching prefix generally selects the route; reachability also depends on the target, return path and filtering.",
    "local routes": "Local VPC routes provide connectivity for associated VPC address ranges. They do not bypass security groups or network ACLs; appliance-routing designs can alter supported local-route behavior.",
    "ipv4": "IPv4 uses 32-bit addresses. In a VPC, private addressing and internet-reachable public addressing have different routing/NAT requirements; a public subnet does not give every workload a public address.",
    "ipv6": "IPv6 uses 128-bit addresses and has distinct routing and egress controls. A workload's address family, assigned address scope and route determine reachability; do not assume an IPv4 NAT design covers IPv6.",
    "ip address management": "IP address management allocates, tracks and monitors network address space across accounts and Regions. It helps prevent overlap and exhaustion but does not itself configure every workload route.",
    "internet gateways": "An internet gateway connects a VPC to the internet when addressing and routing permit. IPv4 public address mapping and IPv6 routing differ; attaching the gateway alone does not expose every resource.",
    "nat gateways": "A NAT gateway translates supported outbound connections and their return traffic. Public and private NAT modes serve different paths; route placement, address family, availability and metered traffic affect the design.",
    "egress-only gateways": "An egress-only internet gateway permits outbound IPv6 connections and return traffic while preventing unsolicited inbound internet connections. It is not IPv4 NAT or an application authorization control.",
    "vpc endpoints": "VPC endpoints provide supported private service paths. Gateway endpoints use routes, while interface endpoints use private interfaces and service DNS; endpoint policy complements service authorization.",
    "privatelink": "AWS PrivateLink exposes supported services through private endpoint connections without general network peering. Consumers reach the selected service; they do not gain arbitrary transitive access to its VPC.",
    "peering": "VPC peering routes permitted traffic directly between two VPCs with compatible address space. It is nontransitive: peering A to B and B to C does not create A-to-C connectivity.",
    "transit gateway": "A Transit Gateway connects networks through a routing hub with attachments and route tables. Association and propagation determine segmentation; connectivity and return routes must be designed explicitly.",
    "network firewall": "AWS Network Firewall applies configured network inspection rules to traffic routed through its endpoints. Correct routing and stateful path symmetry matter; creating a firewall does not automatically inspect every VPC packet.",
    "dns resolver": "The VPC DNS resolver resolves names under VPC settings and private hosted-zone associations. Resolver endpoints and rules support hybrid DNS; name resolution and IP reachability are separate checks.",
    "vpc flow logs": "VPC Flow Logs record supported IP traffic metadata and accept/reject observations. They are not packet payload captures, do not cover every traffic type, and require configured delivery and retention.",
    "packet diagnosis": "Packet diagnosis follows DNS, address family, forward and return routes, filters, transport handshake and application response. Use evidence from the failing layer before changing rules.",
    "cloud mental models": "Cloud design treats compute, storage and networking as metered services controlled through APIs. Separate configuration from workload execution, and name who owns identity, data, capacity and recovery at each boundary.",
    "elasticity": "Elasticity adjusts provisioned capacity as demand changes. Scaling has delay, quotas and cost; it does not remove bottlenecks or repair application correctness.",
    "managed services": "Managed services delegate specified infrastructure operations to the provider. Customers still choose configuration, access, data lifecycle and workload behavior; the exact responsibility split depends on the service.",
    "aws global infrastructure": "AWS infrastructure separates geographic Regions, zonal fault domains, edge points and independent partitions. A service's placement and dependencies determine which failures a design can tolerate.",
    "edge locations": "Edge locations place supported caching, delivery and network services near users. They are not interchangeable with Availability Zones for arbitrary regional workloads.",
    "partitions": "AWS partitions are groups of Regions with independent IAM boundaries, such as aws, aws-cn and aws-us-gov. Credentials and cross-Region service features do not automatically cross partitions; this is distinct from a network communication failure.",
    "service scope": "Service scope identifies whether a resource or operation belongs to an account, Region, Availability Zone or wider service boundary. Global naming does not imply absence of regional dependencies.",
    "organizations": "AWS Organizations groups accounts for centralized billing and policy management. Its organizational hierarchy applies governance without merging account identities or resources.",
    "organizational units": "Organizational units group accounts and nested OUs so common organization policies can be applied through a hierarchy. They are governance groupings, not network segments.",
    "scps": "Service control policies constrain permissions available to member-account principals; they do not grant access. They do not govern management-account principals or service-linked roles, so those require separate controls.",
    "control tower": "AWS Control Tower sets up and governs a multi-account landing zone using account provisioning and controls. Its configured coverage and drift still need ownership and review.",
    "landing zones": "A landing zone is the foundational multi-account environment for workloads: identity, account structure, logging, security controls and networking conventions. It is a starting operating model, not a finished application architecture.",
    "aws cli": "The AWS CLI maps shell commands to AWS service APIs. Effective credentials, Region, endpoint, pagination and output handling determine what the command actually does.",
    "sdks": "AWS SDKs provide language APIs for service requests, credential discovery, signing and retries. Defaults and provider precedence are version- and SDK-specific; they do not make business side effects automatically retry-safe.",
    "profiles": "AWS profiles name configuration and credential sources for a tool invocation. A profile can refer to a role or federated session rather than store a long-lived secret.",
    "credential provider chain": "A credential provider chain searches configured credential sources according to the specific SDK or CLI rules. Verify the effective caller and source rather than assuming a profile name proves the intended identity.",
    "sigv4": "Signature Version 4 authenticates AWS requests by signing a canonical request with credential-derived keys and scope. Request bytes, service, Region and time matter; HTTPS still protects transport confidentiality.",
    "endpoints": "AWS endpoints are service API addresses selected for service, Region, partition and endpoint options. A private endpoint changes network reachability, not the permissions required by the service.",
    "iam identities": "IAM users and roles represent principals and permission-bearing identities; assumed-role sessions use temporary credentials. Groups organize IAM users but are not request principals.",
    "policies": "AWS permission policies contain effect, action, resource and optional condition statements. Whether a policy grants access or only limits it depends on its type and attachment.",
    "resources": "An AWS resource is a service-owned object addressed by an ARN or service identifier. Actions differ in supported resource scoping, and object-level permission is not the same as permission on its container.",
    "conditions": "IAM conditions compare request-context values with policy criteria. Key availability, operators and missing-key behavior matter; a condition only affects the statement that contains it.",
    "evaluation logic": "AWS evaluates the applicable policy types for the principal, action, resource and request context. Explicit deny wins; grants, permission limits and same-account or cross-account rules must be evaluated for the exact principal type rather than one universal union formula.",
    "boundaries": "An IAM permissions boundary limits permissions an identity-based policy can grant to a user or role; it does not grant permissions itself. Resource-policy and session-principal rules require separate evaluation.",
    "explicit deny": "An applicable explicit Deny statement overrides an Allow. This differs from implicit denial because no sufficient permission was granted.",
    "role assumption": "Role assumption exchanges an authorized identity for a temporary role session. The role trust policy controls who can assume it; permission policies constrain what the session can do.",
    "federation": "Federation lets an external identity system authenticate users or workloads that obtain scoped AWS sessions. Trust configuration must bind the intended issuer, audience and identity claims.",
    "iam identity center": "IAM Identity Center centrally manages workforce access to AWS accounts and supported applications using assignments and permission sets. It is not a substitute for workload role design.",
    "oidc": "OpenID Connect supplies signed identity claims from an issuer. AWS workload federation validates configured issuer, audience and subject conditions before granting a role session; an arbitrary repository token is not sufficient authority.",
    "session policies": "Session policies restrict permissions for an individual temporary session; they cannot add permissions beyond the role's permitted identity-based access. Evaluate resource-policy exceptions using the exact principal and policy types.",
    "temporary credentials": "Temporary AWS credentials comprise an access key ID, secret access key and session token with an expiry. Applications must refresh through the supported provider rather than log or persist the session as a permanent key."
  },
  javascript: {
    "primitive values": "A primitive is an immutable value that is not an object: undefined, null, a boolean, number, bigint, string, or symbol. Reassigning a variable replaces its value; it does not mutate the old primitive.",
    "objects": "An object has identity and properties. Two variables can hold the same object value, so a property mutation through one is visible through the other. Two separately created objects have different identities even when their properties match.",
    "null": "null is the primitive value commonly used for an intentional absence. It is not an object, although typeof null returns the string 'object' for historical compatibility.",
    "undefined": "undefined is a primitive value. An initialized variable with no assigned value, a missing property read, or a function with no explicit return value commonly produces it. Reading a lexical binding before initialization instead throws.",
    "numbers": "Number is JavaScript's binary64 numeric type. It includes finite values, infinities, NaN, and signed zero. Integers are exactly representable throughout the safe-integer range; many decimal fractions are rounded.",
    "strings": "A JavaScript string is an immutable sequence of UTF-16 code units. Its length counts code units, which can differ from code points and user-perceived characters.",
    "code units": "A code unit is one unit of a text encoding. In a JavaScript string it is a 16-bit UTF-16 unit; some Unicode code points need two units.",
    "code points": "A code point is a numeric position in Unicode. Iterating a string with for...of combines valid surrogate pairs into code points, but a visible grapheme may still contain several code points.",
    "truthiness": "Conditions convert values to booleans. false, numeric zeros, 0n, the empty string, null, undefined, and NaN are falsy. Ordinary objects are truthy, including empty arrays; the legacy browser document.all object is a special exception.",
    "set": "Set stores unique values in insertion order using SameValueZero equality. Objects are compared by identity; two separate objects with the same fields are two entries.",
    "weakset": "WeakSet holds garbage-collectable values without keeping them alive. It supports membership checks but not enumeration or a reliable size; collection timing must not control correctness.",
    "shallow copies": "A shallow copy creates a new outer container while reusing its nested values. Copying {profile: object} does not create a new profile object.",
    "deep copies": "A deep copy recursively copies supported nested data. Which types, cycles, shared identities, and custom behavior survive depends on the copying mechanism.",
    "freezing": "Object.freeze prevents adding, deleting, and changing ordinary own properties of that object. It is shallow: nested objects can remain mutable, and private state is not frozen by it.",
    "const": "const creates a lexical binding that cannot be reassigned after initialization. It does not make an object held by the binding immutable.",
    "var": "var declares a function-scoped or script-global binding, initialized to undefined before statement execution. It does not create a block-scoped binding.",
    "scope": "Scope determines where an identifier can be resolved. JavaScript follows lexical nesting rather than the caller's local variables.",
    "arguments": "For an ordinary function, arguments is an array-like object describing arguments passed to that call. Arrow functions have no own arguments object and resolve the name lexically.",
    "arity": "Arity usually describes a function's parameter count. JavaScript's function.length counts parameters before the first default and excludes a rest parameter; it is not a runtime argument limit.",
    "await using": "await using declares a resource for asynchronous disposal at scope exit. It awaits its disposal protocol; it does not automatically await an arbitrary resource initializer.",
    "states": "A Promise is pending, fulfilled, or rejected. Fulfilled and rejected are settled states; resolution can also adopt a still-pending promise.",
    "fate": "A Promise is resolved when its eventual outcome is locked in by resolution or rejection. A resolved promise can remain pending while following another promise.",
    "reactions": "Promise reactions are the handlers registered by then, catch, or finally. Their jobs run asynchronously after the promise's relevant settlement.",
    "promise combinators": "Promise.all, allSettled, any, and race combine settlement outcomes in different ways. They do not limit how much work was already started or cancel losing operations automatically.",
    "using": "using declares a resource whose synchronous disposal protocol runs on scope exit. It provides structured cleanup, not nondeterministic garbage-collection cleanup.",
    "symbol.dispose": "Symbol.dispose identifies the synchronous disposal method used by explicit resource management. A resource implements that method to release what it owns."
  },
  typescript: {
    "top or bottom types": "Think of types as sets of possible values. unknown can hold any value: it is a top type. never has no possible values: it is the bottom type. any is an escape hatch with special assignability rules, not a safe replacement for unknown.",
    "transformer": "A compiler transformer rewrites syntax trees for output, such as lowering newer syntax. Transformation is separate from proving type compatibility.",
    "gradual adoption": "Add checks to selected JavaScript or TypeScript modules incrementally, keeping runtime behavior and deployment working at each step.",
    "best common types": "Inference compares candidate types from expressions such as array elements; when no suitable common candidate exists, a union may represent the alternatives.",
    "literal preservation": "Retain a specific value type such as POST instead of widening it to string, so later operations can use that precise information.",
    "type parameters": "Named type variables connect input, output, and member types in a reusable contract. They are not runtime parameters.",
    "defaults": "A generic default supplies a type argument when inference and the call do not supply one. It must satisfy its constraint.",
    "relationships": "A type relationship connects positions: a selected key K determines the result T[K], rather than returning an unrelated broad type.",
    "reusable contracts": "Reusable contracts preserve meaningful relationships across callers without assuming more capabilities than the implementation needs.",
    "lookup relationships": "T[K] relates a key K to its property's type in T. Keeping K generic preserves the selected property's precision.",
    "value-derived types": "Use typeof and keyof to derive static contracts from a runtime declaration, reducing duplicated lists that can drift.",
    "mapping modifiers": "Mapped-type + and - modifiers add or remove readonly and optional markers on the resulting properties.",
    "property transforms": "A mapped type changes a property's name, value type, or modifiers in a static contract; it does not transform runtime data.",
    "homomorphism": "A mapping directly over keyof T can preserve the source's property modifiers and structure. Remapping or explicit modifiers can change that behavior.",
    "recursion": "A recursive type refers back to itself to describe or transform nested structure. Terminate deliberately; deep instantiations can overwhelm the checker.",
    "deferred evaluation": "A conditional type involving unresolved type parameters may remain unevaluated until more specific arguments are substituted.",
    "string unions": "A string union lists permitted literal strings. It restricts checked assignments but does not validate arbitrary external strings.",
    "event names": "Event-name types encode naming rules such as nameChanged and can associate each name with a corresponding payload type.",
    "combinatorial growth": "Combining several literal unions can multiply the number of alternatives. Prefer a simpler contract or generated declarations when checking becomes costly.",
    "parameter properties": "A constructor parameter marked public, protected, private, or readonly also declares and initializes an instance property.",
    "override safety": "override makes inheritance intent explicit, and noImplicitOverride requires it where applicable. Signature checks do not prove behavioral substitutability.",
    "nominal techniques": "Brands and private-member identity can distinguish otherwise structurally compatible types. They reduce accidental mixing without validating external values.",
    "private members": "TypeScript private restricts checked access and affects compatibility; JavaScript # fields provide a separate runtime-enforced privacy mechanism.",
    "opaque ids": "Opaque IDs expose identity without inviting callers to depend on representation. Branded strings still need parsing and authorization at boundaries.",
    "units": "Distinct unit types prevent accidental mixing of quantities such as meters and milliseconds. Runtime conversion still needs correct arithmetic.",
    "standard decorator semantics": "Standard decorators receive a decorated value and context and may replace or initialize supported elements. Legacy experimental decorators use different contracts.",
    "contexts": "A decorator context describes the decorated element and offers supported hooks such as initialization. It is not automatically a dependency container.",
    "metadata boundaries": "Metadata availability depends on the decorator system and runtime conventions. Type erasure does not automatically expose all static types as metadata.",
    "dependency injection": "Supply required capabilities from outside a component instead of constructing hidden dependencies inside it. A function argument often suffices.",
    "runtime objects": "Runtime objects survive emission and can be inspected or called; type aliases and interfaces do not create corresponding objects.",
    "reverse mappings": "Numeric enum objects map numbers back to member names as well as names to numbers. String enums do not emit that reverse mapping.",
    "alternatives": "Compare literal unions, lookup objects, and enums by emitted behavior, wire stability, discoverability, and compilation constraints, not syntax preference alone.",
    "ecmascript modules": "ES modules use import and export with host-defined loading rules. TypeScript's output and resolution settings must match that host.",
    "emit correctness": "Emitted JavaScript must retain required runtime imports and use syntax and specifiers the deployment host actually supports.",
    "bundler mode": "Bundler resolution models common bundler lookup behavior. It does not guarantee the same source specifier will load directly in Node.",
    "imports": "Imports can supply runtime values, static types, or both. Explicit type-only imports are erased and cannot perform runtime initialization.",
    "traceresolution": "traceResolution reports how the compiler searches for modules and declarations. Compare that trace with the actual runtime's lookup behavior.",
    "global scope": "Declarations in script files can contribute to shared global scope. Module files isolate declarations unless they explicitly augment globals.",
    "typed library authoring": "Publish matching JavaScript and declarations, stable exports, and consumer tests rather than relying on workspace-only source resolution.",
    "api surface": "The API surface is what consumers can depend on through public exports, signatures, behavior, and documented errors.",
    "compatibility": "Compatibility means supported consumers continue to compile and behave correctly. Declaration compatibility and wire/runtime compatibility require separate checks.",
    "javascript interop": "JavaScript and TypeScript can share modules, but declarations and JSDoc promises must match the JavaScript that actually executes.",
    "checkjs": "checkJs reports type errors in included JavaScript files. It is distinct from allowJs, which admits JavaScript into the project.",
    "declaration generation": "Declaration emit describes the public static API in .d.ts files. Consumers still require the matching runtime implementation.",
    "gradual migration": "Migrate dependency leaves first, preserve behavior with runtime tests, and tighten contracts without requiring a flag-day rewrite.",
    "decoders": "A decoder checks an external representation and returns a validated value or explicit errors, rather than trusting a cast.",
    "trusted domain types": "Domain types represent values accepted under stated checks. Trust is scoped to those checks, not automatic permission or permanent immutability.",
    "environment": "Environment configuration enters as missing values or strings; parse ranges and required values before starting the application.",
    "database": "Database results depend on schema, driver conversion, nullability, and query behavior. A generated type cannot repair drift or incorrect raw queries.",
    "queue": "Queue payloads cross version and time boundaries; validate message shape and define retry, duplicate, and compatibility policies.",
    "file": "File contents are external bytes or text. Decode and validate them before treating them as a domain object.",
    "promise": "Promise<T> describes eventual fulfillment with T. It does not encode a rejection type, deadline, cancellation, or concurrency limit.",
    "async iterables": "An async iterable supplies values through asynchronously resolved iterator results. Consumers must still handle errors, cleanup, and bounded work.",
    "generators": "Generator types describe yielded, returned, and received values. Async generators wrap protocol results in asynchronous delivery.",
    "error channels": "Errors can be thrown, rejected, or returned as explicit result variants. Choose a consistent boundary contract and preserve useful causes.",
    "component props": "Props describe a component's input contract. Prefer precise relationships and accessible defaults over a large set of unrelated optional flags.",
    "events": "Event handlers connect event or payload types to callbacks. Static event types do not replace runtime handling or accessibility behavior.",
    "refs": "A ref holds a mutable reference, often to an element or imperative handle. Its nullability and lifetime must match mounting behavior.",
    "reducers": "A reducer computes next state from current state and an action. Discriminated unions help with shapes; transition invariants still need logic and tests.",
    "polymorphism": "A polymorphic API supports several related shapes while preserving relevant constraints. More variants increase typing, accessibility, and testing costs.",
    "esm": "ESM is the import/export module system. Package type, extensions, emitted syntax, and host resolution jointly determine runtime loading.",
    "configuration": "Configuration selects checks, platform assumptions, file discovery, and output behavior. Record effective options rather than trusting inherited defaults.",
    "processes": "Processes own runtime resources, signals, and exit status. Type annotations do not stop admission or drain work during shutdown.",
    "streams": "Streams deliver chunks under backpressure and lifecycle rules. Chunk types do not enforce bounded buffering, cancellation, or resource cleanup.",
    "errors": "An error value needs classification, cause preservation, and a safe external representation. unknown catches require evidence before property access.",
    "service boundaries": "Service boundaries separate independently failing or evolving components. Validate inputs and define authorization, timeouts, and version compatibility explicitly.",
    "declaration tests": "Declaration tests compile consumer examples against the published .d.ts API to check accepted and rejected usage.",
    "runtime tests": "Runtime tests execute behavior, including malformed inputs and failures. They cannot alone prove the checker rejects an invalid program.",
    "contract evidence": "Contract evidence combines accepted programs, rejected programs, runtime assertions, and deployment or consumer checks appropriate to the boundary.",
    "target": "target controls which JavaScript syntax the compiler emits or lowers. It does not install missing platform APIs.",
    "lib": "lib selects declarations for assumed platform APIs. Including DOM or a newer ES library does not make those APIs exist at runtime.",
    "module": "module controls module-related emission and behavior. Pair it with a resolution mode and package setup matching the execution host.",
    "moduleresolution": "moduleResolution models how specifiers resolve to source and declarations. It must reflect the runtime or bundler that loads the output.",
    "include": "include selects initial project files using patterns; imports and referenced declarations can bring additional files into the program.",
    "exclude": "exclude filters include discovery, not every possible imported file. Use explainFiles to understand why a file is still checked.",
    "build ownership": "Each build should clearly own its inputs and outputs, preventing different environments from overwriting or checking the wrong artifacts.",
    "composite projects": "composite enables constraints and declaration outputs needed for referenced projects, with explicit source inclusion and reusable build boundaries.",
    "declaration boundaries": "Referenced consumers can check against declarations instead of implementation source. Public API changes propagate across that boundary.",
    "monorepos": "A monorepo stores related packages together. It still needs explicit dependency direction and packaged-consumer checks to avoid hidden source coupling.",
    "build mode": "tsc -b orchestrates project-reference builds in dependency order and reuses build state where applicable.",
    "symbols": "In compiler tooling, a symbol connects declarations of a named entity. This differs from JavaScript's runtime symbol primitive.",
    "types": "A compiler Type represents semantic information at a location, not merely the syntax of a type annotation.",
    "transforms": "Transforms rewrite syntax trees for output or tooling. Preserving syntax does not automatically preserve semantics or source mappings.",
    "tooling": "Compiler tooling can inspect syntax, symbols, and types. Define the intended coverage before treating a small AST scan as a complete analyzer.",
    "type-checker performance": "Checker performance depends on project size and type relationships. Measure time, memory, and instantiations before simplifying types or splitting projects.",
    "instantiation depth": "Instantiation depth reflects nested generic evaluation. Deep recursion can hit compiler limits even when the intended result is understandable.",
    "project size": "Project size includes discovered source files and declarations. Unintended global types or imports can enlarge checking work.",
    "editor latency": "Editor latency includes language-service work and project loading, not just a command-line build. Measure both experiences separately.",
    "release notes": "Release notes identify new checks, changed inference, module behavior, and deprecations that may affect an upgrade.",
    "deprecations": "Deprecations signal planned removal or replacement. Track affected configuration and APIs rather than silencing warnings indefinitely.",
    "strictness migration": "Tighten checks incrementally with explicit error categories, owners, and runtime regression evidence, avoiding blanket suppressions.",
    "dependency types": "Dependency declarations are versioned promises about library behavior. Mismatched package and type versions can create false confidence.",
    "generated code": "Generated code is implementation requiring review; passing types does not prove security, performance, or domain correctness.",
    "tests": "Use static and runtime tests for different claims. Tie each test to an invariant and challenge its failure case.",
    "review": "Review checks contracts, behavior, boundary safety, complexity, and evidence. A green compiler is one input, not the review verdict.",
    "domain modeling": "Domain modeling encodes meaningful states and decisions, not simply the shape of a framework response or database row.",
    "ports": "Ports describe capabilities the domain requires, such as saving an authorized command. Keep them small and driven by actual boundaries.",
    "adapters": "Adapters translate external protocols and vendor representations into domain contracts, including validation and error translation.",
    "dependency direction": "Dependency direction determines which layer must change when another changes. Keep vendor-specific details from becoming unnecessary domain dependencies.",
    "change cost": "Change cost includes edits, tests, rollout risk, and comprehension. Judge type complexity by mistakes prevented and changes made safer.",
    "full-stack contracts": "Full-stack contracts connect client, server, and persistence representations while acknowledging runtime validation and independent version rollout.",
    "reliability": "Reliability requires bounded work, failure handling, recovery, and measured service behavior; types describe only part of those guarantees.",
    "security": "Security requires authentication, authorization, safe boundaries, and controlled resource use. Structural type compatibility is not a permission check.",
    "evolution": "Evolution changes contracts while supporting deployed clients and stored data. Plan compatible rollout order and rollback, not just updated type aliases.",
    "object types": "An object type describes members a value must supply, including properties and possibly call, construct, or index signatures. It can be anonymous, an interface, or named by an alias. It does not construct the object, validate external data, or usually prohibit all extra fields.",
    "property keys": "Runtime object property keys are strings or symbols; numeric property access converts the number to a string. TypeScript also models numeric keys, and PropertyKey is string | number | symbol. Map keys retain their original value identity instead of using object-property coercion.",
    "call signatures": "A call signature describes using a value like a function. Inside an object type, (value: string): string can coexist with properties such as description. It promises call behavior, not construction with new.",
    "construct signatures": "A construct signature such as new (id: string) => Project describes creating an instance through new. An ordinary arrow function can be callable without being constructible.",
    "callbacks": "A callback is a function supplied for another function to invoke. Type its parameters according to what the invoker supplies. A callback can ignore supplied arguments; marking one optional instead permits the invoker to omit it. A void callback contract does not ensure async work is awaited.",
    "typeof": "In an expression, typeof returns a runtime category such as string or number and can supply narrowing evidence; null and arrays both report object. In a TypeScript type position, typeof instead refers to the static type of an existing value. Neither form validates a complete object schema.",
    "instanceof": "instanceof normally checks whether a constructor's prototype occurs in a value's prototype chain; custom Symbol.hasInstance behavior can override it. It can narrow class instances, but it does not validate a JSON object and can be sensitive to different realms or constructor copies.",
    "equality": "Equality checks can eliminate incompatible possibilities. Checking input === null isolates null; comparing compatible union values can narrow them to their overlap. Prefer explicit null or undefined checks when empty strings and zero are valid inputs.",
    "truthiness": "Conditions coerce values to boolean. Empty string, zero, false, null, undefined, and NaN are falsy, so a truthiness guard can accidentally discard valid data. Truthiness is not the same question as presence or type validity.",
    "predicates": "A signature such as input is Command promises that true identifies Command and false excludes it from the current input type. TypeScript trusts that promise. A check for only some numbers must not claim to identify all numbers: its false branch could still contain numbers.",
    "narrowing safety": "A narrowed type is only as trustworthy as its evidence and the assumptions that remain valid. Incorrect predicates, assertions, external data, or later mutation can break those assumptions. Test both accepted and rejected cases; construct an owned value when shared mutation is a risk.",
    "common members": "Before narrowing a union, access only members guaranteed by every remaining alternative. Card and Bank both provide amount; only Card provides token. Checking the literal kind makes that variant-specific access safe.",
    "impossible intersections": "An intersection demands simultaneous compatibility. In {id: string} & {id: number}, id becomes never because no value is both a string and a number. Conflicting literal discriminants can reduce the whole object intersection to never. An intersection is not a last-write-wins merge.",
    "composition": "Build a contract from smaller contracts: a union describes alternatives, while an intersection adds simultaneous requirements. Payment & {traceId: string} requires tracing on either payment alternative. These type operations neither construct fields nor validate external data.",
    "illegal states": "A modeled contradiction, such as success without required data, can be rejected by the checker. A discriminated union removes such combinations from checked construction, but does not automatically prevent illegal transitions, stale events, assertions, or malformed external input.",
    "state machines": "A state machine defines allowed states and the rules for moving between them. A union models state shapes; a transition function must separately enforce temporal rules, such as accepting a completion only for the active request ID.",
    "structural compatibility": "Compatibility depends on members, not a shared declared name. An object with a string name can satisfy {name: string} while carrying extra fields. This does not remove those fields, and class private or protected members introduce additional compatibility rules.",
    "open object types": "An object type usually describes required structure, not an exact list of runtime keys. Extra fields can survive assignment and serialization. Fresh-literal excess-property checks catch likely mistakes; use explicit projection or runtime validation when the boundary needs an exact field policy.",
    "in": "The in operator checks whether a property exists on an object or its prototype chain. TypeScript can use that runtime check to narrow a union; optional properties may keep a variant in both branches.",
    "null": "null is a distinct absence type under strictNullChecks. A value typed as null does not become an ordinary object merely because JavaScript's typeof null returns 'object'.",
    "undefined": "undefined describes the JavaScript undefined value. With strictNullChecks, ordinary value types must explicitly include it when absence is permitted.",
    "exactoptionalpropertytypes": "exactOptionalPropertyTypes distinguishes an absent optional property from a present property containing undefined, unless undefined is explicitly part of that property's value type.",
    "indexed access": "An indexed read such as counts[key] can encounter a missing key. With noUncheckedIndexedAccess, an uncertain dictionary read includes undefined; check it before treating the result as a number.",
    "safe absence": "Give absence an explicit meaning. In a patch, an omitted name can mean leave unchanged, while a present name requests replacement. Do not use truthiness to detect presence: an empty string and zero can be legitimate supplied values.",
    "partial": "Partial<T> makes every property of T optional. It is a shallow type transformation and does not validate an incoming patch or define how to apply it.",
    "required": "Required<T> removes the optional modifier from properties of T. It changes a static contract, not the runtime object.",
    "pick": "Pick<T, K> constructs a type containing the properties of T named by K. It does not remove other properties from a runtime value.",
    "omit": "Omit<T, K> constructs a type excluding the property names K. It is not a runtime redaction operation.",
    "record": "Record<K, V> describes properties with keys K and values V. It does not create a dictionary or validate that external keys exist.",
    "returntype": "ReturnType<F> extracts a function type's return type. For an overloaded function it uses the last signature; it does not perform overload resolution for supplied arguments.",
    "parameters": "Parameters<F> extracts a tuple of a function type's parameter types. Optional and rest parameters remain represented in the tuple.",
    "typeof in type positions": "Type-position typeof derives a type from a value declaration, such as type Config = typeof defaults. It performs no runtime inspection of external input.",
    "strictfunctiontypes": "strictFunctionTypes checks function parameter compatibility more strictly. Method and constructor declarations retain exceptions, so it is not a blanket soundness guarantee.",
    "public": "A public class member is accessible through the class's public type. Public is the default visibility for ordinary class members.",
    "protected": "A protected member is accessible within its class and subclasses according to TypeScript's checking rules. This is not a runtime security boundary.",
    "private": "TypeScript private restricts access during checking. JavaScript #private fields use runtime private-name semantics; these are different guarantees."
  },
  react: {
    "purity": "A pure render computes UI from its inputs without changing pre-existing objects or performing external effects. React may repeat or abandon render work, so rendering must not send a payment or mutate shared state.",
    "derived data": "Derived data is computed from existing props or state, such as a filtered list. Storing both the source and the result creates an extra synchronization problem.",
    "functional updates": "A functional state update receives the pending state and returns the next state. Several queued updates can therefore build on one another instead of reusing one render's captured value.",
    "referential equality": "Referential equality compares whether two object values have the same identity. Creating a fresh object during every render can defeat identity-based memoization even if its fields are unchanged.",
    "identity": "React associates state with a component's type and position in the render tree; keys refine identity among siblings. Changing identity can reset local state.",
    "cleanup": "An Effect cleanup stops the synchronization created by its setup, such as a subscription or connection. It runs before relevant re-setup and on unmount; development checks may exercise an extra setup/cleanup cycle.",
    "dependencies": "Effect dependencies are reactive values read by the Effect. React compares dependency values with Object.is to decide whether synchronization needs to restart; omitting a dependency does not make a captured value current.",
    "authorization": "Authorization decides whether the authenticated user may perform a server operation on a particular resource. A Server Function must enforce this itself; hiding a client button is insufficient.",
    "dangerouslysetinnerhtml": "dangerouslySetInnerHTML inserts supplied HTML into a DOM element. Use it only across an explicit trusted or sanitized HTML boundary because normal text escaping does not protect this path."
  },
  "data-systems": {
    "read committed": "In PostgreSQL Read Committed, an ordinary SELECT sees a snapshot taken when that statement starts. A later statement in the same transaction can see newly committed changes.",
    "repeatable read": "PostgreSQL Repeatable Read uses a transaction snapshot for ordinary queries. It prevents changing snapshots but can still allow serialization anomalies such as write skew.",
    "serializable": "Serializable transactions must have an outcome consistent with some serial execution. PostgreSQL can abort a transaction to preserve that guarantee, so callers must be able to retry the whole transaction.",
    "exec": "Redis EXEC runs the commands queued by MULTI. Other clients do not interleave commands during execution, but a runtime error in one command does not roll back successful commands.",
    "analyze": "ANALYZE collects planner statistics. In EXPLAIN ANALYZE, ANALYZE means actually execute the statement and report measured execution; use caution with writes and side effects.",
    "rpo": "Recovery point objective is the maximum acceptable loss of recent data, expressed as time. Backup success alone does not prove the target is met.",
    "rto": "Recovery time objective is the target time to restore acceptable service after an incident. Test the complete restore and verification path.",
    "index-only scans": "An index-only scan can return needed values from an index, but PostgreSQL may still visit the heap to check visibility when the visibility map cannot establish it.",
    "xmin": "A PostgreSQL tuple's xmin identifies its inserting transaction. Visibility depends on the snapshot and transaction status, not on comparing xmin as an ordinary timestamp.",
    "xmax": "A tuple's xmax records deletion or locking transaction information. A nonzero xmax alone does not prove the tuple is invisible; transaction state and locking cases matter."
  },
  kubernetes: {
    "readiness": "Readiness indicates whether a container is ready to serve traffic. A failed readiness probe removes readiness; it does not itself restart the container.",
    "liveness probes": "A liveness probe checks whether restarting a container may recover it. Repeated failures trigger a restart; dependence on a shared failing service can create a restart storm.",
    "startup": "A startup probe allows a container to finish initialization before liveness and readiness probing begins. It is useful when startup duration differs from steady-state health.",
    "oomkilled": "OOMKilled reports termination due to an out-of-memory condition. Inspect container limits, actual memory use, and node evidence before assuming the application has a leak.",
    "crashloopbackoff": "CrashLoopBackOff describes delayed retries after repeated container failures. It is a symptom, not a root cause; inspect previous logs, exit reason, configuration, and probes.",
    "imagepullbackoff": "ImagePullBackOff means Kubernetes is backing off failed image pulls. Check the image reference, registry access, credentials, and events.",
    "pending": "A Pending Pod has been accepted but its containers are not yet set up to run. Scheduling constraints and image preparation are different possible causes; inspect conditions and events.",
    "spec": "spec contains the desired configuration of a Kubernetes object. Controllers act on this desired state; writing it does not guarantee the requested state is achievable.",
    "status": "status reports observed state recorded by Kubernetes components. Compare it with spec and conditions to see whether reconciliation succeeded.",
    "resource versions": "A resourceVersion identifies an object's persisted version for change detection and optimistic concurrency. Treat it as opaque rather than a wall-clock timestamp or an arithmetic counter."
  }
};

TERM_CORRECTIONS.fastapi = {
  "cli": "The FastAPI CLI starts a development or production server for an application. Record the command, import path, installed versions, and configuration so startup is reproducible.",
  "streaming": "A streaming response sends body chunks incrementally. Its generator and resources must remain valid for the stream's lifetime, and disconnects or errors must release them.",
  "request": "A Request object provides lower-level access to the incoming ASGI request, including headers, URL, state, and body operations. Reading raw data this way does not automatically apply a Pydantic model.",
  "cleanup scopes": "A yield dependency's scope determines when cleanup runs. Function-scoped cleanup runs after the path operation and before sending the response; request-scoped cleanup runs after the response is sent. Verify support and behavior in the installed FastAPI version.",
  "jwt validation": "JWT validation checks the allowed signature algorithm, key, issuer, audience, expiration, and other required claims. Decoding a token without these checks does not authenticate it; object authorization is still separate.",
  "dependencies": "FastAPI dependencies declare values or checks that an endpoint needs. FastAPI resolves their dependency graph, can cache results within a request, and manages yield-based cleanup according to scope.",
  "pools": "A pool reuses a bounded set of resources, such as database connections or threads. Measure both time waiting for a slot and time using it; adding workers can multiply total resource demand."
};

Object.assign(TERM_CORRECTIONS.javascript, {
  "consoles": "A console displays values and errors and can evaluate code in a selected runtime context. Developer tools may display an object's later state, so capture primitive values or snapshots when proving a mutation sequence.",
  "syntax grammar": "The syntax grammar specifies which token sequences form valid JavaScript programs. Parsing success does not establish that names exist or that runtime operations will succeed.",
  "numeric precision": "Numeric precision limits which values a representation distinguishes. Number safely distinguishes consecutive integers through 2^53−1; use a suitable exact representation when rounding would violate the contract.",
  "valueof": "valueOf is a method an object can expose for primitive conversion. Its effect depends on the conversion operation and whether Symbol.toPrimitive overrides the ordinary method order.",
  "tostring": "toString can provide a string representation of a value. During ordinary primitive conversion its position relative to valueOf depends on the conversion hint.",
  "operators": "Operators select language operations such as addition, comparison, or assignment. Their operand conversion rules differ; for example, + can concatenate strings while subtraction requests numeric conversion.",
  "equality": "JavaScript has several equality relations. Choose strict equality, Object.is, or SameValueZero according to the required treatment of types, NaN, signed zero, and object identity.",
  "relational comparison": "Relational operators compare ordered values after specified conversions. Two strings compare lexicographically by code units; mixed operands and NaN require separate reasoning.",
  "declarations": "Declarations introduce bindings or definitions, such as let, const, function, and class. Their creation and initialization timing differs, which explains hoisting and temporal-dead-zone behavior.",
  "name resolution": "Name resolution searches lexical environment records for a binding. It does not search the local variables of whichever function happens to call the code.",
  "call stack": "The call stack tracks active execution contexts. A nested synchronous call adds work to that stack; deferred callbacks run through later host scheduling.",
  "scripts": "A script is a JavaScript source unit evaluated using script rules, distinct from module rules. Global bindings and strictness depend on the host and source form.",
  "functions": "A function is a callable object with code and an associated lexical environment. Its return value, parameters, this behavior, and ability to construct depend on the function form.",
  "let": "let creates a reassignable lexical binding scoped to its block. Access before initialization throws ReferenceError, even when using typeof on that binding.",
  "global bindings": "Global bindings are names accessible through the global environment. In browser scripts, top-level var and top-level let do not have identical relationships to properties of the global object.",
  "captured bindings": "A closure retains access to lexical bindings, not necessarily frozen copies of their current values. A later assignment to a captured binding can be visible when the closure runs.",
  "factories": "A factory is a function that creates a value or object for callers. A closure-based factory can keep instance state in its lexical environment without requiring a class.",
  "privacy": "Privacy limits which code can access state. Closures and #private elements provide different access mechanisms; a naming convention such as _secret is not enforcement.",
  "retention": "Retention means an object remains reachable through a reference path. A closure, listener, or cache can retain a large object graph after the original operation ends.",
  "expressions": "An expression is evaluated to produce a value or an abrupt completion such as an exception. A function expression creates a function value where an expression is allowed.",
  "arrows": "An arrow function resolves this and arguments lexically and cannot be called with new. Replacing an ordinary method with an arrow can therefore change behavior.",
  "parameters": "Parameters are the function's input bindings. Defaults apply when the argument is undefined, and a rest parameter collects remaining arguments into an array.",
  "call": "Function.prototype.call invokes a function with an explicit this argument followed by individual arguments. It cannot replace an arrow function's lexical this.",
  "apply": "Function.prototype.apply invokes a function with an explicit this argument and an array-like argument list. A large expanded argument list can exceed runtime limits.",
  "bind": "Function.prototype.bind creates a function with a bound this argument and optional leading arguments. Construction has special behavior; binding does not change an arrow's lexical this.",
  "method calls": "A call such as object.method() uses the receiver to supply this for an ordinary function. Extracting the function and calling it separately can lose that receiver.",
  "constructors": "A constructor can be invoked with new to create or return an object. Not every callable is constructible; arrow functions, for example, cannot be constructors.",
  "property keys": "Object property keys are strings or symbols. Other supplied keys are converted, so an ordinary object used as a key can collide with another object's string representation.",
  "own properties": "An own property belongs directly to an object rather than to its prototype. Object.hasOwn distinguishes an own property from an inherited one.",
  "descriptors": "A property descriptor specifies a data value or accessor functions and flags such as enumerable and configurable. Data descriptors also control writability.",
  "delegation": "Prototype delegation lets an object reuse behavior found on another object. The property can be inherited while the receiving object remains the method's this value.",
  "property lookup": "An ordinary property read checks the object's own property, then its prototype chain. An accessor may execute code; a Proxy can intercept the operation under invariant rules.",
  "object.create": "Object.create(proto) creates an object with the supplied prototype. Object.create(null) has no inherited Object.prototype methods, which is useful for some dictionary-like data.",
  "prototype mutation": "Prototype mutation changes an object's delegation path or shared inherited behavior. It can affect many readers and invalidate engine assumptions; avoid it without an explicit need.",
  "fields": "Class fields initialize state on each instance, or on the constructor for static fields. Initialization order matters, especially in derived classes around super().",
  "static initialization": "Static fields and static blocks initialize class-level state while the class is evaluated. They do not rerun for each instance.",
  "inheritance": "Class inheritance connects constructors and prototypes so a derived class can extend behavior. Preserve the base contract and understand derived initialization rather than treating inheritance as code copying.",
  "super": "super accesses a superclass constructor or inherited member using the method's home-object relationship. In a derived constructor, super() must initialize this before ordinary use of it.",
  "composition": "Composition builds behavior by combining collaborating values or functions. It can make ownership and change boundaries clearer than a deep inheritance hierarchy.",
  "arrays": "An Array is an object with special indexed-property and length behavior. It can contain holes and non-index properties; iteration and copying methods do not all treat holes alike.",
  "holes": "A hole is an absent indexed property inside an array's length. It differs from an own property whose value is undefined, even when a direct read looks the same.",
  "length": "An array's length bounds its own array indices; it need not equal the number of elements or one plus the largest present index. Increasing length creates holes, not elements. Reducing it attempts to delete higher indexed properties and can fail on non-configurable ones.",
  "mutation": "Mutation changes an existing object's state. Every holder of that same object value may observe it, unlike rebinding one local variable to a new object.",
  "copying methods": "Copying array methods produce a new array instead of changing the receiver. Nested object values can still be shared; a new array does not imply a deep copy.",
  "sorting": "Array.sort mutates the array and defaults to string-based comparison. Supply a consistent numeric comparator when sorting numbers; use a copying approach when mutation is not allowed.",
  "element kinds": "V8 element kinds describe internal array-storage specializations, such as packed or holey numeric elements. They are implementation details to investigate through measurement, not language guarantees.",
  "elements kinds": "V8 elements kinds are internal classifications of indexed storage. Mixed values or holes may change optimization opportunities without changing the specified JavaScript result.",
  "keys": "A Map can use arbitrary values as keys and compares object keys by identity. Ordinary object property keys instead become strings or symbols.",
  "collection selection": "Choose a collection from its required operations: Map for keyed lookup, Set for uniqueness, Array for ordered indexed data, or a weak collection for non-owning associations.",
  "iterable": "An iterable exposes Symbol.iterator, which returns an iterator. for...of and spread use that protocol to obtain successive values.",
  "iterator": "An iterator exposes next(), returning an object with value and done. Its internal state determines which result is produced on the next call.",
  "iterator helpers": "Iterator helpers compose lazy operations such as mapping or filtering over an iterator. Check runtime support and remember that consuming an iterator advances its state.",
  "custom traversal": "Custom traversal defines how values are produced through the iteration protocol. Make completion, early exit, and resource cleanup part of the iterator's contract.",
  "yield": "yield suspends a generator and exposes a value to its consumer. A later next(value) resumes execution and supplies the result of the suspended yield expression.",
  "yield star": "yield* delegates iteration to another iterable. It forwards traversal operations according to the delegation protocol and can return the delegate's completion value.",
  "next": "A generator's next(value) resumes execution. Its result reports a yielded or returned value and whether the generator has completed; the first supplied value has no suspended yield to receive it.",
  "throw": "throw raises an exception. A generator's throw(error) resumes it by throwing at its suspended point, where a surrounding catch may handle the error.",
  "return": "return completes the current function with a value. Calling a generator's return(value) requests completion and can execute finally blocks before it finishes.",
  "suspended execution": "Suspended execution preserves a generator or async function's continuation while other work proceeds. Resuming it continues from that point rather than calling the function from the beginning.",
  "protocols": "A protocol is an agreed set of operations that language features use, such as Symbol.iterator for iteration. Implementing the expected shape lets a custom value participate.",
  "customization hooks": "Customization hooks let an object participate in a built-in operation through a specified method or symbol. Their contracts constrain when they run and what they may return.",
  "traps": "Proxy traps intercept operations such as property reads, writes, or own-key enumeration. They must respect invariants of the target rather than inventing arbitrary contradictory behavior.",
  "revocation": "A revocable Proxy can be disabled using its revoke function. Subsequent operations through that proxy fail, while references to the original target remain separate capabilities.",
  "metaprogramming": "Metaprogramming inspects or customizes program behavior using mechanisms such as descriptors, Proxy, and Reflect. It increases indirection, so preserve explicit contracts and debugging evidence.",
  "errors": "An Error object carries diagnostic information about failure. Throwing transfers control; returning an Error is just returning a value unless the caller treats it specially.",
  "try": "A try block defines a region whose thrown exceptions can be handled by an associated catch, with optional finally cleanup. Unawaited asynchronous failures do not automatically enter that catch.",
  "catch": "catch handles an exception thrown through its associated try block. Translate or recover deliberately; silently swallowing the error can leave callers believing work succeeded.",
  "finally": "finally runs when control leaves try/catch, including many abrupt exits. A return or throw inside finally can replace the earlier completion, so avoid masking the original result.",
  "cause": "An Error's cause can preserve an underlying failure when translating it into a higher-level error. Do not expose sensitive internal details merely because the chain exists.",
  "custom errors": "A custom error names a meaningful failure category and can carry structured context. Use categories that help callers recover or operators diagnose, rather than one subclass for every message.",
  "linking": "Module linking connects imports and exports and prepares module environments before evaluation. Live bindings can exist before their values are initialized, which matters in cycles.",
  "evaluation": "Module evaluation executes module code after linking. Dependencies, cycles, and top-level await determine when exported values become usable.",
  "cycles": "A module cycle occurs when dependencies eventually lead back to a module already in the graph. An early read of an uninitialized lexical export can throw even though linking succeeded.",
  "import attributes": "Import attributes attach metadata to a module request, such as a required module type. Supported attributes and loading behavior depend on the host.",
  "module boundaries": "A module boundary controls which bindings are exported and how code depends on other modules. It is a code-organization boundary, not automatic process or permission isolation.",
  "code splitting": "Code splitting divides shipped code into separately loadable pieces. Measure reduced initial work against later fetch latency, failure handling, and cache behavior.",
  "tree shaking": "Tree shaking removes code a build tool determines is unused. Static module structure and side-effect declarations affect what can safely be eliminated.",
  "resolving functions": "A Promise executor receives resolve and reject functions that lock in its outcome. Calling resolve with a thenable can make the promise follow that thenable rather than immediately fulfill.",
  "chaining": "then returns a new Promise whose outcome depends on the handler's return or throw. Returning another promise makes the chain follow it; forgetting return can detach work from the chain.",
  "await": "await suspends the current async continuation until the awaited value is assimilated and its Promise outcome is ready. Rejection resumes by throwing at the await expression.",
  "suspension": "Suspension pauses an async function's continuation at await. It does not create a worker thread or make synchronous computation before the await non-blocking.",
  "resumption": "Resumption continues a suspended async function through a Promise job. Local bindings persist, but shared external state may have changed while the function was suspended.",
  "sequential execution": "Awaiting one independent operation before starting the next serializes their latency. Start independent work together only with an appropriate concurrency and failure budget.",
  "tasks": "A host task runs scheduled work such as a timer callback or event handler. Host event-loop rules determine task selection and when microtask checkpoints occur.",
  "rendering": "Browser rendering updates style, layout, and pixels at rendering opportunities. Long JavaScript tasks or continuously queued microtasks can delay those opportunities.",
  "browser hosts": "A browser supplies APIs such as the DOM, events, networking, and rendering around the JavaScript engine. Those behaviors are not all specified by ECMAScript.",
  "node hosts": "Node supplies server-oriented APIs and its event-loop integration around the JavaScript engine. Browser scheduling diagrams do not describe every Node phase or nextTick behavior.",
  "partial failure": "Partial failure means some operations succeed while others fail or remain uncertain. Decide whether to return partial results, compensate, retry safely, or fail the overall operation.",
  "abortsignal": "An AbortSignal communicates an abort request to participating APIs. The operation must support the signal; observing abort does not undo effects already committed.",
  "cleanup": "Cleanup releases resources owned by an operation, including listeners, timers, and connections. Give normal completion, failure, and cancellation the same explicit cleanup responsibility.",
  "json": "JSON is a text data format with objects, arrays, strings, numbers, booleans, and null. It does not preserve arbitrary JavaScript values, prototypes, shared identity, or cycles.",
  "transfer": "Transfer moves ownership of supported resources, such as an ArrayBuffer, instead of copying their content. The sender can lose access to a transferred buffer.",
  "date": "A Date represents a time value in milliseconds relative to the Unix epoch, or an invalid time value. It does not store the user's original time-zone identifier.",
  "formatting": "Date and number formatting turns values into locale-dependent display text. Keep display formatting separate from canonical storage and machine parsing.",
  "collation": "Collation defines language-sensitive string ordering. Intl.Collator can compare names using a chosen locale and sensitivity instead of raw code-unit order.",
  "temporal modeling": "Temporal modeling distinguishes instants, local dates, recurring schedules, time zones, and durations. Conflating them produces errors around daylight-saving changes and calendar arithmetic.",
  "groups": "Regular-expression groups combine pattern pieces and can capture matched substrings. Capturing changes the result structure; noncapturing groups only structure the pattern.",
  "lookarounds": "Lookaround assertions test surrounding text without consuming it as part of the match. Their semantics and support must match the runtime and chosen pattern.",
  "backtracking": "A backtracking regular-expression engine may revisit alternative paths after a match attempt fails. Ambiguous nested repetitions can cause very high work on adversarial input.",
  "redos": "Regular-expression denial of service occurs when a pattern consumes excessive computation on crafted input. Bound input and use an algorithm or pattern with predictable work.",
  "typedarray": "A TypedArray is an indexed numeric view over a buffer. Its element type, offset, and length determine how shared bytes are interpreted.",
  "binary data": "Binary data is a sequence of bytes interpreted under a format. Validate lengths, offsets, signedness, and byte order before decoding fields.",
  "detachment": "Detachment makes an ArrayBuffer's data unavailable to its former owner, for example after transfer. Operations on existing views then follow detached-buffer rules.",
  "agents": "An ECMAScript agent is an execution entity participating in the language's execution and memory model. It is unrelated to an LLM agent's planning loop.",
  "workers": "Workers execute JavaScript in a separate execution context and communicate through messages or permitted shared memory. Include startup, serialization, and coordination costs in the design.",
  "memory ordering": "Memory ordering specifies which shared-memory observations are permitted across agents. Atomics provides defined coordination; ordinary shared reads and writes are not a substitute for a synchronization protocol.",
  "synchronization": "Synchronization coordinates access or progress between concurrent participants. State the invariant and use the relevant atomic or message protocol to establish it.",
  "parsing": "Parsing checks source syntax and constructs an internal representation. An engine's parser may defer work, but all optimizations must preserve observable language semantics.",
  "ast": "An abstract syntax tree represents the syntactic structure of a program. It is an implementation/tooling representation, not a trace of actual runtime calls.",
  "bytecode": "Bytecode is an engine-specific instruction representation used by some execution tiers. Its format is not a portable JavaScript API.",
  "interpreter": "An interpreter executes an internal representation without requiring every path to be optimized machine code first. Modern engines can combine interpretation and compilation tiers.",
  "jit": "Just-in-time compilation generates machine code during execution, often using observed behavior to specialize hot paths. Its benefits and warmup costs depend on the workload and engine.",
  "deoptimization": "Deoptimization moves execution out of specialized code when its assumptions no longer hold. Correct engines preserve the same language result despite changing execution strategy.",
  "shapes": "Shapes describe recurring internal object layouts in engines such as V8. Consistent layouts can help property-access optimization, but measurement should justify layout-driven changes.",
  "optimization": "Optimization changes an implementation to improve a measured resource cost while preserving its contract. Start from a representative profile and verify correctness after the change.",
  "performance": "Performance describes costs such as latency, throughput, CPU, and memory under a stated workload. A fast isolated microbenchmark may not improve the user-visible operation.",
  "measurement": "Measurement records behavior at a defined boundary under known inputs and runtime conditions. Repeat runs and include variance before attributing a change to one code edit.",
  "complexity": "Complexity describes how resource use grows with input size under a cost model. It does not replace measurement of constants, allocation, or runtime behavior.",
  "warmup": "Warmup is the execution period during which caches and engine feedback may change performance. Report cold-start and steady-state measurements separately when both matter.",
  "variance": "Measurement variance is the spread of repeated observations. Background load, garbage collection, and adaptive optimization can make one timing sample misleading.",
  "event-loop responsiveness": "Event-loop responsiveness measures how promptly scheduled work can run. Long synchronous work and starvation can delay timers and input even when asynchronous APIs are used elsewhere.",
  "javascript security": "JavaScript security requires controlling untrusted values at executable, object-mutation, network, and dependency boundaries. TypeScript checks or a framework do not remove those runtime responsibilities.",
  "eval": "eval parses and executes a string as JavaScript. Avoid giving untrusted data this capability; use structured data and explicit operations instead.",
  "async tests": "Async tests wait for the operation under test and assert its result, error, and cleanup. An unreturned or unawaited promise can let a test finish before failure is observed.",
  "timers": "Timers schedule callbacks after a delay threshold under host rules. The delay is not an exact execution time; a busy event loop can run the callback later.",
  "module cohesion": "Module cohesion means the responsibilities grouped in a module belong together and tend to change for related reasons. A file containing unrelated utilities has weak cohesion despite being small.",
  "dependency direction": "Dependency direction records which module relies on another's contract. Keep volatile effects behind explicit boundaries so core policy does not depend on incidental framework details.",
  "functional core": "A functional core expresses domain decisions as functions of explicit input, leaving I/O to surrounding code. This makes policy easier to test without simulating external systems.",
  "object boundaries": "Object boundaries define who can observe or mutate an object's state. Exposing mutable internals lets callers bypass invariants even when the class methods are carefully designed.",
  "event pipeline": "An event pipeline accepts events, validates them, processes bounded work, and records outcomes. Identity, retry, ordering, and cleanup policies determine behavior under failure.",
  "reliability": "Reliability is meeting the intended behavior under stated operating and failure conditions. Define the promise and test failures rather than treating absence of exceptions as success.",
  "security": "Security preserves intended authority and data boundaries against misuse. Validate external input, constrain capabilities, and test denied as well as allowed operations."
});

// Only meanings that remain the same across tracks belong here. Overloaded terms
// such as scope, state, keys, inference, normalization, and admission stay local.
Object.assign(TERM_CORRECTIONS["computer-science"] ||= {}, {
  "hashing": "Hashing maps a key to a bounded hash value used to select storage. Collisions require equality checks and a resolution strategy.",
  "sets": "A set stores distinct values under an equality rule and supports membership operations.",
  "grouping": "Grouping collects items under a shared key, such as grouping anagrams by character counts.",
  "lookup tradeoffs": "Choose lookup structures by update cost, memory, ordering needs, and worst-case guarantees, not only average lookup time.",
  "converging scans": "Two pointers move inward from opposite ends while an invariant justifies discarding candidates.",
  "same-direction scans": "Two pointers advance in the same direction, often maintaining a window or separating read and write positions.",
  "partitioning": "Partitioning rearranges or separates values according to a predicate, preserving a stated relationship between the regions.",
  "sorted invariants": "A sorted invariant states which region is ordered and how that order permits pruning or efficient lookup.",
  "fixed windows": "A fixed window maintains information about exactly k consecutive elements as its boundaries advance.",
  "variable windows": "A variable window grows and shrinks to satisfy a condition. Linear scanning requires that boundary movements are justified by monotonic behavior.",
  "frequency constraints": "Frequency constraints limit counts of values within a region, such as at most two occurrences of each character.",
  "monotonic deques": "A monotonic deque retains ordered candidates while removing dominated or expired items, supporting sliding-window extrema.",
  "monotonic stacks": "A monotonic stack keeps values in order so a new value can resolve earlier candidates, such as the next greater element.",
  "matching": "Matching pairs compatible items under constraints, such as closing brackets with the most recent unmatched opening bracket.",
  "deferred resolution": "Deferred resolution stores unresolved candidates until later input supplies the evidence needed to decide them.",
  "amortized analysis": "Amortized analysis bounds total cost across a sequence, allowing occasional expensive operations without claiming every operation is cheap.",
  "linked lists": "A linked list stores nodes connected by references. Given the right node, relinking can be constant time; locating it may not be.",
  "pointer surgery": "Pointer surgery changes node links while preserving reachability and list invariants. Save references before overwriting them.",
  "priority queues": "A priority queue retrieves an extreme-priority item efficiently without keeping all items in fully sorted order.",
  "top-k selection": "Top-k selection finds the k best items without necessarily sorting the entire input; a bounded heap is one option.",
  "multiway merge": "Multiway merge combines sorted streams by repeatedly selecting the smallest available head, often using a heap.",
  "streaming medians": "Streaming medians maintain the middle of an evolving sequence, commonly using balanced lower and upper heaps.",
  "rotated order": "A rotated sorted sequence has an ordering break. Binary search must identify which portion remains ordered before discarding candidates.",
  "depth-first search": "Depth-first search explores a branch before siblings, tracking visited nodes to avoid repeated work or cycles.",
  "recursive frames": "Recursive frames retain a call's local state and return position while a nested call runs.",
  "iterative stacks": "An explicit stack stores pending traversal work, replacing call-stack recursion while making saved state visible.",
  "tree paths": "A tree path is a sequence of connected nodes without repeats. Parent information or recursion can reconstruct it.",
  "connected regions": "Connected regions contain nodes mutually reachable through permitted edges; traversal can label each component.",
  "local choices": "A local choice selects an immediate option. A greedy algorithm needs proof that this choice preserves some optimal solution.",
  "recurrences": "A recurrence expresses a result or cost through smaller subproblems, with base cases preventing infinite regress.",
  "adjacency structures": "Adjacency lists or matrices encode graph edges with different memory and neighbor-query costs.",
  "topological order": "A topological order places every directed dependency before its dependent and exists only for a directed acyclic graph.",
  "weighted shortest paths": "Weighted shortest paths minimize total edge weight. Algorithm choice depends on negative edges, cycles, and graph structure.",
  "reversible state": "Reversible state changes can be undone exactly after exploring a choice, preserving the parent search branch.",
  "constraint search": "Constraint search explores candidate assignments and prunes those that already violate required conditions.",
  "breadth-first search": "Breadth-first search processes reachable nodes by edge-distance layers using a queue.",
  "levels": "Traversal levels group nodes at the same number of edges from a starting node or root.",
  "shortest unweighted paths": "In an unweighted graph, breadth-first search discovers minimum-edge-count paths when nodes are marked consistently.",
  "multi-source expansion": "Multi-source expansion initializes traversal from several sources at once, computing distance to the nearest source.",
  "prefix trees": "A prefix tree stores sequences along edges so shared prefixes share structure.",
  "shared prefixes": "Shared prefixes are identical initial subsequences that can reuse storage or narrow a search together.",
  "autocomplete": "Autocomplete locates stored completions of a prefix, then applies a ranking and result-limit policy.",
  "wildcard search": "Wildcard search branches over allowed alternatives at wildcard positions, potentially increasing search cost substantially.",
  "running aggregates": "A running aggregate incrementally maintains a summary such as a prefix sum rather than rescanning all prior values.",
  "range queries": "Range queries summarize or retrieve values within boundaries; prefix sums or trees trade update cost against query cost.",
  "subarray counts": "Subarray counting measures contiguous ranges meeting a condition; prefix-state frequencies can turn repeated scans into lookups.",
  "matrices": "Matrices store values by row and column. State dimensions and coordinate conventions before traversal or transformation.",
  "coordinate transforms": "Coordinate transforms map positions between representations, such as rotating row-column indices while respecting dimensions.",
  "boundary walks": "Boundary walks visit the perimeter of a region, carefully avoiding repeated corners and handling empty interiors.",
  "in-place rotation": "In-place rotation rearranges values using limited auxiliary storage, with cycles or transposition and reversal where applicable.",
  "marker encoding": "Marker encoding stores temporary state within existing data when the representation has a safe spare value or bit.",
  "merging": "Merging combines ordered items or overlapping intervals while maintaining an explicit output invariant.",
  "insertion": "Insertion adds an item while preserving structure, such as sorted order or nonoverlapping intervals.",
  "sweep events": "Sweep events mark changes while moving through ordered positions, such as interval starts and ends.",
  "overlap counting": "Overlap counting tracks simultaneously active intervals; equal-position event order depends on whether endpoints are inclusive.",
  "set-bit iteration": "Set-bit iteration visits only one-bits, often clearing the lowest set bit with x & (x - 1) for nonnegative integers.",
  "subset encoding": "Subset encoding uses one bit per element to represent membership; the number of possible subsets grows exponentially."
});

Object.assign(TERM_CORRECTIONS["systems-foundations"] ||= {}, {
  "request to wire": "A request passes through application encoding, transport, network routing, and link framing before bytes or signals cross a link.",
  "encapsulation": "Encapsulation wraps a higher-layer payload in lower-layer headers and possibly trailers; receivers remove the corresponding layers.",
  "frames": "Frames are link-layer transmission units exchanged across a local link.",
  "packets": "IP packets carry network-layer addressing and payload through routed networks.",
  "segments": "TCP segments carry sequence-numbered portions of a byte stream and control information.",
  "application messages": "Application messages define protocol-level units such as requests. TCP byte reads need not align with their boundaries.",
  "ipv4": "IPv4 uses 32-bit addresses and packet delivery without an end-to-end reliability guarantee.",
  "ipv6": "IPv6 uses 128-bit addresses and different network-control mechanisms, including neighbor discovery rather than ARP.",
  "cidr": "CIDR expresses a network prefix as an address and prefix length, such as /24 for 24 fixed leading bits.",
  "subnet masks": "A subnet mask identifies the network-prefix bits of an IPv4 address.",
  "mac addresses": "MAC addresses identify link-layer interfaces within relevant local-link communication; they are not globally routed IP destinations.",
  "arp": "ARP discovers an IPv4 neighbor's link-layer address on a local network.",
  "neighbor discovery": "IPv6 neighbor discovery uses ICMPv6 for functions including neighbor address resolution and router discovery.",
  "routing tables": "Routing tables associate destination prefixes with next hops or interfaces.",
  "longest-prefix match": "Longest-prefix matching selects the most specific applicable destination prefix from the routing table.",
  "default gateways": "A default gateway is the next hop used when no more specific route matches a destination.",
  "icmp": "ICMP carries network control and error information; it does not provide application transport reliability.",
  "traceroute": "Traceroute probes increasing hop limits to infer a path from responses. Filtering and asymmetric routing limit what the result proves.",
  "nat": "NAT rewrites network addresses, often ports too, while tracking mappings. It complicates end-to-end addressing and is not authentication.",
  "handshakes": "A handshake exchanges control messages to establish shared connection or protocol state before ordinary communication.",
  "sequence numbers": "TCP sequence numbers identify positions in a byte stream, supporting ordering, acknowledgements, and retransmission.",
  "acknowledgements": "TCP acknowledgements report received stream progress, not application processing or durable storage of a request.",
  "mtu": "MTU is the maximum network-layer packet size a link can carry without link-specific fragmentation behavior.",
  "mss": "TCP MSS limits segment payload size, excluding TCP and IP headers, and is related to the usable path packet size.",
  "tcp flow control": "TCP flow control uses the receiver's advertised window to avoid sending more outstanding data than it can accept.",
  "congestion control": "Congestion control adapts sending pressure to network conditions, distinct from a receiver's buffer capacity.",
  "slow start": "Slow start increases TCP's congestion window rapidly from a small starting point until a threshold or congestion signal changes behavior.",
  "aimd": "Additive increase and multiplicative decrease cautiously raises sending allowance and sharply reduces it after congestion evidence.",
  "retransmission": "Retransmission sends unacknowledged data again after loss is inferred. The receiver must distinguish repeats from new bytes.",
  "overload": "Overload occurs when offered work exceeds sustainable capacity, growing queues or causing rejection and failure.",
  "tls handshakes": "TLS handshakes negotiate cryptographic parameters and authenticate peers as configured, establishing keys for protected traffic.",
  "http semantics": "HTTP semantics define the meaning of methods, fields, status codes, and representations independently of a particular wire version.",
  "http versions": "HTTP versions differ in message framing and transport behavior while retaining core HTTP semantics.",
  "connection reuse": "Connection reuse carries multiple operations over an established connection, reducing setup cost but introducing shared lifecycle and capacity concerns.",
  "system calls": "System calls cross from application code into operating-system services with validation and privilege-boundary handling.",
  "user mode": "User mode restricts application access to privileged instructions and protected resources.",
  "kernel mode": "Kernel mode permits privileged operating-system work; bugs there can affect the whole system.",
  "process creation": "Process creation allocates an execution context and resources; the exact inheritance and startup mechanism depends on the OS.",
  "exec": "On Unix-like systems, exec replaces the current process image with another program while preserving selected process properties.",
  "exit": "Process exit ends execution, releases OS-owned resources, and supplies a status for its parent or supervisor.",
  "process control blocks": "A process control block stores kernel bookkeeping for an execution context, including scheduling and resource state.",
  "context switches": "A context switch saves one execution context and restores another, with direct work and possible cache-related costs.",
  "cpu scheduling": "CPU scheduling selects runnable work according to policy, balancing responsiveness, throughput, and fairness.",
  "race conditions": "A race condition makes correctness depend on an uncontrolled ordering of concurrent actions.",
  "atomics": "Atomic operations provide indivisible access and specified memory-ordering guarantees; composing several atomics does not automatically preserve a larger invariant.",
  "address spaces": "An address space is the range and mapping of addresses visible to an execution context.",
  "segmentation": "Segmentation maps logical memory regions with region-specific bounds or permissions; it differs from fixed-size paging.",
  "paging": "Paging maps fixed-size virtual pages to physical frames, allowing flexible placement and protection.",
  "page tables": "Page tables describe virtual-to-physical mappings and access permissions used during address translation.",
  "the mmu": "The memory management unit performs hardware-supported address translation and permission checking.",
  "virtual memory": "Virtual memory gives processes an address abstraction separate from physical placement, supporting isolation and demand-based allocation.",
  "demand paging": "Demand paging brings a required page into memory when access faults rather than loading everything eagerly.",
  "replacement": "Page replacement chooses which resident page to evict when memory is needed, trading future faults against current pressure.",
  "disks": "Magnetic disks persist data on rotating media; seek and rotational latency make access patterns important.",
  "ssds": "SSDs persist data in flash, with erase/write constraints and controller behavior affecting latency, endurance, and recovery.",
  "vfs": "A virtual filesystem layer presents common file operations over different filesystem implementations.",
  "journaling": "Journaling records selected filesystem changes to support recovery after interruption. It does not imply every application write is durable.",
  "durability": "Durability means acknowledged data survives the failures covered by a stated contract. Buffered writes may require explicit synchronization.",
  "systems debugging capstone": "The systems capstone links application symptoms to process, memory, filesystem, and network evidence, validating the actual failure cause."
});

Object.assign(TERM_CORRECTIONS.react ||= {}, {
  "react setup": "React setup selects a supported build or framework, matching packages, and a repeatable development and production command.",
  "project anatomy": "Project anatomy identifies entry points, components, assets, configuration, and ownership boundaries.",
  "module graph": "The module graph connects imports to dependencies and determines what can enter a browser or server bundle.",
  "development tooling": "Development tooling provides compilation, diagnostics, refresh, and debugging; its behavior can differ from production.",
  "elements": "React elements describe intended UI; they are not mounted DOM nodes or component instances.",
  "expressions": "JSX expressions insert JavaScript values into element descriptions, subject to React's rendering rules.",
  "fragments": "Fragments group children without adding a wrapper DOM element.",
  "element creation": "Element creation builds a description of type, props, and children; rendering and commit determine the actual UI.",
  "children": "children carries nested renderable content, enabling composition without hard-coding every descendant.",
  "public component apis": "A component's public API includes props, events, accessibility behavior, and supported composition patterns.",
  "rules of react": "React relies on pure rendering, immutable props/state snapshots, and valid Hook usage so rendering can be repeated safely.",
  "propagation": "Event propagation determines which handlers observe an event along its path; stopping propagation differs from preventing default browser behavior.",
  "event handlers": "Event handlers respond to interactions and can request updates or perform interaction-specific effects.",
  "update priority": "Update priority distinguishes urgent interaction work from less urgent rendering, without promising a fixed execution time.",
  "update queues": "Update queues retain state updates for a future render; functional updates compose from pending state.",
  "reducer purity": "A reducer computes next state without mutation or external side effects, allowing repeatable evaluation.",
  "initialization": "Initialization establishes state when a component identity is created; an initializer is not rerun as a normal update mechanism.",
  "dispatch": "dispatch submits an action to a reducer's update queue; it does not synchronously replace the current render's state snapshot.",
  "state modeling": "State modeling stores the minimal independent facts needed to render UI while preventing contradictory combinations.",
  "normalization": "Normalization avoids duplicated entities or derived values in state, reducing synchronization bugs.",
  "lifting state": "Lifting state moves shared ownership to a common ancestor that passes values and update callbacks down.",
  "state preservation": "React preserves state when component type, position, and key retain the relevant identity.",
  "reset": "Reset deliberately creates initial state again, often by changing a key when the conceptual entity changes.",
  "createcontext": "createContext creates a context channel with a fallback value; the fallback is not a live shared store.",
  "providers": "Providers supply context values to descendants within their scope.",
  "defaults": "A context default applies when no matching provider exists; supplying undefined is not the same as omitting a provider.",
  "update propagation": "A changed provider value can update context consumers even when an ancestor's props are memoized.",
  "dom refs": "DOM refs expose committed nodes for imperative operations such as focus; they are not render-time data sources.",
  "ref callbacks": "Ref callbacks observe attachment and detachment and must manage any associated resources correctly.",
  "external synchronization": "External synchronization keeps a non-React system aligned with committed component inputs, with matching cleanup.",
  "setup": "Effect setup starts synchronization after commit; cleanup reverses the previous setup before replacement or removal.",
  "reactive dependencies": "Reactive dependencies are props, state, and render-scope values an Effect uses; omitting them can leave synchronization stale.",
  "stale closure repair": "Repair stale closures by expressing dependencies or update relationships honestly, not by suppressing dependency diagnostics.",
  "custom hooks": "Custom Hooks package reusable Hook-based logic; each call has its own state unless sharing is explicitly externalized.",
  "reusable stateful logic": "Reusable stateful logic shares behavior and lifecycle rules rather than necessarily sharing state storage.",
  "hook contracts": "Hook contracts describe inputs, outputs, identity guarantees, side effects, and cleanup expectations.",
  "memo": "memo can skip rendering when props compare unchanged; it is an optimization, not a guarantee or a substitute for correct state design.",
  "starttransition": "startTransition marks supported state updates as nonurgent, allowing urgent work to interrupt their rendering.",
  "interruptible rendering": "Interruptible rendering can pause or abandon render work; render-time side effects would therefore be unsafe.",
  "deterministic identity": "Deterministic identity keeps generated relationships consistent across renders and server/client output; useId is not a list-key generator.",
  "subscriptions": "Subscriptions notify React when external state changes and require cleanup to avoid stale observers.",
  "snapshots": "An external-store snapshot represents currently readable state and must have stable identity when unchanged.",
  "server snapshots": "Server snapshots establish a matching initial external-store value for server rendering and hydration.",
  "use": "React use reads supported resources such as a Promise or context; pending Promises can suspend and rejected ones reach error handling.",
  "promises": "A Promise-backed React resource needs stable ownership and error handling; recreating promises casually during rendering can cause repeated suspension.",
  "context resources": "Context resources supply scoped values through React's context mechanism rather than requiring prop forwarding at every level.",
  "conditional reads": "React use has different conditional-call allowances from ordinary Hooks; this exception does not relax all Rules of Hooks.",
  "pending ui": "Pending UI communicates unfinished work while preserving appropriate interaction and avoiding duplicate submissions.",
  "forms": "Forms coordinate browser submission, validation, values, and pending/error behavior; server-side validation remains necessary.",
  "file fields": "File inputs expose user-selected File objects; validate content and size at trusted boundaries rather than trusting names or client checks.",
  "client data fetching": "Client fetching needs ownership of loading, errors, cancellation, freshness, and stale-result prevention.",
  "caches": "A data cache stores results under keys with explicit freshness and invalidation rules, separate from render memoization.",
  "race prevention": "Race prevention stops older async results from overwriting newer intent, using request identity or cancellation plus guarded application.",
  "optimistic updates": "Optimistic updates show a predicted result before confirmation and must reconcile success, rejection, and concurrent changes.",
  "url state": "URL state stores shareable navigation choices such as filters and pagination; validate parsed values and browser history behavior.",
  "lazy": "lazy defers loading a component module until needed and integrates its pending state with Suspense.",
  "code splitting": "Code splitting divides bundles into independently loadable parts, trading initial bytes against later requests and loading coordination.",
  "streaming boundaries": "Streaming boundaries determine which server-rendered UI can arrive independently while other content remains pending.",
  "fallbacks": "Fallbacks provide UI while a boundary's content is unavailable; design them to preserve layout and meaningful context.",
  "rejected resources": "Rejected resources need an error boundary or explicit error path, not an indefinitely displayed loading state.",
  "overlays": "Overlays display UI above other content and need focus, dismissal, scroll, and accessibility ownership.",
  "stacking contexts": "Stacking contexts constrain z-index ordering; a large z-index cannot necessarily escape an ancestor's context.",
  "focus management": "Focus management deliberately moves and restores keyboard focus during interactions such as opening and closing a dialog.",
  "event trees": "Portal events propagate through the React tree, which can differ from the DOM placement tree.",
  "react with typescript": "React with TypeScript models component and event relationships statically while retaining runtime and accessibility obligations.",
  "component contracts": "Component contracts state supported props, outputs, state ownership, and accessible interaction behavior.",
  "refs": "Refs retain mutable references without triggering renders; avoid using them as hidden render state.",
  "polymorphism": "Polymorphic components vary their rendered element while preserving valid props and accessibility; excessive flexibility complicates callers and tests.",
  "styling": "Styling controls presentation through CSS or related tooling while preserving semantic structure and interaction.",
  "css architecture": "CSS architecture defines how styles are scoped, composed, named, and changed without unintended coupling.",
  "responsive design": "Responsive design adapts layout and interaction to available space and user needs rather than assuming one viewport.",
  "design systems": "Design systems coordinate reusable components, tokens, accessibility rules, and contribution practices.",
  "semantic html": "Semantic HTML uses native elements according to their meaning, gaining built-in interaction and accessibility behavior.",
  "keyboard interaction": "Keyboard interaction makes every relevant action reachable and operable with predictable focus and key behavior.",
  "focus": "Focus identifies the current keyboard interaction target and should remain visible and meaningful.",
  "announcements": "Accessible announcements communicate important dynamic changes without unnecessarily interrupting assistive-technology users.",
  "component tests": "Component tests exercise rendered behavior and interactions with controlled collaborators rather than private implementation details.",
  "browser tests": "Browser tests verify behavior in an actual browser, including navigation, layout-sensitive interactions, and integration boundaries.",
  "act": "act helps tests flush React-related updates before assertions; user-facing testing helpers often coordinate it.",
  "user behavior": "User behavior is observable interaction and outcome, such as submitting a form and seeing an error, not an internal setter call.",
  "profiler": "Profiler measures committed rendering work for a subtree so optimizations can be tied to evidence.",
  "render diagnostics": "Render diagnostics explain why work occurred and its cost; development behavior may differ from production.",
  "performance tracks": "React performance tracks associate runtime work with timeline events to investigate interaction and rendering costs.",
  "optimization workflow": "Measure a slow interaction, identify dominant work, change one cause, and compare under equivalent conditions.",
  "automatic memoization": "Compiler-assisted memoization can reuse eligible computations; it requires supported setup and correct React semantics.",
  "directives": "Compiler directives configure supported compilation behavior locally; they are not universal runtime commands.",
  "linting": "Linting checks patterns such as invalid Hook use and dependency mistakes before runtime.",
  "adoption": "Adoption introduces tooling or patterns incrementally with compatibility checks and measured benefit.",
  "render work": "Render work computes desired UI and may be repeated or abandoned; keep it free of external side effects.",
  "commit work": "Commit work applies accepted changes to the host environment and coordinates relevant lifecycle work.",
  "createroot": "createRoot creates a client-rendered React root in a DOM container.",
  "hydrateroot": "hydrateRoot attaches React behavior to compatible server-rendered HTML; mismatches indicate inconsistent initial output.",
  "strictmode": "StrictMode enables development checks, including extra lifecycle work that exposes impurity or missing cleanup; it is not production duplication.",
  "react dom client apis": "React DOM client APIs establish and manage browser roots for rendering or hydration.",
  "server rendering": "Server rendering produces initial UI output on the server; interactive browser behavior still needs the appropriate client code.",
  "streaming html": "Streaming HTML sends available server-rendered content progressively instead of waiting for the entire response.",
  "static rendering": "Static rendering produces noninteractive markup or precomputed output under a chosen server API and deployment model.",
  "bundle boundaries": "Bundle boundaries decide which modules ship to clients or remain on servers, affecting size and secret exposure.",
  "use server": "The use server directive identifies supported server functions in an integrated environment; it does not replace authentication or authorization.",
  "progressive enhancement": "Progressive enhancement preserves useful baseline browser behavior while adding richer client capabilities.",
  "framework boundaries": "Framework boundaries determine ownership of routing, rendering, data loading, caching, and server execution beyond core React.",
  "layouts": "Layouts compose persistent or route-specific UI structure; identity choices determine which state survives navigation.",
  "data loading": "Data loading coordinates request timing, ownership, caching, errors, and rendering so unrelated fetches do not form accidental waterfalls.",
  "resource preloading": "Resource preloading hints that a resource will soon be needed, trading earlier work for possible latency reduction.",
  "react security": "React security includes safe rendering, URL policies, authorization at server boundaries, and careful handling of raw HTML or third-party code.",
  "escaping": "Escaping treats ordinary rendered strings as text rather than executable markup; raw-HTML insertion bypasses that protection.",
  "component stacks": "Component stacks identify React ancestry relevant to an error, complementing JavaScript call stacks.",
  "source maps": "Source maps relate transformed code to original sources for debugging; publishing them can disclose implementation details.",
  "production incidents": "Production incidents require reproduction context, user impact, error evidence, mitigation, and follow-up rather than guesses from a stack trace alone.",
  "lifecycle methods": "Class lifecycle methods run at defined rendering or commit stages; migration should preserve behavior, not mechanically translate each method to an Effect.",
  "legacy context": "Legacy context is an older context mechanism; migrations should move to supported context APIs with explicit provider ownership.",
  "migration": "Migration changes implementation incrementally while preserving user-visible behavior and checking edge cases.",
  "react architecture": "React architecture places state and responsibilities near their owners while keeping framework and domain boundaries clear.",
  "feature boundaries": "Feature boundaries group behavior with coherent ownership and limit unrelated changes across the application.",
  "state placement": "State placement chooses the narrowest owner satisfying all consumers, avoiding unnecessary global stores or duplicate facts.",
  "scalable delivery": "Scalable delivery makes features independently understandable, testable, and releasable without sacrificing shared contracts.",
  "portfolio project 1": "This portfolio project turns frontend concepts into an inspectable application with tests, accessibility evidence, and documented trade-offs.",
  "production-quality frontend": "A production-quality frontend addresses accessibility, failures, security, performance, observability, and maintainability beyond the happy-path demo."
});

Object.assign(TERM_CORRECTIONS.python ||= {}, {
  "scripts": "Scripts are Python modules executed as entry points; imports and direct execution can establish different package context.",
  "code blocks": "Code blocks are execution units with name-binding rules; indentation groups statements but not every block creates a new scope.",
  "type": "An object's type determines supported behavior and operations. An annotation does not change the runtime object's type.",
  "value": "A value is the state represented by an object; names bind to objects rather than storing independently typed slots.",
  "numbers": "Python integers have arbitrary precision within available memory; floating-point values use finite precision and rounding.",
  "booleans": "bool represents True and False and is a subclass of int; avoid accepting booleans accidentally in numeric validation.",
  "none": "None is the singleton absence value. Test it with is None when other falsy values are meaningful.",
  "arithmetic": "Arithmetic follows each operand type's numeric protocol, including division, rounding, overflow, and precision behavior.",
  "strings": "str stores Unicode text, not encoded bytes. Encode and decode explicitly at byte boundaries.",
  "bytearray": "bytearray is a mutable sequence of byte values, useful when modifying binary buffers.",
  "text boundaries": "Text boundaries convert between Unicode strings and bytes using an explicit encoding and error policy.",
  "ranges": "range represents an integer progression compactly rather than allocating every element eagerly.",
  "unpacking": "Unpacking assigns or passes elements into separate positions, with arity and starred-capture rules.",
  "sequence costs": "Sequence costs depend on representation: list append is amortized constant time, while insertion near the front shifts elements.",
  "collisions": "Hash collisions occur when unequal keys share a hash; dictionaries still compare equality to distinguish keys.",
  "insertion order": "Python dictionaries preserve insertion order; sets do not promise the same ordering contract.",
  "control flow": "Control flow chooses which statements execute through conditions, loops, exceptions, and function calls.",
  "loops": "Loops repeat work by iteration or a condition; break and continue alter progress, while loop else depends on normal completion.",
  "assignment expressions": "The := operator binds a value inside an expression; use it only when the resulting control flow stays clear.",
  "parameters": "Parameters define accepted call inputs, including positional-only, keyword-only, defaults, and variadic arguments.",
  "args": "A *args parameter collects extra positional arguments into a tuple.",
  "kwargs": "A **kwargs parameter collects extra keyword arguments into a dictionary.",
  "lambdas": "A lambda creates a function from one expression and follows ordinary closure and name-resolution behavior.",
  "partials": "functools.partial creates a callable with selected arguments already supplied; it does not execute the underlying function immediately.",
  "custom errors": "Custom exception classes identify domain-specific failures while preserving normal exception handling and chaining.",
  "groups": "Exception groups represent multiple failures together, with except* handling matching subsets.",
  "notes": "Exception notes attach explanatory context without replacing the original exception type or traceback.",
  "failure design": "Failure design defines expected exceptions, recovery ownership, cleanup, and safe external error information.",
  "sys.modules": "sys.modules caches imported module objects by name and affects later imports and partially initialized circular imports.",
  "finders": "Import finders locate module specifications using the configured import machinery.",
  "loaders": "Import loaders create or execute modules according to a module specification.",
  "instances": "Instances are objects created according to a class's construction protocol, with behavior resolved through its type.",
  "attributes": "Attributes are named object members resolved through Python's lookup and descriptor protocols, not merely dictionary access.",
  "classmethods": "classmethod binds the class rather than an instance as the first argument, supporting polymorphic constructors and class-level behavior.",
  "staticmethods": "staticmethod exposes a function through a class without automatically binding an instance or class argument.",
  "binding": "Descriptor binding can transform a function retrieved from an instance into a bound method carrying that instance.",
  "inheritance": "Inheritance reuses and specializes behavior through the method-resolution order; subclasses must preserve required behavioral contracts.",
  "mixins": "Mixins contribute focused reusable behavior through inheritance and need compatible method and initialization conventions.",
  "data model": "Python's data model specifies protocols connecting objects to operations such as iteration, arithmetic, comparison, and context management.",
  "operators": "Operators dispatch through type-specific methods and may return NotImplemented to allow another operand's protocol handling.",
  "representations": "repr targets an informative developer representation; str targets readable display. Neither should accidentally expose secrets.",
  "attribute lookup": "Attribute lookup coordinates descriptors, instance storage, class inheritance, and fallback hooks.",
  "__getattribute__": "__getattribute__ participates in ordinary instance attribute access; custom implementations must avoid accidental recursive lookup.",
  "__getattr__": "__getattr__ supplies fallback behavior when normal attribute lookup fails.",
  "frozen models": "Frozen models restrict field assignment under their library's contract; nested mutable objects may still change.",
  "next": "next requests one iterator item and either returns it, uses a provided default, or propagates exhaustion.",
  "stopiteration": "StopIteration signals iterator exhaustion. Generators should normally return rather than explicitly raise it.",
  "sentinel iteration": "iter(callable, sentinel) repeatedly calls a function until its result equals the sentinel.",
  "lazy traversal": "Lazy traversal computes items only when requested, reducing eager storage but deferring work and possible errors.",
  "send": "Generator send resumes execution while supplying the value of the suspended yield expression.",
  "throw": "Generator throw raises an exception at the suspension point, allowing the generator's handlers or cleanup to respond.",
  "close": "Generator close requests termination and cleanup through GeneratorExit; it is not a normal request for another item.",
  "delegation": "yield from delegates iteration and parts of the generator protocol to another iterable or generator.",
  "generator expressions": "Generator expressions create lazy iterators from expression syntax instead of building an eager collection.",
  "itertools": "itertools supplies iterator-building operations for composition, grouping, products, and bounded traversal; some operations retain data or require ordering.",
  "laziness": "Laziness delays computation until consumption; it changes resource lifetime and when failures become visible.",
  "streaming pipelines": "Streaming pipelines process incremental items while controlling buffering, cleanup, and downstream failure propagation.",
  "wrappers": "Wrappers call or replace another callable to add behavior while preserving its intended contract.",
  "functools.wraps": "functools.wraps copies useful function metadata and exposes __wrapped__; it does not automatically preserve every runtime or typing behavior.",
  "stacking": "Stacked decorators apply in a defined nested order, so swapping them can change behavior.",
  "registration": "Registration records a callable or component in a registry; import-time registration can introduce order-dependent side effects.",
  "with": "with coordinates a context manager's entry and exit, including exceptional exits and possible exception suppression.",
  "contextlib": "contextlib provides context-manager helpers and exit stacks for explicit, exception-safe resource ownership.",
  "async context": "An async context manager awaits entry and exit, enabling asynchronous resource acquisition and cleanup.",
  "unions": "A union annotation permits values matching one of several types; runtime validation remains separate.",
  "aliases": "Type aliases name type expressions, while ordinary assignment can create another reference to the same mutable object.",
  "literal": "Literal annotations preserve specific allowed values such as named modes rather than an unrestricted base type.",
  "never": "Never denotes a type with no values, useful for unreachable paths and functions that do not return normally.",
  "static analysis": "Static analysis reasons about code without executing every path; assumptions and tool configuration limit its guarantees.",
  "type parameters": "Type parameters express relationships across generic inputs, outputs, and members rather than runtime validation.",
  "overloads": "Typing overloads advertise multiple call contracts above one runtime implementation, which must implement them honestly.",
  "typed api boundaries": "Typed API boundaries pair static contracts with parsing and validation of external values.",
  "standard library": "The standard library supplies maintained core tools; verify version and behavior before adding a dependency for the same need.",
  "json": "JSON serialization maps supported values to a text interchange format; parsing does not validate application-specific structure.",
  "csv": "CSV parsing handles dialect, quoting, and newline rules; splitting each line on commas is not a complete parser.",
  "zones": "Time zones map instants to local civil times with offset changes; naive timestamps cannot express all ambiguity.",
  "resources": "Resources such as files, sockets, and locks need explicit lifetime ownership rather than relying on finalization timing.",
  "regular expressions": "Regular expressions match text patterns; poorly chosen patterns or unbounded inputs can cause excessive processing.",
  "os boundaries": "OS boundaries expose paths, environment, permissions, subprocesses, and platform-dependent behavior requiring explicit validation.",
  "awaitables": "Awaitables support awaiting eventual completion; creating a coroutine does not by itself schedule it to run.",
  "cooperative scheduling": "Cooperative scheduling switches tasks at suspension points, so blocking or CPU-heavy work can stall unrelated async tasks.",
  "the gil": "The GIL in conventional CPython builds restricts simultaneous Python execution; free-threaded builds have different constraints. Neither makes compound application operations automatically safe.",
  "conditions": "Condition variables coordinate waiting for a predicate under a lock; recheck the predicate after waking.",
  "thread safety": "Thread safety preserves invariants under concurrent calls and depends on synchronization and ownership, not only interpreter behavior.",
  "pickling": "Pickle serializes Python-specific objects and can execute code during loading. Never treat untrusted pickle data as safe input.",
  "concurrency selection": "Choose async tasks, threads, or processes from workload, isolation, cancellation, and serialization costs rather than a universal preference.",
  "async": "Async Python structures cooperative concurrency through coroutines and await; it does not automatically parallelize CPU computation.",
  "executors": "Executors submit work to threads or processes behind futures, with queueing, shutdown, and result-transfer costs.",
  "distributed workers": "Distributed workers execute tasks across processes or hosts and require explicit retry, duplicate, and version contracts.",
  "finalization": "Finalization occurs when objects are cleaned up by runtime mechanisms; timing is not a substitute for deterministic resource release.",
  "object graphs": "Object graphs describe references connecting objects, explaining reachability, sharing, cycles, and retained memory.",
  "unittest": "unittest provides standard-library assertions, test discovery, fixtures, and test-case organization.",
  "pytest concepts": "pytest organizes tests with assertions, fixtures, and parametrization; fixture scope and cleanup affect isolation.",
  "determinism": "Determinism makes results repeatable under controlled inputs, clocks, randomness, ordering, and external dependencies.",
  "tracebacks": "Tracebacks show exception propagation through execution frames; preserve them when translating failures.",
  "pdb": "pdb supports interactive inspection and stepping through Python execution.",
  "warnings": "Warnings report noteworthy conditions without necessarily failing execution; tests and policy can promote selected warnings to errors.",
  "build backends": "Build backends create Python distribution artifacts under standardized build interfaces and declared build dependencies.",
  "publishing": "Publishing distributes versioned artifacts whose metadata, contents, compatibility, and provenance must be verified.",
  "c api": "The Python C API integrates native code with Python objects and runtime rules, including reference and interpreter ownership.",
  "buffer protocol": "The buffer protocol exposes binary memory to compatible consumers without requiring a copy, with explicit lifetime and layout constraints.",
  "performance boundaries": "Performance boundaries identify where conversion, copying, native calls, scheduling, or I/O dominates execution cost.",
  "domain boundaries": "Domain boundaries separate business invariants from framework, storage, and external representation details.",
  "deserialization": "Deserialization reconstructs values from external representations and may be unsafe or insufficiently validated depending on the format.",
  "hardening": "Hardening reduces attack surface through least privilege, safe defaults, dependency control, validation, and resource limits."
});

Object.assign(TERM_CORRECTIONS.nodejs ||= {}, {
  "operating system": "The OS schedules threads and provides networking, files, memory, and process services used by Node.",
  "host apis": "Host APIs are Node-provided capabilities beyond ECMAScript, such as files, sockets, and processes.",
  "pending callbacks": "The pending-callback phase runs selected callbacks deferred from earlier event-loop work.",
  "close callbacks": "Close callbacks report completion of supported handle shutdown paths; closing a resource and receiving its close notification are distinct.",
  "iteration": "An event-loop iteration progresses through phases; ready work and runtime version affect the observed order.",
  "file system": "Node filesystem APIs delegate file operations to OS facilities, often through asynchronous worker-pool work.",
  "crypto": "Node crypto exposes cryptographic operations with algorithm, key, randomness, and execution-cost requirements.",
  "callback apis": "Callback APIs report completion through a supplied function, commonly with an error-first convention.",
  "promises": "Promises represent eventual completion but do not automatically cancel work or prevent unhandled rejection.",
  "async functions": "Async functions return Promises; synchronous work before suspension still runs on the current JavaScript thread.",
  "synchronous dispatch": "EventEmitter normally invokes listeners synchronously during emit; an async listener's Promise is not automatically awaited.",
  "once": "A once listener is removed after its first invocation; Promise-based event waiting needs error and cancellation consideration.",
  "copies": "Copies allocate independent storage, while views may share backing memory and observe each other's mutations.",
  "binary safety": "Binary safety preserves exact bytes, bounds, and ownership without accidental text conversion or exposure of uninitialized data.",
  "typedarray": "A TypedArray views binary storage as fixed-width numeric elements with alignment and offset rules.",
  "dataview": "DataView reads or writes binary values with explicit offsets and endianness over an ArrayBuffer.",
  "states": "Stream state tracks readable, writable, ended, destroyed, and error conditions; not every closed-looking state means successful completion.",
  "consumption": "Stream consumption pulls or receives chunks; mixing consumption styles can produce confusing ownership and flow behavior.",
  "write return values": "A false return from writable.write signals backpressure: stop writing until drain rather than treating false as write failure.",
  "node streams": "Node streams implement incremental I/O with buffering, backpressure, errors, and destruction protocols.",
  "adapters": "Stream adapters bridge Node and Web Stream protocols, with possible differences in buffering and cancellation behavior.",
  "compression": "Compression reduces bytes using CPU and buffering; limit decompressed output and handle partial-input failures.",
  "interoperability": "Interoperability aligns contracts across APIs or module systems rather than assuming similar names imply identical behavior.",
  "promises api": "Promise-based Node APIs expose completion through returned Promises while retaining the underlying resource and error obligations.",
  "paths": "Paths identify filesystem locations using platform rules; normalization alone does not enforce a trusted-directory boundary.",
  "import.meta": "import.meta supplies module-related metadata under the host's ESM implementation; supported properties depend on runtime version.",
  "cross-platform behavior": "Cross-platform behavior accounts for path syntax, case sensitivity, signals, permissions, and process differences.",
  "traversal": "Path traversal escapes an intended directory through crafted paths or filesystem indirection such as symlinks.",
  "filesystem boundaries": "Filesystem boundaries restrict which resolved objects a process may access; string-prefix checks alone can be insufficient.",
  "net server": "A net server accepts TCP or IPC connections as byte streams, not pre-framed application messages.",
  "connections": "Connections own transport state and resources and require timeout, closure, and error handling.",
  "address selection": "Address selection chooses among resolved endpoints and families, affecting connection latency and failure behavior.",
  "thread pool": "The libuv thread pool executes selected blocking or expensive operations; its saturation can delay otherwise unrelated APIs.",
  "connection behavior": "Connection behavior includes establishment, reuse, half-close, timeout, reset, and resource cleanup.",
  "trust stores": "Trust stores contain certificate authorities used in peer verification; changing trust affects which peers may authenticate.",
  "sessions": "TLS sessions can reuse negotiated security state to reduce setup cost; they are distinct from application login sessions.",
  "verification": "TLS verification checks certificate trust and peer identity under configured rules; encryption without verification does not establish the intended peer.",
  "secure contexts": "Secure contexts hold TLS configuration such as certificates, keys, and trust settings for connections.",
  "parser": "An HTTP parser recognizes protocol framing and fields; application body validation remains a separate layer.",
  "request": "An incoming request exposes metadata and a consumable body stream whose size, timing, and validity must be controlled.",
  "response": "A response writes status, headers, and body while respecting backpressure and client disconnects.",
  "connection lifecycle": "Connection lifecycle covers creation, use, idle reuse, shutdown, and cleanup after errors or peer closure.",
  "connection pools": "Connection pools reuse bounded transport connections; limits, queueing, and idle policies affect latency and resource use.",
  "dispatchers": "Undici dispatchers control outbound request execution, pooling, and related connection policy.",
  "web apis": "Node implements selected Web APIs, but API presence and semantics still depend on the installed Node version.",
  "multiplexing": "Multiplexing carries multiple logical streams over one connection, introducing shared connection-level limits and failure effects.",
  "resolution": "Module resolution maps a specifier to a module under host, package, and condition rules.",
  "loading": "Module loading reads, links, and evaluates resolved modules according to their format and dependencies.",
  "top-level await": "Top-level await can suspend ESM evaluation and dependent initialization; cycles and startup failure require care.",
  "customization hooks": "Module customization hooks alter supported resolution or loading stages and need version-specific behavior and security review.",
  "wrapper function": "CommonJS modules execute within a wrapper supplying module, exports, require, and location-related values.",
  "require resolution": "require resolution follows CommonJS-compatible lookup rules and package metadata before loading a module.",
  "exports": "Package exports define permitted package entry points and conditional mappings; CommonJS exports instead exposes runtime module values.",
  "cycles": "Module cycles create initialization dependencies and may expose partially initialized or unavailable bindings.",
  "initialization": "Initialization executes module setup once under the relevant module cache and can fail or depend on evaluation order.",
  "esm": "ESM uses import/export linking and live bindings, with loading rules distinct from CommonJS.",
  "package type": "The package type field influences how Node interprets relevant JavaScript files; explicit extensions can override that default.",
  "imports": "Package imports provide internal specifier mappings; they differ from the import syntax used to reference modules.",
  "conditions": "Export or import conditions select mappings for environments such as import, require, or custom supported conditions.",
  "erasable syntax": "Erasable TypeScript syntax can be removed without transforming runtime behavior; other constructs need transformation or are unsupported by simple stripping.",
  "tsconfig limits": "Native TypeScript execution does not imply support for every tsconfig feature; type checking and path mapping may require separate tooling.",
  "module formats": "Module formats define loading and export behavior, chiefly ESM and CommonJS in Node.",
  "distribution": "Distribution packages runnable artifacts, dependencies, metadata, and supported runtime requirements for consumers.",
  "npm": "npm manages package installation and scripts using manifests and lockfiles; scripts and dependencies are executable supply-chain inputs.",
  "supply-chain risk": "Supply-chain risk comes from trusting dependencies, lifecycle scripts, registries, maintainers, and build artifacts.",
  "env files": "Environment files provide configuration text; parsing rules and precedence depend on the loader and runtime.",
  "command-line arguments": "Command-line arguments are external strings that need explicit parsing, validation, and safe subprocess forwarding.",
  "startup validation": "Startup validation rejects invalid configuration before opening listeners or accepting work.",
  "system errors": "System errors report OS-level failures through codes and context; decide recovery from the operation and error code.",
  "causes": "Error causes preserve the underlying failure when adding higher-level context.",
  "codes": "Error codes provide machine-readable classification that is more stable than parsing human-readable messages.",
  "warnings": "Warnings surface deprecated or suspicious behavior without necessarily terminating execution; production policy determines response.",
  "crash policy": "Crash policy defines when a process should terminate rather than continue in an unsafe state and how recovery is supervised.",
  "supervisors": "Supervisors manage process lifecycle, restarts, backoff, and exit observation outside the application process.",
  "stdio": "Standard input, output, and error are process streams with platform- and destination-dependent behavior.",
  "shell injection": "Shell injection occurs when untrusted text is interpreted as shell syntax; prefer direct executable arguments without a shell.",
  "message ports": "Message ports exchange messages between workers or contexts using cloning or transfer rules.",
  "atomics": "Atomics coordinate supported shared-memory operations with explicit ordering semantics; compound invariants may need additional synchronization.",
  "load balancing": "Load balancing distributes work across capacity while accounting for health, locality, and persistent connections.",
  "shared ports": "Multiple workers can receive connections associated with a shared listening endpoint under supported clustering or OS mechanisms.",
  "mocking": "Mocking replaces collaborators to control tests; it cannot establish the real dependency's integration behavior.",
  "coverage": "Coverage records executed code regions, not correctness of assertions or completeness of failure cases.",
  "node security": "Node security combines least privilege, input boundaries, safe subprocess and filesystem use, dependency controls, and resource limits.",
  "marks": "Performance marks label points in a timeline for later measurement.",
  "measures": "Performance measures calculate durations between defined marks or times.",
  "capacity evidence": "Capacity evidence ties workload and limits to measured throughput, latency, errors, and saturation.",
  "inspector": "The inspector exposes debugging and profiling capabilities; exposing it to untrusted networks can compromise the process.",
  "trace events": "Trace events record timestamped runtime activity for correlation and performance diagnosis.",
  "flame graphs": "Flame graphs aggregate sampled stack activity to reveal where execution time or another sampled cost concentrates.",
  "incident evidence": "Incident evidence preserves relevant versions, configuration, logs, traces, profiles, and workload context for diagnosis.",
  "abi stability": "ABI stability concerns binary compatibility across runtime versions, distinct from source-level API compatibility.",
  "ffi": "A foreign-function interface calls native code with explicit data layout, lifetime, and failure-boundary obligations.",
  "failure boundaries": "Failure boundaries determine whether a fault affects one operation, worker, process, or the entire service.",
  "dependency direction": "Dependency direction keeps application decisions from unnecessarily depending on transport or vendor details.",
  "resilience": "Resilience preserves or restores useful behavior under failures using bounded work, isolation, recovery, and tested policies.",
  "streaming": "Streaming processes incremental data rather than buffering the whole result, while preserving backpressure and cleanup.",
  "workers": "Worker threads run separate JavaScript execution contexts and exchange or share data under explicit communication rules."
});

Object.assign(TERM_CORRECTIONS["javascript"] ||= {}, {
  "well-known symbols": "Well-known symbols are standardized shared symbol values, such as Symbol.iterator, that select language protocols. They are not fresh symbols created from matching descriptions.",
  "unicode modes": "The u and v regular-expression flags enable Unicode-aware pattern semantics; v additionally supports Unicode set notation and properties of strings. They cannot be combined and neither makes matching equivalent to grapheme segmentation. Check target-runtime support.",
  "ownership": "For object properties here, ownership means a property is directly on an object rather than inherited. Object.hasOwn tests that distinction; it does not establish resource ownership or access control."
});

Object.assign(TERM_CORRECTIONS["react"] ||= {}, {
  "events": "React event handlers respond to browser interactions through React's event system. Capture, bubbling, default behavior and the component's current render snapshot determine what each handler observes.",
  "routing": "Frontend routing maps a URL and navigation intent to the active UI and data-loading boundaries. A router or framework supplies matching, history and lifecycle policy; React alone does not define them."
});

Object.assign(TERM_CORRECTIONS["lld-machine-coding"] ||= {}, {
  "composition": "In UML, composite aggregation models whole-part ownership: a part belongs to at most one composite at a time, and deletion of the whole deletes its remaining object parts. Merely storing references in a Python list does not enforce that lifecycle contract.",
  "uml class diagrams": "UML class diagrams show classes and relationships; use them to clarify ownership and cardinality rather than decorating obvious code.",
  "parking lot machine-coding case study": "The parking-lot exercise models admission, allocation, tickets, and charging while preserving capacity and ownership under contention.",
  "splitwise expense-sharing case study": "The expense-sharing exercise records exact obligations and settlements while preserving total balances and currency rules.",
  "timed machine-coding capstone": "The timed capstone prioritizes a working core, executable acceptance criteria, and a clear explanation of deliberate omissions.",
  "rate limiter": "A rate limiter bounds admitted work over time using a defined identity, algorithm, burst allowance, and concurrency policy."
});

Object.assign(TERM_CORRECTIONS["data-systems"] ||= {}, {
  "backend processes": "PostgreSQL backend processes execute client-session work and coordinate shared database resources.",
  "background workers": "Background workers perform server-side tasks outside ordinary foreground client-query execution.",
  "relations": "PostgreSQL relations include tables, indexes, and other relation-like objects represented in system catalogs.",
  "forks": "Relation forks separate storage purposes, such as main data, free-space information, and visibility information.",
  "physical": "Physical storage describes files, pages, and records used to represent database state on disk.",
  "logical storage": "Logical storage describes tables, rows, and schemas independently of their physical placement.",
  "postgresql data types": "PostgreSQL data types define values, operators, conversion, and storage behavior; choose them to express domain constraints.",
  "domains": "A PostgreSQL domain is a named type based on another type with additional constraints.",
  "enums": "PostgreSQL enums define ordered named values; evolving them requires compatibility planning.",
  "arrays": "PostgreSQL arrays store multiple typed elements in one value; they do not automatically replace normalized relationships.",
  "ranges": "Range types represent bounded intervals with explicit inclusive, exclusive, empty, and unbounded behavior.",
  "generated columns": "Generated columns derive values from expressions under supported storage and dependency rules.",
  "relational modeling": "Relational modeling organizes facts into relations with keys and constraints that preserve meaning under updates.",
  "relationships": "Database relationships connect entities through keys and cardinality constraints rather than duplicated informal identifiers.",
  "keys": "Keys identify rows or connect them to other rows; uniqueness and nullability determine their guarantees.",
  "functional dependencies": "A functional dependency means one set of attributes determines another, guiding normalization and anomaly prevention.",
  "primary keys": "A primary key uniquely identifies each row and disallows null key values.",
  "foreign keys": "Foreign keys enforce references to qualifying keys in another or the same table, with explicit update/delete behavior.",
  "uniqueness": "Uniqueness prevents duplicate key values under the constraint's comparison and null-handling rules.",
  "check": "CHECK constraints reject rows when their expression is false; null results are not false, so nullability often needs a separate constraint.",
  "exclusion": "Exclusion constraints prevent pairs of rows from satisfying specified operator comparisons, useful for conflicting time ranges.",
  "deferred validation": "Deferred constraints can postpone eligible checks until transaction completion, permitting temporary intermediate states.",
  "transactional ddl": "Many PostgreSQL schema changes participate in transactions, but command-specific exceptions and locking behavior still matter.",
  "zero-downtime change": "Zero-downtime schema change uses compatible staged changes and bounded locks rather than assuming DDL is operationally free.",
  "select": "SELECT specifies output expressions; it does not imply a particular physical execution order.",
  "from": "FROM supplies source relations and joins for a query.",
  "where": "WHERE filters individual rows before grouping under the query's logical semantics.",
  "group by": "GROUP BY combines rows into groups for aggregate computation.",
  "having": "HAVING filters groups after grouping, unlike WHERE's row filtering.",
  "order by": "ORDER BY requests output ordering; without it, apparent row order is not a contract.",
  "limit": "LIMIT bounds returned rows; deterministic selection also requires a suitable ordering.",
  "inner": "An inner join returns matching row combinations according to its predicate.",
  "outer": "An outer join also preserves unmatched rows from specified sides with null-extended columns.",
  "cross": "A cross join forms all row combinations, so result size multiplies.",
  "semi": "A semi-join returns qualifying left rows when a match exists, without duplicating them for every right match.",
  "anti": "An anti-join returns left rows lacking a qualifying match; null semantics can make NOT IN differ from NOT EXISTS.",
  "join predicates": "Join predicates define which row combinations match; moving filters can change outer-join meaning.",
  "subqueries": "Subqueries embed query results or existence checks inside another query; the planner may transform their execution.",
  "correlated execution": "A correlated subquery refers to outer-row values; actual repeated work depends on the resulting plan.",
  "recursive queries": "Recursive queries repeatedly expand a working result from an initial seed, requiring termination and cycle considerations.",
  "materialization": "Materialization stores an intermediate result for reuse, trading storage and optimization freedom against repeated computation.",
  "aggregates": "Aggregates summarize groups of rows, with function-specific null and ordering behavior.",
  "frames": "Window frames select the rows contributing to a window calculation relative to the current row.",
  "grouping sets": "Grouping sets request multiple grouping combinations in one query.",
  "rollup": "ROLLUP produces hierarchical subtotal grouping sets plus a grand total.",
  "ordered-set analysis": "Ordered-set aggregates compute results such as percentiles over explicitly ordered input.",
  "autocommit": "Autocommit runs statements in separate transactions unless an explicit transaction groups them.",
  "begin": "BEGIN starts an explicit transaction whose subsequent operations share a transaction boundary.",
  "savepoints": "Savepoints permit rollback of part of a transaction without abandoning all prior work.",
  "commit": "COMMIT requests successful transaction completion; durability depends on database configuration and covered failure assumptions.",
  "transaction ids": "Transaction IDs participate in visibility and maintenance; their finite representation requires wraparound management.",
  "tuple versions": "MVCC stores row versions so transactions can observe data according to snapshot visibility.",
  "visibility": "Visibility determines which row versions a transaction can observe under its snapshot and isolation rules.",
  "anomalies": "Transaction anomalies are surprising outcomes permitted by an isolation level, such as stale reads or conflicting concurrent decisions.",
  "ssi": "Serializable Snapshot Isolation detects dangerous dependency patterns and may abort transactions to preserve serializable behavior.",
  "wait queues": "Wait queues hold operations blocked on unavailable resources such as locks.",
  "nowait": "NOWAIT requests immediate failure rather than waiting for certain unavailable locks.",
  "serialization failures": "Serialization failures require retrying the complete transaction with fresh reads, not only the final statement.",
  "retry budgets": "Retry budgets bound total repeated work and time so contention does not become a retry storm.",
  "splits": "Index-page splits redistribute entries when a page cannot accommodate an insertion, adding write and maintenance cost.",
  "hash": "PostgreSQL hash indexes support equality lookup, not ordered range traversal.",
  "gist": "GiST is an extensible index framework supporting operator classes such as geometric and range searches.",
  "sp-gist": "SP-GiST supports partitioned search structures suited to selected nonbalanced data decompositions.",
  "gin": "GIN is an inverted index useful when a value contains multiple searchable components, such as array elements or document terms.",
  "brin": "BRIN summarizes block ranges compactly and works best when values correlate with physical placement.",
  "extension indexes": "Extension indexes add specialized access methods or operator classes; their guarantees and maintenance costs depend on the extension.",
  "composite": "Composite indexes cover multiple columns; column order and query predicates influence useful access paths.",
  "partial": "A partial index includes only rows matching a predicate, reducing size when queries can use that predicate.",
  "expression": "An expression index stores computed keys, improving matching expressions at additional write and maintenance cost.",
  "unique indexes": "Unique indexes enforce uniqueness under their key and null-comparison rules while supporting lookup.",
  "index design tradeoffs": "Index design balances read access, write amplification, storage, maintenance, and actual query selectivity.",
  "cardinality estimation": "Cardinality estimation predicts row counts that drive plan cost comparisons.",
  "selectivity": "Selectivity estimates the fraction of rows satisfying a predicate.",
  "histograms": "Histograms summarize value distributions for selectivity estimates without retaining every value.",
  "mcvs": "Most-common-value statistics record frequent values and frequencies that uniform estimates would misrepresent.",
  "correlation": "Planner correlation statistics describe how column order relates to physical row order, affecting access-cost estimates.",
  "extended statistics": "Extended statistics capture selected relationships among columns that independent estimates can miss.",
  "buffers": "EXPLAIN BUFFERS reports buffer activity; distinguish shared-buffer hits from physical storage reads and timing.",
  "settings": "EXPLAIN SETTINGS exposes relevant nondefault planning settings for interpreting a plan.",
  "timing": "Plan timing measures execution components with instrumentation overhead; compare equivalent workloads and cache states.",
  "plan interpretation": "Plan interpretation compares estimated and actual rows, loops, access paths, memory, and I/O to locate dominant work.",
  "bitmap scans": "Bitmap scans gather matching tuple locations before visiting heap pages, trading startup work for grouped page access.",
  "nested loops": "Nested-loop joins repeatedly access an inner input for outer rows; they can be efficient with selective indexed lookups.",
  "sorts": "Sorts order rows using memory and possibly temporary disk storage when memory is insufficient.",
  "spills": "Spills move work such as sorting or hashing to temporary storage when its memory allowance is exceeded.",
  "free space map": "The free space map tracks available page space to help place new tuples.",
  "visibility map": "The visibility map tracks page-level visibility facts used by maintenance and index-only scans.",
  "fillfactor": "Fillfactor reserves page space for future updates or insertions, trading initial density against later movement and splits.",
  "bloat": "Bloat is excess relation storage caused by no-longer-useful or poorly packed space; distinguish reusable space from space returned to the OS.",
  "autovacuum": "Autovacuum schedules vacuum and analyze work to manage dead versions, statistics, and transaction-age safety.",
  "freezing": "Freezing marks sufficiently old tuple transaction information so it remains safe across transaction-ID wraparound.",
  "transaction id wraparound": "Transaction-ID wraparound can make old visibility information ambiguous unless vacuum freezing advances safely.",
  "maintenance tuning": "Maintenance tuning adjusts vacuum, analyze, and related work based on churn, age, locking, and measured resource pressure.",
  "lsns": "Log sequence numbers identify positions in PostgreSQL's write-ahead log and help compare replication or recovery progress.",
  "commit durability": "Commit durability states which acknowledged commits survive a failure under WAL flush and replication settings.",
  "checkpoints": "Checkpoints establish recovery progress by coordinating dirty-page persistence and WAL recovery boundaries.",
  "full-page writes": "Full-page writes log page images as required to protect recovery from torn-page writes after checkpoints.",
  "crash recovery": "Crash recovery replays necessary WAL from a recovery point to restore a consistent database state.",
  "sessions": "Database sessions retain connection-level state such as settings and temporary objects, which pooling can complicate.",
  "prepared statements": "Prepared statements reuse parsed or planned statement state subject to server, driver, and pool behavior.",
  "pgbouncer": "PgBouncer pools PostgreSQL connections; its pooling mode changes which session assumptions remain valid.",
  "pool sizing": "Pool sizing bounds concurrent database work against capacity rather than equating more connections with more throughput.",
  "transaction pooling": "Transaction pooling assigns a server connection for a transaction, so callers cannot assume stable server-session ownership between transactions.",
  "pruning": "Partition pruning avoids scanning partitions proven irrelevant to query constraints.",
  "maintenance": "Database maintenance manages statistics, dead versions, storage, backups, and safety without ignoring workload impact.",
  "partition-wise execution": "Partition-wise execution can process eligible joins or aggregates by partition when structure and planner settings permit.",
  "wal senders": "WAL sender processes stream log information to replication or backup consumers.",
  "receivers": "WAL receivers accept streamed log data on standby servers.",
  "slots": "Replication slots retain needed progress and log data for consumers; stalled consumers can cause storage growth.",
  "lag": "Replication lag is delay in sending, writing, flushing, or replaying changes; name which stage is measured.",
  "synchronous replication": "Synchronous replication waits for configured standby acknowledgement stages, trading latency and availability for stronger commit guarantees.",
  "publications": "Publications describe which eligible database changes are made available through logical replication.",
  "subscriptions": "Subscriptions consume publication changes and apply them to a target database.",
  "decoding": "Logical decoding translates WAL changes into a logical change stream under a plugin and slot contract.",
  "replication identity": "Replica identity supplies the row-identifying information needed for supported logical updates and deletes.",
  "cdc": "Change data capture exposes committed data changes for downstream processing, with explicit position and schema-evolution handling.",
  "pg_dump": "pg_dump creates a logical database backup; a successful dump is not proof that the restore meets recovery objectives.",
  "pg_restore": "pg_restore loads supported archive-format dumps with options for selecting and ordering restored objects.",
  "wal archiving": "WAL archiving retains completed log segments for recovery beyond a base backup.",
  "pitr": "Point-in-time recovery restores a base backup and replays archived WAL to a selected recovery target.",
  "pg_hba.conf": "pg_hba.conf controls matching client-authentication rules; rule order and connection attributes determine the applied method.",
  "default privileges": "Default privileges govern permissions assigned to future objects created by relevant roles, not retroactively all existing objects.",
  "postgresql observability": "PostgreSQL observability combines activity, locks, waits, plans, logs, and resource measurements to explain database behavior.",
  "pg_stat_activity": "pg_stat_activity exposes backend activity and wait context, useful for identifying long-running or idle-in-transaction sessions.",
  "pg_locks": "pg_locks exposes held or awaited lock information for contention analysis.",
  "progress views": "Progress views report supported long-running operations' stages and counters rather than assuming elapsed time predicts completion.",
  "wait events": "Wait events identify what a backend is waiting on, distinguishing lock, I/O, client, and other waits.",
  "postgresql performance tuning": "PostgreSQL tuning starts from workload and plan evidence, then changes queries, indexes, concurrency, or settings with measurements.",
  "work_mem": "work_mem influences memory available to individual query operations, so concurrent operations can multiply total usage.",
  "cache": "Database caching reuses pages or results at different layers; warm-cache behavior can hide storage costs.",
  "full-text search": "Full-text search indexes normalized document terms for linguistic matching and ranking rather than raw substring search alone.",
  "extensions": "Extensions add database capabilities with their own versions, privileges, upgrade, and operational requirements.",
  "vector indexes": "Vector indexes accelerate similarity search with access-method-specific recall, filtering, memory, and update trade-offs.",
  "hybrid retrieval": "Hybrid retrieval combines lexical and semantic candidates with a defined fusion and evaluation policy.",
  "application data patterns": "Application data patterns coordinate persistence with retries, events, reads, and domain invariants.",
  "inbox": "An inbox records consumed message identities transactionally with effects to support duplicate-safe processing.",
  "pagination": "Pagination divides results into bounded pages; stable ordering and concurrent changes determine whether rows can be skipped or repeated.",
  "read consistency": "Read consistency defines which committed state a read may observe, especially through replicas and caches.",
  "event loop": "Redis's event loop coordinates connections and ready work; long command execution can delay other clients.",
  "command execution": "Redis command execution applies each command's semantics and resource costs; atomicity does not mean cheap or durable.",
  "i/o threads": "Redis I/O threads can assist supported networking work; they do not imply all commands execute concurrently.",
  "keyspace": "The keyspace maps Redis keys to typed values and associated metadata such as expiration.",
  "expiration cycle": "Redis removes expired keys through passive checks and active expiration work rather than an exact per-key deletion timer.",
  "clients": "Redis clients own connections, protocol state, and buffers whose size and lifetime affect server resources.",
  "protocol framing": "Protocol framing identifies command and response boundaries in a byte stream.",
  "output buffers": "Output buffers retain replies awaiting clients; slow consumers can create memory pressure.",
  "counters": "Redis counters use atomic numeric updates, subject to value format and numeric range rules.",
  "hashes": "Redis hashes store field-value pairs under one key, with encoding and memory costs depending on content and size.",
  "object encodings": "Redis object encodings choose internal representations for values; representation changes can alter memory and operation costs.",
  "memory usage": "Memory usage includes stored values, metadata, fragmentation, buffers, and persistence-related overhead.",
  "atomic updates": "Atomic updates prevent interleaving within a defined operation; they do not automatically coordinate external database effects.",
  "redis lists": "Redis lists support ordered pushes and pops and selected blocking operations, but are not a full durable workflow protocol.",
  "sets": "Redis sets store unique members and support membership and set algebra without a sorted-order contract.",
  "blocking operations": "Blocking Redis operations wait for a condition such as available list data while the server continues other client work.",
  "indexes": "An index stores an alternative access structure to reduce selected query work at storage and update cost.",
  "leaderboard patterns": "Leaderboards commonly use sorted sets with explicit score ties, ranking, update, and retention policies.",
  "redis bitmaps": "Redis bitmap operations treat string storage as bits, enabling compact flags and counts.",
  "bitfields": "Bitfields read or update fixed-width integers packed into bit ranges with explicit overflow behavior.",
  "geospatial indexes": "Redis geospatial structures index coordinates for supported proximity queries with representation-specific limits.",
  "probabilistic tradeoffs": "Probabilistic structures reduce memory by accepting defined estimation error or false-positive behavior.",
  "compact analytics": "Compact analytics uses packed or approximate representations when exact per-item storage is unnecessary.",
  "redis ttl": "TTL associates expiration with a Redis key; expiration is distinct from eviction under memory pressure.",
  "expiration": "Expiration removes keys after their time policy makes them invalid; physical cleanup timing is implementation-dependent.",
  "maxmemory": "maxmemory sets a Redis memory threshold used with an eviction or rejection policy; total process memory can include additional overhead.",
  "lfu": "LFU eviction approximates least-frequently-used selection with frequency tracking and aging.",
  "lru sampling": "Redis approximates LRU by sampling candidates rather than maintaining a perfect global recency order.",
  "memory pressure": "Memory pressure occurs when active data and overhead approach available memory or configured limits.",
  "redis atomic commands": "An atomic Redis command applies without interleaved command effects, but may still fail or lack the desired persistence guarantee.",
  "optimistic locking": "Optimistic locking detects changes before committing conditional work; Redis WATCH requires retries when watched keys change.",
  "server-side functions": "Server-side functions execute registered logic near data under Redis execution rules and must avoid unbounded server blocking.",
  "batching": "Batching groups operations to reduce overhead; pipelining is not the same as transactional atomicity.",
  "round trips": "A round trip includes request delivery and response return; reducing them can improve latency even without faster commands.",
  "redis rdb": "RDB persistence writes point-in-time snapshots, with potential loss of changes since the last recoverable snapshot.",
  "fsync": "fsync requests persistence through the OS storage stack; policy and storage guarantees affect acknowledged-data durability.",
  "rewrites": "AOF rewrites create a compact persistence representation while coordinating new changes and resource use.",
  "fork": "Redis persistence may fork a child process, introducing page-table and copy-on-write memory costs.",
  "copy-on-write": "Copy-on-write initially shares pages and copies them when modified, so concurrent writes can increase memory during persistence.",
  "restart": "Restart reconstructs Redis state from configured persistence; test the actual recoverable data rather than assuming all acknowledged writes survive.",
  "durability tradeoffs": "Durability trade-offs balance flush frequency, latency, throughput, and acceptable loss under specific failures.",
  "replication ids": "Replication IDs identify a replication history used with offsets to determine synchronization compatibility.",
  "offsets": "Replication offsets identify progress within a replication stream, not wall-clock time.",
  "psync": "PSYNC requests partial replication resynchronization when retained history and replication identity permit it.",
  "backlog": "The replication backlog retains recent stream data so reconnecting replicas may avoid full synchronization.",
  "hash slots": "Redis Cluster assigns keys to hash slots distributed among nodes.",
  "hash tags": "Hash tags choose a shared substring for slot calculation, allowing related keys to occupy the same slot.",
  "moved": "MOVED redirects a client to the node currently responsible for a slot and generally prompts routing-table refresh.",
  "ask": "ASK provides a temporary redirection during slot migration and requires the corresponding client protocol handling.",
  "resharding": "Resharding moves key ownership between nodes while clients and migration state coordinate access.",
  "gossip": "Gossip spreads membership and state information among peers incrementally rather than through one global instantaneous view.",
  "quorum": "A quorum is a required set or count of participants for a decision under a specified protocol; counting votes alone does not prove safety.",
  "entries": "Stream entries are ordered records identified within a Redis stream, with field-value payloads.",
  "consumer groups": "Consumer groups coordinate delivery among consumers while tracking pending work separately from stream storage.",
  "pending entries": "Pending entries have been delivered to a group consumer but not acknowledged under that group's tracking.",
  "acknowledgements": "A stream acknowledgement clears group pending status; it does not itself atomically commit an external side effect.",
  "claiming": "Claiming transfers eligible pending work to another consumer for recovery, potentially repeating prior side effects.",
  "trimming": "Trimming removes stream history under a retention policy; consider pending consumers and replay requirements.",
  "write-through": "Write-through caching updates the cache as part of the write path under a defined failure and ordering policy.",
  "stampedes": "Cache stampedes occur when many misses trigger duplicate expensive work; coordination and expiry spreading can reduce them.",
  "stale data": "Stale data reflects an earlier state; define which staleness is acceptable and how clients detect or recover from it.",
  "negative caching": "Negative caching stores absence or failure results briefly to avoid repeated work, with careful invalidation and error classification.",
  "delayed work": "Delayed work becomes eligible at a future time; clocks, retries, ownership, and persistence determine actual execution guarantees.",
  "correctness boundaries": "Correctness boundaries identify which component atomically enforces an invariant and where retries or partial failure can escape it.",
  "redis security": "Redis security requires network restriction, authentication, least-privilege commands, transport protection, and safe operational access.",
  "acls": "Redis ACLs restrict authenticated users by supported command and key/channel permissions.",
  "protected mode": "Protected mode restricts unsafe default exposure in supported configurations; it is not a replacement for network and identity controls.",
  "command controls": "Command controls restrict dangerous or unnecessary operations according to application and operator roles.",
  "memory diagnostics": "Memory diagnostics distinguish dataset size, encodings, fragmentation, buffers, and persistence overhead before changing eviction policy.",
  "postgresql": "PostgreSQL is a relational database with transactional constraints and extensibility; model its actual isolation and durability contract.",
  "redis": "Redis provides in-memory data structures with configurable persistence and replication; treat cache, coordination, and durable-state uses differently."
});

Object.assign(TERM_CORRECTIONS.docker ||= {}, {
  "writable layers": "A container's writable layer records that container's filesystem changes above its image layers. It survives stopping/restarting the same container but disappears when the container is removed; mounts have separate lifetimes.",
  "user namespaces": "User namespaces map user and group IDs inside a container to different IDs outside it. They constrain host authority but do not replace filesystem permissions, capabilities or other isolation controls.",
  "network namespaces": "A network namespace gives a process its own network interfaces, routes, sockets and local addresses. Container localhost belongs to that namespace unless a shared network mode changes the boundary.",
  "minimal images": "Minimal images retain only required runtime content and selected operational support. Omitting a shell changes debugging options; fewer packages alone do not establish security or lower runtime memory.",
  "repositories": "A registry repository is a named collection of image manifests, blobs and references governed by registry access and retention rules. It is unrelated to a domain-model persistence repository.",
  "garbage collection": "Registry garbage collection reclaims stored content no longer retained by the registry's reference and policy rules. Coordinate it with uploads and rollback retention; removing a tag and reclaiming shared blobs are different operations.",
  "multi-platform images": "A multi-platform image index references platform-specific image manifests. The client selects compatible content; one index digest and its selected platform-manifest digest identify different objects.",
  "base images": "A base image supplies a build stage's starting filesystem and configuration through FROM. Its digest, architecture, trust and update policy become inputs to the derived image.",
  "immutability": "Image immutability means a content digest continues to identify the same image bytes/configuration. Running containers still have mutable state unless constrained, and tags can point to new digests.",
  "client": "The Docker client sends requests to a configured engine; the engine may run on another machine.",
  "daemon": "The Docker daemon manages images, containers, networks, and volumes with privileges that require careful access control.",
  "containerd": "containerd manages container lifecycle and related image operations beneath higher-level tooling.",
  "runc": "runc creates and runs containers according to the OCI runtime specification using host-kernel facilities.",
  "object lifecycle": "Docker object lifecycle covers creation, use, stopping, and removal; stopping a container does not remove its volumes.",
  "content digests": "Content digests identify content cryptographically rather than through a movable human-readable tag.",
  "tags": "Tags name image references and can be moved; a tag alone does not guarantee immutable content.",
  "dockerfile from": "FROM selects a base image or starts a build stage, establishing its initial filesystem and metadata.",
  "run": "RUN executes a build step and records resulting filesystem changes, not a command that runs on every container startup.",
  "copy": "COPY adds selected build-context or stage files into an image.",
  "add": "ADD supports additional source-handling behavior beyond COPY; use it only when that behavior is intended.",
  "workdir": "WORKDIR sets the working directory for subsequent applicable build and runtime instructions.",
  "user": "USER selects the identity for subsequent applicable instructions and container execution; permissions must support the chosen identity.",
  "env": "ENV stores environment defaults in image configuration and is not a safe secret-storage mechanism.",
  "arg": "ARG supplies build-time variables with scope rules; values may leak through build metadata or history and are not secret mounts.",
  "expose": "EXPOSE documents intended listening ports; it does not publish them to the host by itself.",
  ".dockerignore": ".dockerignore excludes selected files from the build context, reducing transferred data and accidental inclusion.",
  "cache keys": "Build cache keys depend on instructions and relevant inputs; changing dependencies can invalidate later work.",
  "named stages": "Named stages let later build steps reference an earlier stage explicitly in a multi-stage build.",
  "targets": "A build target chooses which stage to produce, useful for separate development, test, and runtime outputs.",
  "cache mounts": "Cache mounts retain reusable build data without making it part of the final image layer by default.",
  "secret mounts": "Secret mounts expose build secrets temporarily without intentionally baking them into layers; commands must still avoid copying or logging them.",
  "ssh mounts": "SSH mounts let build steps use forwarded authentication facilities without copying private keys into the image.",
  "outputs": "Build outputs select how results are exported, such as an image, local files, or registry content.",
  "exec form": "Exec-form instructions specify an argument array without implicit shell parsing, preserving clearer signal and argument behavior.",
  "shell form": "Shell-form instructions use shell parsing and expansion, which changes quoting, process, and signal behavior.",
  "arguments": "Container arguments combine entrypoint and command configuration under explicit override rules.",
  "environment expansion": "Environment expansion depends on whether a shell or instruction processor performs substitution; exec-form arguments are not automatically shell-expanded.",
  "overrides": "Overrides replace or merge image and orchestration defaults; inspect effective configuration rather than assuming additive behavior.",
  "process reaping": "Process reaping collects exited child status to avoid zombies; PID 1 responsibilities can require an init helper.",
  "init": "An init helper forwards signals and reaps children when the application does not correctly own those responsibilities.",
  "stop timeout": "The stop timeout bounds graceful termination before forcible killing, so application cleanup must finish within it.",
  "application lifecycle": "Application lifecycle includes startup validation, readiness, normal work, signal handling, and bounded cleanup.",
  "container isolation": "Container isolation combines kernel mechanisms and configuration; sharing a kernel differs from a full virtual-machine boundary.",
  "non-root users": "Running as a non-root user reduces default process privilege but does not replace capabilities, filesystem, or host-boundary controls.",
  "no-new-privileges": "No-new-privileges restricts gaining additional privilege through execution mechanisms covered by the kernel setting.",
  "hardening": "Container hardening minimizes privileges, writable paths, attack surface, exposed interfaces, and untrusted dependencies.",
  "docker networking": "Docker networking connects container endpoints through configured drivers, addressing, name resolution, and port-publishing rules.",
  "host": "Host networking shares the host network namespace where supported, reducing network isolation and changing port behavior.",
  "none": "The none network mode avoids ordinary external container networking while retaining the container's local network context.",
  "user-defined networks": "User-defined networks provide explicit connectivity scopes and supported container name resolution.",
  "nat": "Docker can use network address translation to connect isolated addresses with external networks and published ports.",
  "published ports": "Published ports map host listening endpoints to container endpoints; binding address determines exposure.",
  "ipv6": "IPv6 container networking needs compatible daemon, network, routing, and host configuration rather than only an application listener.",
  "overlay filesystems": "Overlay filesystems combine read-only image layers with writable container state, affecting copy-up behavior and I/O cost.",
  "container writable state": "Writable container state belongs to that container's lifecycle and should not be confused with independently managed persistent volumes.",
  "tmpfs": "tmpfs stores temporary filesystem data in memory-backed storage with lifecycle and memory-pressure implications.",
  "backup": "A backup is a recoverable copy with consistency and restore procedures; copying a live volume may not produce an application-consistent backup.",
  "projects": "Compose projects group related services and resources under a project identity.",
  "networks": "Compose networks define service connectivity and naming scopes, not automatic application authorization.",
  "profiles": "Compose profiles selectively enable groups of services for different workflows.",
  "startup ordering": "Startup ordering coordinates dependency startup but does not guarantee ongoing availability or application-level readiness.",
  "restart policies": "Restart policies control process restart after termination; they do not repair invalid data or every unhealthy-but-running application.",
  "dependency failure": "Dependency failure requires application timeouts, retries, and degradation even when startup ordering was correct.",
  "self-healing limits": "Restarting replaces a process, not necessarily the bad configuration, external outage, or persistent corruption that caused failure.",
  "container logs": "Container logs capture configured output streams; retention, sensitive data, and collection reliability still need policy.",
  "stdout": "Standard output carries ordinary process output to the configured runtime logging or stream destination.",
  "stderr": "Standard error separates diagnostic output from ordinary output where tooling preserves that distinction.",
  "logging drivers": "Logging drivers route container output to configured destinations with driver-specific buffering and failure behavior.",
  "docker stats": "docker stats reports live resource metrics useful for correlating workload with CPU, memory, and I/O usage.",
  "container debugging": "Container debugging compares effective configuration, processes, logs, networks, and filesystems against the expected lifecycle.",
  "inspect": "docker inspect exposes object configuration and state; protect outputs that include sensitive environment values.",
  "exec": "docker exec starts an additional process inside an existing running container; it does not rebuild the image.",
  "top": "docker top lists container processes as exposed by the runtime and host.",
  "diff": "docker diff reports filesystem changes in a container's writable layer relative to its image.",
  "digests": "Image digests identify content; manifest-list and platform-specific image digests refer to different objects.",
  "push": "Push uploads image content and references to a registry under authorization and naming rules.",
  "pull": "Pull retrieves registry content for a reference and selected platform, subject to trust and authentication policy.",
  "buildx": "Buildx provides extended BuildKit build workflows such as multi-platform builds and explicit output handling.",
  "builders": "Builders execute builds in a configured environment whose driver, cache, platform, and access affect results.",
  "emulation": "Emulation executes another architecture's instructions, often increasing build or test cost and differing from native performance.",
  "cross-compilation": "Cross-compilation produces binaries for another target without necessarily running target instructions during compilation.",
  "manifests": "Image manifests describe content and configuration; multi-platform indexes select manifests for supported platforms.",
  "platform selection": "Platform selection chooses compatible OS and architecture content rather than assuming one tag is one executable image.",
  "container supply-chain security": "Container supply-chain security verifies base images, dependencies, builders, provenance, and delivered content across the build path.",
  "vulnerabilities": "Vulnerabilities are exploitable weaknesses whose risk depends on reachability, privileges, configuration, and available fixes.",
  "policy": "Image policy states which provenance, vulnerabilities, privileges, and deployment properties are acceptable.",
  "production image optimization": "Image optimization removes unnecessary content while preserving required runtime libraries, diagnostics, and operational behavior.",
  "dependency caching": "Dependency caching reuses installation work when relevant inputs are unchanged, without substituting for pinned dependencies.",
  "distroless": "Distroless images omit many general-purpose tools, reducing content while requiring another debugging approach.",
  "operational tradeoffs": "Operational trade-offs compare image size, startup, security, diagnostics, portability, and recovery under real constraints.",
  "docker production capstone": "The Docker capstone demonstrates a reproducible image, bounded runtime, network/storage design, and evidence from a failure drill.",
  "build": "A build transforms declared inputs into an artifact; reproducibility requires controlling dependencies, tools, and relevant environment.",
  "networking": "Networking connects runtime endpoints through namespaces, interfaces, routes, and port policy.",
  "storage": "Storage choices separate immutable image content, temporary state, and persistent application data with explicit ownership.",
  "incident diagnosis": "Incident diagnosis uses observed configuration, logs, resource metrics, and controlled experiments to distinguish competing causes."
});

Object.assign(TERM_CORRECTIONS.devops ||= {}, {
  "flow": "Flow measures how work moves from request to usable delivery, including waiting and handoffs.",
  "learning": "Learning turns incident and delivery evidence into tested changes rather than repeated blame or intuition.",
  "socio-technical systems": "Socio-technical systems combine software, people, incentives, and organizational boundaries that jointly affect outcomes.",
  "linux processes": "Linux processes own execution and resource contexts observed through process, signal, and filesystem interfaces.",
  "systemd": "systemd manages services and related units with dependency, restart, logging, and resource-control configuration.",
  "services": "Services are managed workloads with defined startup, readiness, shutdown, and recovery behavior.",
  "networking diagnostics": "Networking diagnostics trace name resolution, routing, connection, TLS, and application behavior separately.",
  "shell automation": "Shell automation coordinates commands with explicit quoting, exit handling, environment, and safe target selection.",
  "strict mode": "Shell strict-mode options expose selected errors but have context-dependent behavior; they do not replace deliberate error handling.",
  "quoting": "Quoting controls expansion and word splitting so data is not accidentally reinterpreted as arguments or syntax.",
  "pipes": "Pipes connect process output to another process's input; pipeline status and buffering need explicit handling.",
  "traps": "Shell traps run handlers for supported signals or exit conditions, commonly to clean up owned temporary resources.",
  "portable scripts": "Portable scripts restrict themselves to declared shell and platform features and test those environments.",
  "git workflows": "Git workflows define integration, review, release, and recovery practices around versioned changes.",
  "trunk-based development": "Trunk-based development integrates small changes frequently into a shared mainline, supported by fast checks and release controls.",
  "branches": "Branches name lines of development; long divergence increases integration and review cost.",
  "pull requests": "Pull requests package proposed changes with evidence and discussion before integration.",
  "reviews": "Reviews examine correctness, risk, maintainability, and evidence rather than only formatting or approval ceremony.",
  "rebasing": "Rebasing replays commits on another base and changes commit identities, requiring care on shared history.",
  "triggers": "CI triggers define which events start automation and under which trust context.",
  "runners": "Runners execute automation jobs and require isolation, credential boundaries, and lifecycle management.",
  "jobs": "Jobs group automation steps with a defined execution environment and dependency order.",
  "matrices": "CI matrices run combinations such as runtime versions and platforms, expanding coverage and resource cost.",
  "caches": "CI caches reuse work under keys; untrusted or stale cache content can undermine correctness or security.",
  "automated testing strategy": "A testing strategy assigns checks to risks and feedback stages, balancing speed, realism, and maintenance.",
  "unit": "Unit tests isolate a small behavior with controlled collaborators and fast feedback.",
  "integration": "Integration tests verify actual interactions among components such as an application and database.",
  "contract": "Contract tests verify that providers and consumers agree on supported interactions and representations.",
  "end-to-end": "End-to-end tests exercise complete user or system flows across boundaries, with higher setup and diagnosis costs.",
  "quality gates": "Quality gates prevent promotion when required evidence or policy checks fail; define who can override them and why.",
  "software supply chain": "The software supply chain includes source, dependencies, builders, artifacts, and deployment trust relationships.",
  "dependency trust": "Dependency trust evaluates provenance, maintenance, permissions, and update behavior rather than treating installation success as safety.",
  "slsa concepts": "SLSA describes supply-chain assurance requirements such as build provenance and stronger protection of build processes.",
  "ci identity": "CI identity determines which workflow can obtain credentials and act on deployment or artifact resources.",
  "environments": "Environments separate configuration, access, data, and release stages; names alone do not enforce isolation.",
  "approvals": "Approvals authorize high-impact transitions after evidence review, with accountable identity and audit history.",
  "runner security": "Runner security isolates untrusted jobs, limits credentials, and prevents persistence or cross-job data leakage.",
  "artifact repositories": "Artifact repositories store versioned build outputs with integrity, access, retention, and promotion policy.",
  "semantic versioning": "Semantic versioning communicates intended API compatibility through version components; it does not prove compatibility automatically.",
  "immutable releases": "Immutable releases promote the same identified artifact instead of rebuilding different content under the same version.",
  "promotion": "Promotion moves a verified artifact between release stages without losing its identity or evidence.",
  "rollback identity": "Rollback identity records the exact previous artifact and configuration to restore, not merely a mutable tag.",
  "feature flags": "Feature flags separate code deployment from selected behavior exposure, with ownership and eventual cleanup.",
  "dynamic config": "Dynamic configuration changes behavior without rebuilding, requiring validation, propagation, and rollback rules.",
  "deployment strategies": "Deployment strategies control how old and new instances coexist and how traffic shifts under health and rollback constraints.",
  "rolling": "Rolling deployment replaces instances incrementally, requiring compatible mixed-version operation.",
  "recreate": "Recreate deployment stops the old workload before starting the new one, accepting an availability gap.",
  "blue-green": "Blue-green deployment switches traffic between separate old and new environments, trading extra capacity for controlled cutover.",
  "feature release": "Feature release exposes behavior to users and can occur separately from deploying its code.",
  "database delivery": "Database delivery coordinates schema and data changes with application versions, lock limits, and recovery constraints.",
  "alerts": "Alerts request human or automated action when meaningful conditions occur; every alert needs an owner and response.",
  "dashboards": "Dashboards organize signals around operational questions rather than collecting unrelated charts.",
  "severity": "Severity classifies incident impact and urgency using explicit service and user consequences.",
  "command": "Incident command coordinates priorities, ownership, and decisions so responders do not work at cross purposes.",
  "communication": "Incident communication shares impact, current understanding, actions, and next update timing without unsupported certainty.",
  "mitigation": "Mitigation reduces current harm, possibly before the root cause is fully understood.",
  "capacity planning": "Capacity planning forecasts resource needs from workload, growth, failure headroom, and measured bottlenecks.",
  "autoscaling": "Autoscaling adjusts capacity from signals and policy, with delay, limits, and downstream capacity constraints.",
  "performance budgets": "Performance budgets set acceptable limits such as latency or resource cost and enforce them with representative measurements.",
  "desired state": "Desired state describes intended resources or behavior for comparison and reconciliation with actual state.",
  "plans": "Infrastructure plans preview intended changes; review replacements, deletions, drift, and assumptions before applying.",
  "modules": "Infrastructure modules package reusable resource definitions with explicit inputs, outputs, and ownership boundaries.",
  "policy checks": "Policy checks evaluate changes against security, cost, compliance, or reliability requirements before or during deployment.",
  "change review": "Change review examines impact, dependencies, evidence, rollout, and recovery for an intended modification.",
  "internal developer platforms": "Internal developer platforms provide supported delivery capabilities as a product for development teams.",
  "golden paths": "Golden paths offer maintained default workflows for common tasks without forcing every exceptional workload into one template.",
  "self-service": "Self-service lets authorized teams obtain capabilities through controlled interfaces without repetitive manual tickets.",
  "scorecards": "Scorecards summarize capability or quality signals; avoid turning incomplete metrics into a universal performance ranking.",
  "product thinking": "Product thinking evaluates a platform by user needs, adoption, friction, and outcomes rather than tool count.",
  "devsecops": "DevSecOps integrates security responsibilities and evidence into delivery instead of relegating them to a final approval stage.",
  "threat modeling": "Threat modeling identifies assets, adversaries, trust boundaries, and plausible misuse to prioritize controls.",
  "secure defaults": "Secure defaults make the ordinary configuration acceptably restricted without relying on every user to discover dangerous options.",
  "policy as code": "Policy as code expresses enforceable rules in versioned, testable form with controlled exceptions.",
  "compliance evidence": "Compliance evidence records how controls operated; documentation alone does not prove implementation or effectiveness.",
  "vulnerability response": "Vulnerability response assesses exposure, prioritizes mitigation, deploys fixes, and verifies closure.",
  "value stream": "A value stream traces work from need to delivered outcome, exposing delays and handoffs.",
  "infrastructure": "Infrastructure provides compute, network, storage, and supporting services with explicit provisioning and operational ownership.",
  "improvement": "Improvement changes a measured constraint or failure pattern and verifies that the change helped without unacceptable side effects."
});

Object.assign(TERM_CORRECTIONS.kubernetes ||= {}, {
  "garbage collection": "Kubernetes garbage collection removes dependent API objects according to owner references and deletion policy. Finalizers can delay removal for required cleanup; this is not language memory reclamation.",
  "pending pods": "Pending Pods have been accepted but are not yet set up to run; scheduling and image preparation are different possible causes. Inspect node assignment, conditions and events instead of treating the phase as a diagnosis.",
  "readiness gates": "Readiness gates require additional named Pod status conditions to be true before the Pod is ready, alongside container readiness. An external controller must publish the relevant condition; missing conditions are not success.",
  "ingress controllers": "An ingress controller watches routing configuration and configures or operates its HTTP proxy/load balancer implementation. An Ingress API object without a matching controller does not establish a traffic path.",
  "scaling limits": "Scaling limits bound replica or node growth through configured minimums, maximums, rate policies, quotas and available capacity. They differ from per-container CPU and memory cgroup limits.",
  "kubernetes purpose": "Kubernetes coordinates declarative workloads across machines; it does not remove application reliability or operational ownership.",
  "declarative apis": "Declarative APIs accept desired state that controllers attempt to reconcile with observed state.",
  "nodes": "Nodes supply execution capacity through kubelet, a container runtime, networking, and host resources.",
  "workloads": "Workloads are applications managed through Pods and controllers with lifecycle-specific guarantees.",
  "orchestration tradeoffs": "Orchestration trades manual coordination for controller, cluster, networking, and operational complexity.",
  "kubernetes objects": "Kubernetes objects are persisted API records describing desired or observed cluster resources.",
  "apiversion": "apiVersion selects an API group and version whose schema and behavior the server must support.",
  "kind": "kind names an API resource type, such as Deployment or Service.",
  "uids": "UIDs identify a particular object lifetime, distinguishing deletion and recreation under the same name.",
  "generations": "Generations track relevant desired-state changes; controller observedGeneration indicates which generation it has processed.",
  "admission": "Admission examines or mutates eligible API requests before persistence, after earlier authentication and authorization stages.",
  "conversion": "API conversion translates supported version representations while preserving the resource's intended semantics.",
  "persistence": "API persistence stores accepted state; acceptance does not mean controllers have already made the workload ready.",
  "audit": "API audit records selected request activity under a configured policy, with sensitive-data and retention implications.",
  "reconciliation loops": "Reconciliation loops repeatedly compare desired and observed state and perform bounded corrective actions.",
  "watches": "Watches stream resource changes; clients must handle disconnection, stale versions, and relisting.",
  "work queues": "Controller work queues schedule resource reconciliation and retries rather than preserving every event as a unique business action.",
  "owner references": "Owner references connect dependent object lifetimes to owners for garbage-collection behavior.",
  "finalizers": "Finalizers delay object removal until responsible controllers complete required cleanup; stuck controllers can leave deletion pending.",
  "filtering": "Scheduler filtering removes nodes that cannot satisfy a Pod's placement requirements.",
  "scoring": "Scheduler scoring ranks feasible nodes according to enabled placement policies.",
  "binding": "Binding records the selected node assignment for a Pod.",
  "plugins": "Scheduler plugins implement supported scheduling phases and policies under a configured framework.",
  "scheduling failure": "Scheduling failure means no acceptable placement was found or binding failed; inspect constraints and events before adding nodes blindly.",
  "cri": "The Container Runtime Interface connects kubelet to a supported container runtime implementation.",
  "container runtime": "The container runtime manages image and container execution operations requested by kubelet.",
  "pod sandbox": "A Pod sandbox establishes shared execution context such as networking for its containers.",
  "image pulls": "Image pulls retrieve required image content under registry credentials, policy, and node storage constraints.",
  "leases": "Leases record renewable coordination or heartbeat information used by supported cluster components.",
  "node failure": "Node failure affects Pods and attached resources; detection and replacement take time and do not guarantee application-level recovery.",
  "shared network": "Containers in a Pod share a network namespace and can communicate over localhost while sharing its port space.",
  "volumes": "Volumes expose storage to containers under a Pod and storage-provider lifecycle contract.",
  "restart policy": "Pod restart policy controls container restart behavior on the node; it does not by itself replace a deleted Pod.",
  "init containers": "Init containers perform setup before ordinary application containers proceed, under their configured lifecycle rules.",
  "sidecars": "Sidecars provide supporting behavior alongside application containers, with lifecycle rules depending on their declared form and supported version.",
  "ephemeral containers": "Ephemeral containers support targeted debugging of existing Pods and have restrictions distinct from normal application containers.",
  "conditions": "Conditions report typed observations such as readiness, not a complete explanation of application correctness.",
  "termination": "Termination coordinates lifecycle hooks, signals, grace periods, and eventual forceful stopping.",
  "grace periods": "Grace periods bound orderly termination; applications and attached work must fit within the configured time.",
  "replicasets": "ReplicaSets maintain a desired count of matching Pods and commonly sit beneath Deployments.",
  "rolling updates": "Rolling updates replace Pods gradually under availability and surge constraints.",
  "maxsurge": "maxSurge bounds additional Pods above the desired replica count during a Deployment rolling update.",
  "maxunavailable": "maxUnavailable bounds permitted unavailability during the relevant rollout strategy.",
  "progress": "Deployment progress records rollout advancement; a progress deadline failure does not automatically implement every desired rollback policy.",
  "pause": "Pausing a Deployment suspends further rollout processing while allowing controlled changes to accumulate.",
  "stable identity": "StatefulSet identities persist across Pod replacement by ordinal and naming conventions, unlike interchangeable stateless replicas.",
  "ordered rollout": "Ordered rollout coordinates replacement sequence, but application replication and readiness still determine actual safety.",
  "persistent storage": "Persistent storage outlives selected Pod lifetimes through volumes and claims under provider-specific guarantees.",
  "failure recovery": "Failure recovery coordinates replacement workloads and surviving state; controllers cannot repair arbitrary data corruption.",
  "daemonsets": "DaemonSets place node-local Pods on eligible nodes, commonly for networking, logging, or monitoring agents.",
  "node-local agents": "Node-local agents provide per-node services and need carefully limited privileges and resource budgets.",
  "update strategies": "Update strategies control how controller-managed Pods are replaced and which availability or ordering constraints apply.",
  "tolerations": "Tolerations allow Pods to tolerate matching taints; they do not force placement on those nodes.",
  "priority": "Pod priority influences scheduling and supported preemption behavior, not unlimited access to resources.",
  "system workload safety": "System workload safety protects essential cluster agents from disruptive rollout, excessive privilege, and resource starvation.",
  "jobs": "Jobs run work toward completion and may create replacement Pods; application effects must tolerate possible repeated execution.",
  "cronjobs": "CronJobs schedule Jobs over time with concurrency and missed-run policies, not an exactly-once clock guarantee.",
  "completions": "Job completions specify required successful work units under the configured completion mode.",
  "parallelism": "Job parallelism bounds concurrently active work, subject to scheduling and controller behavior.",
  "concurrency policy": "CronJob concurrency policy governs overlapping Jobs from that CronJob, not every external side effect or separately created Job.",
  "failure handling": "Failure handling specifies retry, termination, and diagnosis for workload failures rather than relying only on Pod replacement.",
  "clusterip": "ClusterIP exposes a Service through a stable cluster-internal virtual address under the cluster networking implementation.",
  "nodeport": "NodePort exposes a Service through a configured port on nodes, with reachability governed by networking and security policy.",
  "loadbalancer": "LoadBalancer requests an implementation-managed load balancer for a Service where supported.",
  "selectors": "Selectors match labels to choose resources or endpoints; accidental label matches can route traffic to the wrong Pods.",
  "kube-proxy": "kube-proxy implements Service traffic forwarding on many clusters; some networking implementations replace its role.",
  "traffic policies": "Service traffic policies influence endpoint locality and forwarding behavior, affecting source addresses and availability.",
  "gateway api": "Gateway API defines role-oriented resources for configuring supported traffic routing implementations.",
  "routes": "Gateway routes associate traffic-matching and backend rules with eligible parent listeners.",
  "listeners": "Listeners define accepted addresses, ports, protocols, and attachment policy on a Gateway.",
  "north-south traffic": "North-south traffic crosses between the cluster and external clients or systems.",
  "cluster dns": "Cluster DNS resolves supported Service and Pod-related names within configured search and forwarding rules.",
  "coredns": "CoreDNS commonly implements Kubernetes DNS using configurable plugins and upstream forwarding.",
  "service discovery": "Service discovery maps logical service identity to reachable endpoints without hard-coding replaceable Pod addresses.",
  "search domains": "DNS search domains expand relative names, potentially causing additional queries or unexpected resolution.",
  "ndots": "ndots influences when a resolver tries a name as absolute versus using search domains.",
  "dns debugging": "DNS debugging separates resolver configuration, search expansion, DNS responses, and actual endpoint connectivity.",
  "kubernetes network model": "The Kubernetes network model defines Pod addressing and communication expectations implemented by cluster networking components.",
  "pod ips": "Pod IPs identify Pod network endpoints and can change when Pods are replaced.",
  "encapsulation": "Some cluster networks wrap Pod traffic in another transport to cross the underlying network, adding overhead and MTU considerations.",
  "egress": "Egress is outbound traffic from workloads; policies and infrastructure controls determine permitted destinations.",
  "projected volumes": "Projected volumes combine supported data sources into a mounted filesystem view.",
  "downward api": "The Downward API exposes selected Pod or container metadata and resource information to workloads.",
  "reload": "Reload updates application behavior after configuration changes; mounted data updates do not guarantee the application rereads them.",
  "encryption": "Encryption protects data under configured transit or storage boundaries; Secret objects are not automatically secret merely because values are base64-encoded.",
  "external secret stores": "External secret stores supply credentials through integrations with separate identity, refresh, and failure contracts.",
  "cpu": "CPU requests affect scheduling while limits can enforce throttling; CPU is not reserved by simply observing low average usage.",
  "qos classes": "QoS classes categorize Pods from resource requests and limits and influence behavior under pressure.",
  "throttling": "CPU throttling limits execution when a configured quota is exhausted, potentially increasing latency without high node-wide CPU.",
  "oom": "Out-of-memory failures occur when memory allocation exceeds a relevant limit or available capacity and can cause process termination.",
  "eviction": "Eviction removes Pods under supported pressure or administrative policies; it differs from a container restart.",
  "pvcs": "PersistentVolumeClaims request storage with specified capacity and access requirements.",
  "storageclasses": "StorageClasses define provisioning policy for supported storage, including parameters and binding behavior.",
  "csi": "The Container Storage Interface connects orchestration to storage-provider operations under a standard contract.",
  "dynamic provisioning": "Dynamic provisioning creates storage to satisfy claims through an eligible class and provisioner.",
  "access modes": "Volume access modes describe supported mounting patterns; they are not a substitute for application-level concurrent-write coordination.",
  "reclaim policy": "Reclaim policy controls what happens to storage after its claim is released, affecting data retention and deletion.",
  "labels": "Labels are structured identifying attributes used for selection and grouping.",
  "annotations": "Annotations hold nonidentifying metadata for tools or operators rather than ordinary label selection.",
  "quotas": "ResourceQuotas limit aggregate resource use or counts within a namespace.",
  "limitranges": "LimitRanges constrain or default supported resource requests and limits within a namespace.",
  "multi-tenancy": "Multi-tenancy shares infrastructure among tenants while requiring explicit identity, network, resource, and data isolation.",
  "isolation boundaries": "Isolation boundaries define which failures, privileges, and resources are separated; a namespace alone is not a complete hostile-tenant boundary.",
  "serviceaccounts": "ServiceAccounts identify workload access to the Kubernetes API and supported identity integrations.",
  "clusterroles": "ClusterRoles define reusable RBAC permissions that may be bound at appropriate scopes.",
  "bindings": "RBAC bindings grant a role's permissions to selected subjects at a defined scope.",
  "verbs": "RBAC verbs identify permitted operations such as get, list, watch, create, or delete.",
  "resources": "RBAC resources identify API object categories on which permissions apply.",
  "subresources": "Subresources expose specialized operations such as status or exec that can require separate permissions.",
  "groups": "RBAC groups collect authenticated subjects under identity-provider or authentication conventions.",
  "capabilities": "Linux capabilities split selected root privileges; drop unnecessary capabilities rather than granting broad privilege.",
  "pod security standards": "Pod Security Standards define baseline security profiles for Pod configuration; enforcement depends on configured admission controls.",
  "horizontal pod autoscaler": "HPA changes replica counts from metrics and policy, subject to signal delay, readiness, and downstream capacity.",
  "vertical pod autoscaler": "VPA components recommend or adjust resource requests under their configured mode and workload-disruption constraints.",
  "cluster autoscaling": "Cluster autoscaling changes node capacity based on supported demand and removal policies, with provisioning delay.",
  "stabilization": "Autoscaling stabilization dampens rapid reversals by considering recent recommendations and policy windows.",
  "affinity": "Affinity attracts Pods toward nodes or other Pods matching placement criteria.",
  "anti-affinity": "Anti-affinity separates Pods from matching placements to reduce unwanted colocation.",
  "topology spread": "Topology spread constraints distribute Pods across failure or location domains under a maximum-skew policy.",
  "preemption": "Preemption may remove lower-priority Pods to make room for higher-priority work, without guaranteeing immediate successful placement.",
  "disruption budgets": "PodDisruptionBudgets constrain supported voluntary disruptions; they do not prevent all involuntary failures or guarantee rollout safety.",
  "drains": "Node drains evict eligible workloads before maintenance while respecting supported disruption and Pod rules.",
  "kubernetes observability": "Cluster observability correlates workload symptoms with events, metrics, logs, API activity, and control-plane state.",
  "api audit": "API audit provides request-level accountability under a chosen logging policy and retention boundary.",
  "control-plane signals": "Control-plane signals reveal API, scheduling, controller, and storage health that workload-only metrics can miss.",
  "slos": "SLOs define measurable service objectives; cluster health does not automatically imply the application meets them.",
  "kubernetes debugging": "Kubernetes debugging traces desired state, scheduling, startup, readiness, networking, and application behavior in order.",
  "networking": "Cluster networking connects Pods and Services under routing, DNS, and policy rules implemented by installed components.",
  "storage": "Cluster storage coordinates claims, provisioning, attachment, mounting, and application consistency across workload lifetimes.",
  "charts": "Helm charts package parameterized Kubernetes resources with release-related metadata and dependencies.",
  "templates": "Templates render manifests from inputs; valid rendering does not guarantee valid cluster behavior.",
  "values": "Helm values parameterize chart rendering; inspect merged effective values and avoid exposing secrets in rendered output.",
  "hooks": "Helm hooks run resources at selected lifecycle points, adding ordering, cleanup, and failure considerations.",
  "kustomize": "Kustomize transforms and composes manifests through bases and overlays without general-purpose text templating.",
  "overlays": "Overlays express environment-specific manifest changes over a reusable base.",
  "configuration management": "Configuration management versions intended state and controls how changes reach clusters and are reconciled.",
  "reconciliation": "Reconciliation repeatedly attempts to align observed resources with declared intent; failures must remain diagnosable.",
  "pull-based delivery": "Pull-based delivery lets an in-cluster or nearby controller fetch desired configuration and reconcile it under scoped credentials.",
  "operators": "Operators encode domain-specific operational reconciliation around custom or built-in resources.",
  "webhooks": "Webhooks extend supported API processing through external callbacks, adding availability, latency, and trust dependencies.",
  "api extension": "API extension adds resource schemas or behavior and must define versioning, validation, ownership, and lifecycle.",
  "cluster operations": "Cluster operations manage upgrades, capacity, credentials, backups, failures, and component compatibility.",
  "version skew": "Version-skew policies constrain supported component-version combinations; verify the specific release policy before upgrading.",
  "upgrades": "Upgrades change cluster components through a tested compatibility and rollback plan, including APIs used by workloads.",
  "node replacement": "Node replacement moves workloads and resources to new capacity, requiring disruption and storage considerations.",
  "eks boundaries": "EKS divides responsibility between AWS-managed control-plane capabilities and customer-owned workloads, configuration, identity, and supporting infrastructure.",
  "kubernetes production capstone": "The capstone demonstrates deployment, access, scaling, observability, and recovery with workload-level evidence rather than only successful manifests.",
  "architecture": "Cluster architecture assigns control, data, identity, network, and failure responsibilities to explicit components.",
  "delivery": "Delivery moves verified application artifacts and configuration into the cluster with controlled rollout and recovery.",
  "failure injection": "Failure injection deliberately introduces bounded faults in an authorized test environment to verify recovery assumptions."
});

Object.assign(TERM_CORRECTIONS["api-distributed-systems"] ||= {}, {
  "service ownership": "Service ownership assigns responsibility for contracts, data, operations, incidents, and compatible change.",
  "semantics": "Semantics defines what an operation means, including effects and guarantees, independently of its syntax.",
  "http methods": "HTTP methods convey standardized request intent; safety and idempotency are properties of intended semantics, not permission to ignore side effects.",
  "conditional behavior": "Conditional requests apply an operation only when supplied validators or dates satisfy protocol preconditions.",
  "uris": "URIs identify resources; design stable identifiers without exposing incidental database or deployment structure unnecessarily.",
  "collections": "Collections represent groups of resources with explicit pagination, filtering, ordering, and access rules.",
  "relationships": "API relationships connect resources through identifiers, links, or nested representations with clear ownership and authorization.",
  "action endpoints": "Action endpoints model operations that do not fit a simple field update; define their repeatability and lifecycle explicitly.",
  "media types": "Media types identify representation formats and interpretation rules, not merely filename extensions.",
  "compression": "Compression trades CPU and buffering for fewer bytes; define negotiation and decompression limits.",
  "localization": "Localization adapts representations to language or region while preserving stable machine-readable contracts.",
  "status codes": "HTTP status codes communicate protocol-level outcomes; pair them with consistent application error details.",
  "failure contracts": "Failure contracts define error categories, retry guidance, partial effects, and safe information for callers.",
  "unknown fields": "Unknown-field policy chooses rejection, ignoring, or preservation based on typo detection and compatibility needs.",
  "error aggregation": "Error aggregation reports several validation failures together without leaking sensitive information or doing unbounded work.",
  "deduplication records": "Deduplication records bind a logical operation identity to its request fingerprint, outcome, and retention policy.",
  "replayed responses": "Replayed responses return a stored outcome for the same logical operation instead of repeating its side effect.",
  "last-modified": "Last-Modified reports a representation's modification time for supported conditional requests, with timestamp precision limitations.",
  "optimistic concurrency": "Optimistic concurrency validates that observed state has not changed before applying an update.",
  "lost-update prevention": "Lost-update prevention rejects or coordinates competing updates so one writer does not silently overwrite another's work.",
  "stable ordering": "Stable ordering uses deterministic tie-breakers so pagination and repeated reads have a defined sequence.",
  "filtering": "Filtering selects matching resources under validated fields and bounded query complexity.",
  "sorting": "Sorting defines result order; restrict supported keys and include tie-breakers for predictable pagination.",
  "search": "Search retrieves ranked or matching resources under explicit query, indexing, freshness, and access rules.",
  "sparse fields": "Sparse fields let clients request selected representation fields, with per-field access and cost considerations.",
  "includes": "Includes expand related resources in a response and need depth, cardinality, and authorization limits.",
  "query complexity": "Query complexity measures potentially expensive combinations of filters, expansions, joins, and pagination.",
  "additive evolution": "Additive evolution introduces fields or operations without removing existing ones, but strict clients and new enum values can still break.",
  "sunset policy": "A sunset policy communicates deprecation timing, migration support, and eventual removal of supported behavior.",
  "validators": "HTTP validators such as ETags identify representation state for cache validation or preconditions; they are not input-schema validators.",
  "vary": "Vary identifies request fields affecting cached representation selection, preventing reuse under incompatible request preferences.",
  "cdns": "CDNs cache and serve content near clients under key, freshness, invalidation, and access-control policies.",
  "statelessness": "Stateless request handling carries the information needed to interpret each request rather than relying on hidden conversational server state.",
  "uniform interface": "A uniform interface uses consistent resource identification, representations, and interaction semantics to reduce client-server coupling.",
  "maturity tradeoffs": "API maturity choices balance standardized constraints and discoverability against client needs and implementation cost.",
  "code generation": "Code generation derives clients or schemas mechanically; generated code still needs compatibility and runtime tests.",
  "linting": "Contract linting catches inconsistent or prohibited API patterns before publishing them.",
  "contract governance": "Contract governance assigns review, versioning, ownership, and compatibility policy for shared APIs.",
  "streaming rpcs": "Streaming RPCs exchange multiple messages within one call and need flow control, cancellation, and partial-result semantics.",
  "status": "RPC status communicates completion or failure under the protocol's error model, separate from successful message delivery.",
  "resolvers": "GraphQL resolvers supply field values and can create hidden fan-out or authorization gaps if composed carelessly.",
  "batching": "Batching combines related operations to reduce overhead while preserving per-item authorization, limits, and failure reporting.",
  "federation": "GraphQL federation composes schemas across owners, introducing cross-service execution, compatibility, and failure concerns.",
  "signatures": "Webhook signatures authenticate message integrity under a shared or public-key scheme; replay prevention needs additional state or timing rules.",
  "timestamps": "Timestamps record clock readings, not universal causality; validate units, skew, and replay-window assumptions.",
  "event evolution": "Event evolution preserves interpretation for consumers that may read old and new payloads long after publication.",
  "bidirectional streams": "Bidirectional streams allow both peers to send messages independently under explicit framing and lifecycle rules.",
  "heartbeats": "Heartbeats provide evidence of recent communication; missing heartbeats suggest failure but cannot distinguish every delay from a crash.",
  "reconnects": "Reconnects restore communication but require resumption, replay, and duplicate policies to preserve application meaning.",
  "scopes": "Scopes describe delegated permission categories; resource- and tenant-specific authorization must still be enforced.",
  "tenant isolation": "Tenant isolation prevents one tenant's identity, data, or resource usage from crossing another's allowed boundary.",
  "delegation": "Delegation grants limited authority to act for another identity, with explicit scope, audience, and lifetime.",
  "audit": "Audit records security-relevant actions and decisions with accountable identity and protected retention.",
  "reverse proxies": "Reverse proxies accept client traffic and forward it to services, establishing trusted forwarding and policy boundaries.",
  "quotas": "Quotas bound use over a defined scope and period; choose identity and distributed accounting semantics deliberately.",
  "policy placement": "Policy placement decides which boundary can enforce a rule authoritatively without conflicting duplicate implementations.",
  "api security": "API security combines authentication, authorization, input limits, safe outputs, and protection against valid-looking abusive requests.",
  "abuse resistance": "Abuse resistance limits exploitation of legitimate functionality, such as enumeration or expensive query patterns.",
  "api observability": "API observability connects requests, failures, latency, and downstream effects without exposing sensitive payloads.",
  "correlation": "Correlation links related operations through identifiers; shared IDs aid diagnosis but do not prove causality by themselves.",
  "audit events": "Audit events record meaningful actions and authorization decisions, distinct from verbose debugging logs.",
  "api testing": "API testing exercises valid, invalid, unauthorized, concurrent, and versioned interactions against the contract.",
  "schema conformance": "Schema conformance checks representation structure; it does not establish business correctness or permissions.",
  "developer experience": "Developer experience measures how easily consumers discover, integrate, debug, and safely upgrade an API.",
  "documentation": "API documentation explains supported behavior, examples, errors, limits, authentication, and compatibility obligations.",
  "sdks": "SDKs package client interaction patterns and must preserve protocol semantics, error information, and version compatibility.",
  "changelogs": "Changelogs communicate observable changes and required consumer actions, not merely commit summaries.",
  "sandboxes": "API sandboxes provide isolated test behavior with documented differences from production.",
  "governance": "Governance defines who can change contracts, which checks apply, and how exceptions are reviewed.",
  "nodes": "Nodes are independent participants with local state and failure behavior, not a shared-memory abstraction.",
  "assumptions": "Assumptions state timing, failure, storage, and trust conditions required for a claimed guarantee.",
  "failure models": "Failure models define permitted faults such as crashes, omissions, partitions, or arbitrary behavior.",
  "crashes": "Crashes stop a participant's execution; recovery behavior depends on durable state and protocol design.",
  "omissions": "Omission failures lose a send, receive, or response while leaving uncertainty about other effects.",
  "network uncertainty": "Network uncertainty includes delay, loss, duplication, and reordering, making timeout-based conclusions incomplete.",
  "delay": "Delay postpones communication or work without necessarily losing it; deadlines limit waiting but do not undo completed effects.",
  "loss": "Loss removes a message or response from observation; the sender may not know whether an operation executed.",
  "duplication": "Duplication delivers repeated messages or attempts, requiring identity and effect-level handling.",
  "reordering": "Reordering changes arrival order relative to send order across relevant paths or attempts.",
  "tcp limits": "TCP orders bytes within a connection; it does not prove application completion, cross-connection order, or exactly-once effects.",
  "physical clocks": "Physical clocks estimate real time and can drift or jump, complicating timestamp ordering and deadlines.",
  "ntp": "NTP synchronizes clock estimates but does not create perfectly identical clocks or eliminate uncertainty.",
  "timestamp hazards": "Timestamp hazards include skew, jumps, precision loss, and treating wall-clock order as causal order.",
  "logical clocks": "Logical clocks track ordering relationships without claiming exact wall-clock time; different designs preserve different information.",
  "hybrid logical clocks": "Hybrid logical clocks combine physical-time estimates with logical ordering information under a defined update rule.",
  "percentiles": "Percentiles describe a latency distribution; averages can hide slow-tail behavior important to user requests.",
  "fan-out": "Fan-out sends one operation to multiple dependencies, multiplying work and exposing the caller to slow or failed branches.",
  "coordinated omission": "Coordinated omission occurs when a load generator stops issuing expected work during slow periods and understates latency impact.",
  "budget propagation": "Budget propagation passes remaining deadline or work allowance downstream rather than restarting a full budget at each hop.",
  "overload control": "Overload control bounds admission, queues, and concurrency so excess demand fails predictably instead of collapsing capacity.",
  "degradation": "Degradation deliberately reduces features or freshness to preserve essential service under pressure.",
  "dependency isolation": "Dependency isolation limits the resources one slow dependency can consume from unrelated work.",
  "commutativity": "Commutative operations produce the same result in either order under their stated model, reducing ordering requirements.",
  "monotonic operations": "Monotonic operations add information without retracting prior conclusions under a defined order, simplifying some coordination problems.",
  "broker internals": "Broker internals coordinate storage, replication, delivery, acknowledgements, and retention rather than merely forwarding messages.",
  "visibility": "Queue visibility controls which messages are available for delivery while work is pending; timeout expiry can cause redelivery.",
  "delivery semantics": "Delivery semantics describe message-delivery guarantees under failures, not automatically the number of external business effects.",
  "redelivery": "Redelivery retries message delivery after uncertain or failed processing and can repeat previously completed effects.",
  "duplicate effects": "Duplicate effects occur when repeated attempts apply a business action more than intended; deduplicate at the authoritative effect boundary.",
  "rebalancing": "Rebalancing changes partition or work ownership among consumers and must coordinate in-flight work and position tracking.",
  "parallelism": "Parallelism executes work simultaneously; preserving order usually limits parallelism within the required ordering scope.",
  "retry topics": "Retry topics separate delayed retries from primary traffic with explicit ordering and retention consequences.",
  "quarantine": "Quarantine isolates problematic work for inspection instead of retrying it indefinitely.",
  "replay": "Replay processes retained historical events again and requires version, idempotency, and external-effect policy.",
  "cdc": "Change data capture exposes database changes for downstream processing, with position, schema, and delivery semantics.",
  "atomic publication": "Atomic publication couples state change and event intent so a crash cannot silently leave only one committed; outbox patterns require a reliable relay.",
  "compensating actions": "Compensating actions counteract business effects but are not necessarily exact inverses or guaranteed to succeed.",
  "orchestration": "Orchestration centralizes workflow coordination while participants execute individual steps.",
  "choreography": "Choreography coordinates participants through events, distributing workflow understanding and failure diagnosis.",
  "workflow durability": "Workflow durability preserves progress and decisions across restarts so recovery does not depend only on process memory.",
  "distributed transactions": "Distributed transactions coordinate atomic decisions across participants under protocol-specific availability and recovery constraints.",
  "coordinator failure": "Coordinator failure can leave participants uncertain about a shared decision; recovery depends on durable protocol state.",
  "blocking": "Blocking waits for unavailable coordination or resources, potentially preserving safety while sacrificing progress.",
  "wal": "Write-ahead logging records recovery information before corresponding durable state changes are considered complete.",
  "commit index": "A replicated-log commit index marks the prefix considered committed under the protocol's rules.",
  "follower lag": "Follower lag means a replica has not received or applied the leader's latest progress, affecting reads and recovery.",
  "read scaling": "Read scaling distributes reads while preserving the chosen freshness and consistency contract.",
  "conflict detection": "Conflict detection identifies concurrent changes that cannot be silently combined under the application model.",
  "resolution": "Conflict resolution chooses or combines competing versions according to explicit domain rules.",
  "topology": "Topology describes communication and ownership relationships among participants and failure domains.",
  "convergence": "Convergence means replicas eventually agree under stated communication and update assumptions; agreement alone does not prove the chosen value is correct.",
  "quorums": "Quorums require intersecting participant sets under a protocol; simple count formulas alone do not prove linearizability.",
  "sloppy quorums": "Sloppy quorums may use temporary substitute nodes, affecting intersection and consistency assumptions.",
  "hinted handoff": "Hinted handoff temporarily stores updates intended for unavailable replicas and forwards them later.",
  "read repair": "Read repair reconciles divergent replica data discovered during reads, under the system's conflict rules.",
  "consistency models": "Consistency models specify permitted observation histories, such as linearizable, causal, or session-level guarantees.",
  "causal": "Causal consistency preserves cause-before-effect relationships while allowing independent operations to be observed differently.",
  "session": "Session guarantees constrain observations for a client's sequence of operations, such as read-your-writes.",
  "availability": "Availability must be defined precisely: protocol response guarantees and operational uptime percentages are different claims.",
  "partition keys": "Partition keys assign related data or work to ownership units, affecting locality, balance, and transaction scope.",
  "hotspots": "Hotspots concentrate demand on one key, partition, or resource despite overall spare capacity.",
  "scatter-gather": "Scatter-gather fans requests across partitions and combines results, increasing tail-latency and partial-failure complexity.",
  "replicated state machines": "Replicated state machines apply an agreed command sequence to deterministic state transitions across replicas.",
  "elections": "Elections select a leader under protocol rules; leadership must be tied to safety against stale or competing owners.",
  "log replication": "Log replication distributes ordered records and establishes commitment under a consensus or replication protocol.",
  "membership": "Membership tracks participating nodes and configuration changes, which must preserve protocol quorum assumptions.",
  "distributed locks": "Distributed locks coordinate ownership across participants; lease expiry requires fencing or equivalent protection against stale holders.",
  "split brain": "Split brain occurs when multiple participants act as authoritative owners without safe coordination.",
  "client-side": "Client-side load balancing lets callers choose endpoints using discovery and health information.",
  "server-side load balancing": "Server-side load balancing routes requests through an intermediary that selects backend endpoints.",
  "locality": "Locality favors nearby data or compute to reduce latency and transfer cost, while considering failure-domain concentration.",
  "layer 4": "Layer-4 balancing operates primarily on transport connections rather than application request semantics.",
  "layer 7 load balancing": "Layer-7 balancing uses application information such as HTTP routes, with protocol parsing and policy costs.",
  "active-passive": "Active-passive deployment keeps standby capacity for failover, requiring promotion and data-currency policy.",
  "active-active": "Active-active deployment serves work in multiple locations and must coordinate or resolve cross-location state changes.",
  "geo-routing": "Geo-routing chooses locations based on geography, latency, availability, or policy rather than one global endpoint rule.",
  "sovereignty": "Data sovereignty requirements constrain where data is stored, processed, or accessed; verify applicable legal and organizational rules separately.",
  "materialized views": "Materialized views store derived read models that need refresh, replay, and consistency policy.",
  "temporal queries": "Temporal queries ask about state at a time or across changes and require a clear event-time and validity model.",
  "bottlenecks": "Bottlenecks are the resources or coordination points limiting throughput or latency under the current workload.",
  "url shortener": "The URL-shortener exercise joins identifier allocation, redirects, abuse controls, caching, and durability under explicit scale assumptions.",
  "chat": "The chat exercise models message identity, ordering scope, delivery, reconnection, presence, and authorization.",
  "feed": "The feed exercise compares write-time and read-time fan-out with ranking, freshness, privacy, and hotspot constraints.",
  "file processing": "File processing coordinates upload, validation, durable work tracking, bounded execution, and result publication.",
  "ai inference": "AI inference serving combines model execution with admission, token budgets, cancellation, data safety, and latency/cost measurement.",
  "resilience": "Resilience preserves or restores useful behavior under faults through bounded work, isolation, recovery, and tested assumptions."
});

Object.assign(TERM_CORRECTIONS["international-interviews"] ||= {}, {
  "skill signals": "Skill signals are inspectable evidence of capability, such as a tested project or a well-explained incident, not a list of tools alone.",
  "keywords": "Keywords connect relevant experience to a role's language; use accurate terms rather than claiming unearned experience.",
  "headline": "A professional headline concisely states role focus and relevant value without unsupported seniority or impact claims.",
  "about": "An About section connects career focus, strengths, and selected evidence in a short coherent narrative.",
  "experience": "Experience describes personal scope, decisions, collaboration, and outcomes, separating your contribution from the team's total work.",
  "skills": "Skills are demonstrable abilities with a depth and recency, not simply technologies encountered once.",
  "pinned repositories": "Pinned repositories highlight a small set of relevant, understandable projects with runnable instructions and honest limitations.",
  "portfolio evidence": "Portfolio evidence includes code, tests, design decisions, measurements, and recovery demonstrations that support specific claims.",
  "readmes": "READMEs explain purpose, setup, usage, architecture, tests, and limitations so another engineer can inspect the project independently.",
  "demos": "Demos show observable behavior and selected failure cases; distinguish a prepared demonstration from real production operation.",
  "search queries": "Job-search queries combine role, location, technologies, and constraints to find relevant opportunities.",
  "filters": "Search filters narrow results by meaningful constraints without assuming every listing's metadata is complete or current.",
  "sourcing channels": "Sourcing channels include company sites, professional networks, communities, and referrals, each with different evidence and trust quality.",
  "personalized outreach": "Personalized outreach states a specific reason for contact, relevant evidence, and a small respectful request.",
  "follow-ups": "Follow-ups briefly renew a request with context and appropriate spacing, respecting a decline or lack of consent for repeated contact.",
  "analytics": "Search analytics track applications, responses, stages, and outcomes to identify where the process needs improvement.",
  "introduction": "An interview introduction connects current role, relevant experience, and motivation to the opportunity in a concise narrative.",
  "motivation": "Motivation explains why the role and work fit your interests and evidence, beyond generic praise of the employer.",
  "compensation": "Compensation includes salary, variable pay, equity, and benefits with different certainty and timing; compare stated terms rather than headline totals alone.",
  "availability": "Availability communicates realistic interview and start-date constraints without promising commitments you cannot meet.",
  "javascript": "JavaScript interview evidence includes value semantics, closures, asynchronous behavior, and debugging observable runtime behavior.",
  "python coding interviews": "Python coding interviews assess problem solving and language-aware correctness, including data structures, iteration, exceptions, and complexity.",
  "standard libraries": "Standard-library fluency means choosing supported built-in tools and explaining their behavior and costs instead of unnecessarily reimplementing them.",
  "execution": "Execution means producing and checking a working solution, not stopping at a plausible verbal algorithm.",
  "node.js": "Node.js interview evidence connects JavaScript execution to I/O, backpressure, modules, process lifecycle, and service failure behavior.",
  "python": "Python interview evidence demonstrates correct object, iteration, typing, concurrency, and resource-management reasoning in working code.",
  "react": "React interview evidence explains state ownership, render/commit behavior, effects, identity, accessibility, and measured performance.",
  "fastapi": "FastAPI interview evidence connects request validation, dependency lifetime, async execution, authorization, persistence, and deployment behavior.",
  "cloud": "Cloud interview evidence defends identity, network, compute, storage, reliability, and cost choices under a concrete workload.",
  "model behavior": "Model-behavior reasoning explains probabilistic outputs, context limits, failure modes, and what evaluation evidence supports deployment.",
  "rag": "RAG interview evidence covers retrieval quality, permissions, context construction, grounding, freshness, and evaluation rather than only vector-database selection.",
  "agents": "Agent interview evidence explains tool authority, state, termination, retries, cost limits, and recoverable failures.",
  "evaluations": "Evaluation evidence ties datasets, metrics, grading, critical slices, and uncertainty to a concrete release decision.",
  "safety": "Safety reasoning identifies harmful outcomes and controls, including tool permissions and data exposure, without claiming a prompt guarantees protection.",
  "cost": "Cost reasoning connects workload and usage to unit economics, including retries, idle resources, data transfer, and model tokens.",
  "principles": "Engineering principles guide decisions but require context and trade-offs rather than being repeated as universal slogans.",
  "conflict": "A conflict story explains competing goals, your actions, how agreement or escalation happened, and what changed.",
  "failure": "A failure story acknowledges your role, impact, recovery, and changed practice without fabricating a success narrative.",
  "ambiguity": "Ambiguity involves missing or conflicting information; senior evidence shows how you reduced uncertainty and made reversible decisions.",
  "project walkthroughs": "Project walkthroughs connect requirements, architecture, implementation, evidence, failures, and trade-offs through one coherent example.",
  "live questions": "Live questions test reasoning under changed constraints; clarify, state assumptions, and revise openly when evidence changes.",
  "communities": "Communities provide peer feedback and shared practice; participation should be relevant, respectful, and consistent with your preferences.",
  "outreach": "Outreach starts a relevant professional conversation with a clear, modest request rather than indiscriminate repeated messaging.",
  "official research": "Official research verifies work-authorization and relocation requirements through current government and employer sources; do not infer eligibility from anecdotes.",
  "recordings": "Practice recordings provide evidence of explanation clarity and pacing; obtain consent before recording other participants.",
  "improvement cycles": "Improvement cycles identify one gap, practice a targeted change, collect feedback, and retest after a delay."
});

export const COMMON_TERMS = Object.fromEntries(Object.entries({
  "recovery": "Recovery restores a usable state after failure. Specify which data and effects survive, how work resumes, and how restoration is verified.",
  "reliability": "Reliability is sustained correct service under stated conditions. Define failure, measure it, and test recovery instead of relying only on availability claims.",
  "security": "Security protects assets against a threat model using authorization, isolation, validation, and controlled resource use. Passing types or authentication alone is insufficient.",
  "operations": "Operations covers running, observing, changing, and recovering a service, with named owners and tested procedures.",
  "performance": "Performance describes latency, throughput, and resource cost under a defined workload. Report measurements and bottlenecks, not just asymptotic claims.",
  "concurrency": "Concurrency allows tasks to make overlapping progress. Shared state still needs an explicit ordering or ownership rule.",
  "constraints": "Constraints are limits a solution must respect, such as latency, memory, permissions, compatibility, or delivery time.",
  "requirements": "Requirements state observable needs and constraints. Separate required behavior from an implementation someone happens to prefer.",
  "scope|scope control": "Scope identifies what is included and excluded. State exclusions explicitly so a small solution is not mistaken for a complete system.",
  "actors": "Actors are people or external systems that interact with the system, each with goals, permissions, and failure possibilities.",
  "use cases": "Use cases describe an actor's goal, normal interaction, and alternative or failure outcomes.",
  "requirement discovery": "Requirement discovery turns ambiguous requests into examples, invariants, constraints, and acceptance criteria before design commitments.",
  "domain modeling": "Domain modeling represents business concepts, identity, rules, and transitions independently of incidental framework structure.",
  "entities": "Entities retain identity across changing attributes. Two records with identical fields need not represent the same entity.",
  "value objects": "Value objects represent meaning through their values rather than lifecycle identity, such as an amount with currency.",
  "identity": "Identity answers whether two references represent the same logical or runtime thing; equality may instead compare their contents.",
  "equality": "Equality compares values under a chosen rule. Specify structural, reference, or domain equality and how it relates to hashing.",
  "composition": "Composition builds behavior from collaborating parts with explicit ownership and contracts, rather than inheriting all behavior from a base class.",
  "aggregation": "Aggregation groups related objects whose lifetimes can remain independent of the grouping object.",
  "association|object relationships": "An association connects objects without necessarily implying ownership. Document direction, cardinality, and lifetime expectations.",
  "interfaces|protocols": "An interface or protocol describes observable operations and guarantees collaborators must satisfy. It does not by itself implement behavior.",
  "abstract base classes": "An abstract base class supplies shared structure or behavior while requiring subclasses to implement selected operations.",
  "dependency injection": "Dependency injection supplies collaborators from outside, making ownership and substitutions explicit. A constructor or function argument often suffices.",
  "substitutable adapters": "Substitutable adapters preserve the caller's behavioral contract, including errors and ordering, not merely method names.",
  "strategy": "Strategy makes one varying algorithm replaceable behind a stable contract, such as a pricing policy.",
  "factory": "A factory centralizes construction choices when callers should not know concrete creation details. A simple function may be sufficient.",
  "adapter": "An adapter translates an existing interface or representation into the contract a caller expects.",
  "decorator": "A decorator wraps behavior while preserving the wrapped contract, such as adding instrumentation without changing results or errors.",
  "observer": "Observer notifies registered subscribers when something changes. Define subscription lifetime, ordering, and error isolation.",
  "pattern selection by changing requirement": "Choose a pattern because a specific requirement varies; demonstrate the change it makes simpler before introducing the abstraction.",
  "state machines": "A state machine defines possible states and allowed transitions. Valid state shapes alone do not enforce valid transitions.",
  "transitions": "Transitions move a system between states under a condition or event. State the guard, side effects, and failure behavior.",
  "workflows": "A workflow coordinates dependent steps and their recovery. Durable workflows must distinguish retries from new attempts.",
  "invalid-state prevention": "Prevent invalid states by making required relationships explicit and rejecting transitions or inputs that violate them.",
  "thread-safe design": "Thread-safe design preserves invariants under permitted concurrent calls, using ownership, synchronization, or immutable data deliberately.",
  "repositories": "A repository exposes persistence operations in terms of a domain contract, including transaction and failure expectations.",
  "transactions": "Transactions group operations under a defined atomicity and isolation contract. Effects outside the transaction need separate coordination.",
  "persistence boundaries": "Persistence boundaries identify where in-memory decisions become durable and where partial failure or concurrent updates can intervene.",
  "errors": "Errors report failed operations. Preserve useful causes, distinguish expected failures, and avoid leaking sensitive internal details.",
  "testable design": "Testable design exposes controllable inputs and observable results while isolating nondeterministic clocks, I/O, and shared state.",
  "executable acceptance criteria": "Executable acceptance criteria turn required behavior into checks with concrete input, action, and expected outcome.",
  "exact arithmetic": "Exact arithmetic represents quantities without unintended rounding, such as integer minor currency units with an explicit rounding policy.",
  "balances": "Balances summarize obligations or holdings. Define sign conventions, currency, precision, and the invariant relating balances to underlying entries.",
  "settlement": "Settlement records discharge of an obligation. Distinguish a suggested transfer from one actually completed and recorded.",
  "simplification": "Simplification reduces representation or implementation cost while preserving specified invariants and observable behavior.",
  "allocation": "Allocation assigns a limited resource to a request according to eligibility and policy, handling concurrent contention explicitly.",
  "tickets": "Tickets identify an admitted activity or resource allocation and carry the data needed for validation and completion.",
  "pricing": "Pricing maps a defined usage and policy to a charge, with explicit time, currency, rounding, and version rules.",
  "extension seams": "Extension seams isolate a proven axis of change so new behavior can be added without rewriting unrelated rules.",
  "lru cache": "An LRU cache evicts the least recently used entry at capacity. A hash map plus doubly linked list can support constant-time lookup and recency updates.",
  "demo strategy": "A demo strategy shows the essential behavior, an edge case, and a failure or recovery path within the available time.",
  "review rubric": "A review rubric states the evidence used to judge correctness, design clarity, testing, and trade-offs consistently.",
  "tradeoffs|trade-offs": "A trade-off exchanges one desirable property for another under constraints. Name the cost, beneficiary, and condition that would change the decision.",
  "decisions": "A decision selects an option given evidence and constraints. Record alternatives, consequences, and a trigger for revisiting it.",
  "feedback": "Feedback compares observed behavior with an intended result so the next attempt can be adjusted.",
  "examples": "Examples give concrete inputs and expected outcomes. Include boundary and counterexamples so a rule's limits are visible.",
  "tests": "Tests compare actual behavior with an explicit expectation under controlled conditions. A passing test proves only the cases and assumptions it exercises.",
  "evidence": "Evidence is an inspectable observation or artifact supporting a specific claim, with context and limitations stated.",
  "context": "Context describes the conditions that give a decision or result meaning, including workload, ownership, versions, and constraints.",
  "options|alternatives": "Alternatives are plausible different choices, including doing less. Compare them against the same requirements and evidence.",
  "actions": "Actions are deliberate operations that change state or produce effects. Identify who owns them and whether they can be safely repeated.",
  "results": "Results are observed outcomes, not intentions. Separate measured impact from estimates and attribution from coincidence.",
  "clarification": "Clarification resolves ambiguity with a concrete question or example before assumptions become costly implementation choices.",
  "estimates|estimation": "Estimation approximates quantities from explicit assumptions. Show the calculation, plausible range, and sensitivity to uncertain inputs.",
  "complexity": "Complexity describes how resource use or reasoning burden grows. Distinguish algorithmic growth from implementation and operational complexity.",
  "optimization": "Optimization improves a measured objective without violating required behavior. Establish a baseline and verify the improvement under the same workload.",
  "brute force": "Brute force enumerates possibilities directly. It supplies a correctness baseline and can be the best choice at small scale.",
  "data": "Data is a representation of observations or state. Its meaning depends on schema, provenance, units, and freshness.",
  "apis|api": "An API exposes an interaction contract: inputs, outputs, failures, permissions, and compatibility obligations.",
  "scaling": "Scaling increases capacity for a defined workload. Identify the limiting resource and the new coordination or cost introduced.",
  "evolution": "Evolution changes a system while supporting existing users and data. Plan compatibility, rollout order, and rollback constraints.",
  "reproducible execution": "Reproducible execution records versions, configuration, dependencies, inputs, and commands so another run can meaningfully reproduce the result.",
  "cleanup": "Cleanup releases owned resources on success, failure, and cancellation. Ownership must be clear for cleanup to be reliable.",
  "parsing": "Parsing converts a representation into structured values. Syntax acceptance does not establish domain validity or permission.",
  "configuration": "Configuration supplies deployment-specific choices. Validate required values and ranges before accepting work.",
  "environment": "Environment values are external configuration, often strings or absent values. Parse them rather than trusting implicit conversion.",
  "metadata": "Metadata describes another object, such as identity, version, or ownership. It can affect behavior and may contain sensitive information.",
  "caching": "Caching reuses stored results to avoid repeated work. Define key scope, freshness, invalidation, memory limits, and behavior on misses.",
  "cache invalidation|invalidation": "Invalidation prevents stale cached data from being served after a relevant change. Coordinate keys, versions, and races explicitly.",
  "memory": "Memory holds active code and data. Measure retained objects, buffers, and peak allocation, not just average usage.",
  "i/o": "Input/output moves data between a process and external devices or systems. Waiting for I/O differs from consuming CPU.",
  "files": "Files expose named or referenced byte sequences through operating-system APIs, with permissions, lifecycle, and durability behavior.",
  "directories": "Directories map names to filesystem objects. Path resolution and permissions determine which object an operation reaches.",
  "processes": "Processes are operating-system execution units with their own resource and address-space context; they may contain multiple threads.",
  "threads": "Threads execute within a process and commonly share its memory, making concurrent access and lifetime coordination important.",
  "scheduling": "Scheduling decides which ready work gets execution resources and when. Queuing and priorities affect latency and fairness.",
  "resource limits|limits": "Resource limits bound consumption or admission, such as memory, CPU, payload size, or concurrency. Define rejection and recovery behavior.",
  "buffering": "Buffering temporarily stores data between producers and consumers. Unbounded buffering converts a throughput mismatch into memory exhaustion.",
  "flow control": "Flow control limits sending to what a receiver can accept. It differs from protecting the wider network against congestion.",
  "streams": "Streams expose a sequence of data over time. Define ordering, backpressure, cancellation, and resource ownership.",
  "events": "Events report something that occurred. Specify identity, ordering scope, delivery behavior, and payload evolution.",
  "state": "State is information retained between operations that affects future behavior. Identify its owner, lifetime, and consistency requirements.",
  "snapshots": "A snapshot captures a view of state at a defined boundary. State what consistency it guarantees and whether concurrent changes are included.",
  "failover": "Failover transfers responsibility after a failure. Detecting failure, preventing two active owners, and preserving acknowledged data are separate problems.",
  "consistency": "Consistency specifies which combinations and orderings of observations are permitted. Name the model instead of treating consistency as a boolean.",
  "partitions": "In distributed systems, a partition interrupts communication between some nodes. Local progress may continue without agreement on global state.",
  "replicas": "Replicas maintain copies of state. Replication policy determines lag, read behavior, failover, and possible data loss.",
  "latency": "Latency is elapsed time for an operation under a defined measurement boundary. Separate service time, queueing, and end-to-end delay.",
  "capacity": "Capacity is the workload a system can sustain while meeting constraints. Measure the bottleneck under realistic concurrency and failure conditions.",
  "capacity testing|load testing": "Load testing applies a defined workload and checks latency, errors, saturation, and recovery rather than only maximum throughput.",
  "isolation": "Isolation limits interference between activities or tenants. Specify whether it covers state visibility, resource contention, permissions, or failure propagation.",
  "routing": "Routing chooses where traffic or work goes based on destinations and policy. Examine the actual decision path when diagnosis differs from expectation.",
  "permissions|privileges": "Permissions authorize specific operations on resources. Grant only what is needed and distinguish identity from allowed actions.",
  "users": "Users are identities under which actions are attributed and authorized. Shared or overly privileged identities obscure accountability.",
  "dependencies": "Dependencies are components required for behavior or builds. Their versions, failure modes, and trust become part of the system's obligations.",
  "releases": "Releases identify a versioned set of artifacts and behavior made available to users. Track the immutable artifact, not only a branch name.",
  "retention": "Retention defines how long data or artifacts remain available. Balance recovery, cost, privacy, and replay requirements.",
  "namespaces": "Namespaces separate names or views of resources within a scope. They are not automatically complete security or resource-isolation boundaries.",
  "containers": "Containers package and run processes with operating-system isolation and resource controls; they usually share a host kernel.",
  "virtualization": "Virtualization presents an abstract machine or resource view. Distinguish guest-kernel isolation from process containers sharing a kernel.",
  "resource diagnosis": "Resource diagnosis connects symptoms to CPU, memory, I/O, queues, and limits using measurements before changing capacity or configuration.",
  "health|health checks": "Health checks report a specific capability. Separate process liveness, readiness for traffic, and deeper dependency or business checks.",
  "headers": "HTTP headers carry message metadata and control information. Parse them according to their field rules rather than treating every field as interchangeable text.",
  "urls": "URLs identify resources using a scheme and structured components. Parsing a URL does not establish that fetching it is authorized or safe.",
  "ports": "Transport ports identify communication endpoints within a host. Binding or publishing a port does not authenticate callers.",
  "sockets": "Sockets are operating-system endpoints for communication. Their lifecycle, buffering, and errors are distinct from application message boundaries.",
  "requests": "Requests ask a component to perform an operation. Define deadlines, identity, validation, and repeatability at the receiving boundary.",
  "responses": "Responses report an operation's outcome or representation. A lost response does not prove that the requested side effect did not happen.",
  "compatibility": "Compatibility preserves supported client behavior and data interpretation across changes. Test old and new participants during mixed-version rollout.",
  "ordering": "Ordering specifies which events or operations must be observed in sequence. State the scope: one connection, key, partition, or the entire system.",
  "backoff": "Backoff spaces repeated attempts to reduce pressure. Bound attempts and total time, and use jitter to avoid synchronized retries.",
  "dead letters": "Dead-letter storage holds work that could not be processed under the retry policy. It still needs ownership, diagnosis, retention, and safe replay.",
  "observability": "Observability provides evidence about internal behavior through outputs such as traces, metrics, and logs. Collect signals tied to decisions and failure modes.",
  "failure isolation": "Failure isolation confines a fault's impact using boundaries such as separate processes, queues, concurrency limits, or independent dependencies.",
  "draining": "Draining stops new work and allows bounded existing work to finish or be safely released before shutdown.",
  "shared memory": "Shared memory allows participants to access the same storage. It reduces copying but requires synchronization and explicit ownership.",
  "cpu work": "CPU work actively computes rather than waiting for external progress. Long CPU tasks can delay unrelated work sharing an execution thread.",
  "execution context": "Execution context is the environment needed to run work, including call state and associated request or task information.",
  "workflow": "A workflow coordinates steps toward an outcome, including prerequisites, failure paths, and ownership of retries or cleanup.",
  "observability": "Observability is the ability to investigate system behavior using its outputs, such as metrics, logs, traces, and profiles. Collect signals that can distinguish competing explanations for a failure.",
  "metrics": "Metrics are numerical measurements recorded over time, such as request counts, latency distributions, and queue depth. Their labels and aggregation determine which questions they can answer.",
  "logs": "Logs record individual events with relevant context. Include enough identity and timing to investigate behavior, while excluding secrets and unnecessary personal data.",
  "traces|tracing": "A distributed trace connects spans of work across an operation, such as an HTTP request and its database calls. Sampling and missing instrumentation limit what a trace proves.",
  "latency": "Latency is elapsed time between a defined start and completion boundary. Specify that boundary and report a distribution; an average can hide slow requests.",
  "throughput": "Throughput is completed work per unit of time. Increasing concurrent work beyond capacity can increase waiting without increasing throughput.",
  "backpressure": "Backpressure makes a producer slow down when a consumer cannot keep up. It limits queued work rather than allowing memory and latency to grow without bound.",
  "timeouts": "A timeout limits how long a caller waits. It does not prove that the underlying operation stopped or that a remote side effect did not happen.",
  "deadlines": "A deadline is the latest acceptable completion time. Each downstream operation should consume the remaining budget rather than starting a fresh full timeout.",
  "cancellation": "Cancellation requests that work stop. Each participating API must cooperate, and cleanup still needs an owner; cancelling a wait may leave an external operation running.",
  "retries": "A retry makes another attempt after failure or uncertainty. Bound attempts and elapsed time, and repeat side effects only when their contract supports safe retries.",
  "exponential backoff": "Exponential backoff increases the delay between attempts, usually up to a limit. It reduces repeated pressure but still needs a retry budget and safe operation semantics.",
  "jitter": "Jitter adds randomness to retry timing so many clients do not retry at the same instant. It spreads load without making an unsafe operation idempotent.",
  "idempotency": "Idempotency means repeating the same logical operation has the same intended effect as applying it once. It does not require every response, log, or timestamp to be identical.",
  "deduplication": "Deduplication identifies repeated work and avoids applying its effect again. Correctness depends on the identity, atomic claim, retention period, and stored outcome.",
  "contention": "Contention occurs when operations compete for a limited resource such as a lock, connection, or CPU. Waiting and retries can dominate the useful work.",
  "invariants": "An invariant is a condition that must stay true across the relevant state transitions. For example, available inventory must never become negative after accepting a reservation.",
  "atomic operations": "An atomic operation is indivisible at its stated observation boundary. Several individually atomic operations do not automatically form one atomic check-and-update sequence.",
  "immutability": "Immutability means a value cannot change after construction under its contract. A shallow immutable container may still refer to mutable nested objects.",
  "shallow copies": "A shallow copy creates a new outer container but reuses its nested values. Mutating a shared nested object can therefore affect both the original and the copy.",
  "deep copies": "A deep copy duplicates supported nested data. Define how it handles cycles, shared identity, custom types, and external resources before relying on it.",
  "ownership": "Ownership assigns responsibility for creating, changing, and releasing state or resources. Clear ownership explains who closes a connection or resolves a conflicting update.",
  "lifecycle": "A lifecycle is the sequence from creation through use to termination or release. Each transition needs a responsible component and defined failure behavior.",
  "contracts": "A contract states observable inputs, outputs, guarantees, errors, and constraints that callers may rely on. It includes failure behavior, not just a function signature.",
  "trust boundaries": "A trust boundary separates data or authority with different levels of trust. Validate input and enforce permission when crossing it before performing privileged work.",
  "authentication": "Authentication verifies evidence of a principal's identity. It does not by itself grant permission to every resource or action.",
  "authorization": "Authorization decides whether a principal may perform an action on a resource under the current policy. Enforce it at the server boundary, including alternate access paths.",
  "secrets": "Secrets are credentials or sensitive values that grant access, such as tokens and private keys. Limit their scope and lifetime, rotate them, and exclude them from source and logs.",
  "injection": "Injection occurs when untrusted data is interpreted as instructions by a command, query, template, or other interpreter. Preserve the data/code boundary with the appropriate safe API.",
  "least privilege": "Least privilege grants only the actions and resources needed for a task. It reduces the damage a bug or compromised identity can cause.",
  "validation": "Validation checks input against explicit rules before relying on it. Structure, length, range, allowed values, and business constraints may require different checks.",
  "schemas": "A schema describes allowed data structure and constraints. It helps validate representation but does not automatically prove authorization or business correctness.",
  "schema evolution": "Schema evolution changes a data contract while accounting for existing readers, writers, and stored data. Test mixed versions and replay, not just the newest producer and consumer.",
  "serialization": "Serialization converts a value into a representation for storage or transport. Define which types, identities, versions, and precision survive the round trip.",
  "encodings": "An encoding maps values such as text into bytes and back. Both sides must agree on the encoding and malformed-input policy.",
  "endianness": "Endianness is the byte order used to store a multi-byte number. Reading bytes with the wrong order produces a different value even when the buffer length is correct.",
  "garbage collection": "Garbage collection reclaims memory that is no longer needed under the runtime's reachability rules. Its timing is not a dependable way to close external resources.",
  "leaks": "A leak retains resources longer than their intended lifetime. Look for growing ownership paths such as listeners, caches, handles, or unfinished work under comparable load.",
  "debugging": "Debugging tests explanations for an observed failure. Reproduce the symptom, preserve evidence, isolate one cause, and verify a targeted change against the original case.",
  "breakpoints": "A breakpoint pauses execution at a selected location or condition so you can inspect values and control flow. Pausing can change concurrency and timing behavior.",
  "stack traces": "A stack trace records call locations associated with execution or failure. Match it to the deployed artifact and account for asynchronous boundaries and source maps.",
  "heap snapshots": "A heap snapshot records objects and their retaining relationships at a point in time. Compare snapshots under controlled workloads to investigate unexpected retention.",
  "cpu profiles": "A CPU profile samples or records where execution spends CPU time. It does not directly measure time spent waiting for an external service.",
  "assertions": "An assertion checks that a stated condition holds during a test or execution. Know whether your assertion mechanism throws, only logs, or can be disabled.",
  "unit tests": "Unit tests exercise a small behavioral boundary in isolation. Prefer observable inputs and results over assertions about incidental implementation steps.",
  "contract tests": "Contract tests check that a producer or adapter satisfies the behavior its consumers rely on, including errors and compatibility.",
  "integration tests": "Integration tests exercise cooperating components across a real boundary, such as an application and database. They can expose behavior that isolated fakes omit.",
  "property tests": "Property tests run many generated inputs against a general invariant. A failing case should be reduced to a small reproducible counterexample.",
  "table tests": "Table tests run one behavior against an explicit list of input and expected-result cases. Include normal, boundary, and failure inputs.",
  "fakes": "A fake is a simplified working substitute for a dependency, such as an in-memory store. Verify that its relevant behavior agrees with the real dependency.",
  "edge cases": "Edge cases exercise boundaries such as empty inputs, maximum sizes, duplicates, missing values, or concurrent transitions. Choose cases from the actual contract.",
  "reproducibility|reproducible experiments|reproducible checks": "Reproducibility means another run can use the recorded inputs, versions, configuration, and procedure to check the result. Record environmental differences and randomness where they matter.",
  "reproducible builds": "A reproducible build produces identical output from the same specified source and build inputs. Pinned dependencies help, but timestamps, toolchains, and environmental inputs also matter.",
  "environment variables": "Environment variables are string-valued process configuration inherited at process creation. Parse and validate them explicitly; a child changing its environment does not update its parent.",
  "exit codes": "An exit code reports process termination status. Zero conventionally means success; a shell pipeline may report only its last command unless the shell is configured otherwise.",
  "signals": "A signal notifies a process of an event such as termination. Some can be handled for cleanup; SIGKILL cannot be caught or used for graceful shutdown.",
  "file descriptors": "A file descriptor is a process-local integer referring to an open kernel-managed I/O resource. Close descriptors when their owning operation ends, including failure paths.",
  "ipc": "Inter-process communication transfers data or coordination between processes, using mechanisms such as pipes, sockets, or shared memory. Each mechanism has ownership and synchronization costs.",
  "locks": "A lock coordinates access to shared state. Protect the whole invariant, define lock ordering, and release the lock on every exit path.",
  "semaphores": "A semaphore controls access using a count of permits. It can bound concurrent operations, but the waiting queue and cancellation behavior also need limits.",
  "condition variables": "A condition variable lets a thread wait for a predicate protected by a lock. Recheck the predicate after waking because notification alone does not establish it.",
  "deadlocks": "A deadlock is a cycle of operations waiting for resources held by one another. Consistent ordering, bounded waits, and smaller critical sections can help prevent or recover from it.",
  "lock ordering": "Lock ordering requires code paths to acquire shared locks in a consistent order. It prevents a common class of circular waits when multiple locks are needed.",
  "dns|dns resolution": "DNS resolves names to records such as addresses using a distributed hierarchy and caches. Record lifetimes, resolver behavior, and negative caching affect changes and failures.",
  "tcp": "TCP provides a reliable ordered byte stream between endpoints. It does not preserve application message boundaries or prove that a remote business operation committed.",
  "udp": "UDP sends independent datagrams without providing TCP-style delivery, ordering, or retransmission guarantees. An application needing those properties must provide them elsewhere.",
  "tls": "TLS protects a connection's confidentiality and integrity and authenticates peers according to its configuration. It does not replace application-level authorization.",
  "certificates": "A certificate binds an identity or name to a public key under an issuer's signature. Verify the trust chain, intended name, validity, and applicable policy.",
  "http": "HTTP defines request and response semantics between clients, servers, and intermediaries. Methods, status codes, headers, caching, and preconditions are part of the contract.",
  "proxies": "A proxy handles traffic on behalf of another endpoint. Its routing, timeout, buffering, trust, and header behavior can change the end-to-end result.",
  "load balancers": "A load balancer selects a target for traffic. Define whether selection is per connection or request, how health is measured, and how existing traffic behaves during failure.",
  "queues": "A queue holds work waiting to be processed. Define capacity, admission, ordering, acknowledgement, retry, and expiration; a queue cannot create processing capacity.",
  "messages": "A message is a unit of information exchanged between components. Its identity, schema, ordering, and delivery contract determine how retries and duplicates are handled.",
  "commands": "A command expresses intent to perform an operation. The receiver may accept or reject it according to its current state and policy.",
  "domain events": "A domain event records a business fact that has already happened in a domain model. Decide separately whether and how that fact becomes a stable integration contract.",
  "idempotent consumers": "An idempotent consumer makes repeated delivery of the same logical event safe. Its deduplication record and local state change should share an atomic boundary.",
  "poison messages": "Poison messages repeatedly fail processing because of their content or an incompatible handler. Bound retries, preserve diagnostic evidence, and define quarantine and replay procedures.",
  "backfills|data backfills": "A backfill populates or repairs existing records after a model change. Use bounded batches, restartable progress, validation, and load controls so live traffic remains safe.",
  "backward compatibility": "Backward compatibility lets existing consumers keep working after a change. Include data, behavior, errors, and deployment order in that promise.",
  "read-only filesystems": "A read-only filesystem prevents writes at that mount boundary. Provide explicitly writable locations when needed and remember that other mounts may still be writable.",
  "seccomp": "Seccomp restricts which Linux system calls a process may make according to a policy. It reduces kernel attack surface but is not a complete isolation boundary by itself.",
  "apparmor": "AppArmor is a Linux security mechanism that restricts process access using profiles. Effective protection depends on the loaded policy and enforcement mode.",
  "selinux": "SELinux applies mandatory access control using security labels and policy. File permissions alone do not describe the complete access decision when it is enforcing.",
  "cgroups": "Linux control groups account for and limit resources used by groups of processes, such as CPU and memory. Resource controls complement namespace isolation.",
  "supply chain": "The software supply chain includes source, dependencies, build tools, artifacts, registries, and delivery systems. A compromise at any trusted stage can affect the shipped application.",
  "provenance": "Provenance records where an artifact came from and how it was built. Verify the identity and evidence; a signed artifact can still contain vulnerable or incorrect code.",
  "signing": "Signing lets a verifier check integrity and signer identity using cryptography. It does not establish that the signed content is safe or correct.",
  "attestations": "An attestation is a verifiable statement about an artifact or process, such as its build inputs. Evaluate who made the statement and which policy accepts it.",
  "drift": "Configuration drift is a difference between the intended configuration and the observed system. Detect the difference and decide whether to reconcile or deliberately change the declared intent.",
  "load shedding": "Load shedding rejects or drops work when capacity is insufficient. Define which work may be rejected and return a useful failure signal before queues collapse.",
  "saturation": "Saturation measures pressure at a resource's capacity limit, such as queued disk operations or exhausted connections. It can increase latency before CPU reaches 100%.",
  "error budgets": "An error budget is the allowed unreliability implied by an SLO over a specified window. Teams use its consumption to make delivery and reliability decisions.",
  "blameless postmortems": "A blameless postmortem explains an incident's conditions, impact, response, and contributing factors without treating individual blame as the solution. Assign concrete improvement actions and owners.",
  "rollback": "Rollback abandons uncommitted database changes or returns a deployed component to an earlier version, depending on the boundary. Code rollback does not automatically undo committed data, emitted events, or external effects.",
  "graceful shutdown": "Graceful shutdown stops admission, drains or safely releases bounded in-flight work, closes owned resources, and exits within a deadline.",
  "readiness": "Readiness describes whether an instance can currently accept the intended work. It differs from whether its process is alive and must be defined against the serving contract.",
  "disaster recovery": "Disaster recovery restores service after a major failure using tested procedures and recoverable data. Measure both restoration time and data loss against the stated objectives."
}).flatMap(([names, definition]) => names.split("|").map(name => [name, definition])));
